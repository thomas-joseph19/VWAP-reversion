---
phase: 01-data-pipeline-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/vwap_revert/data_pipeline/quality.py
  - src/vwap_revert/data_pipeline/cache.py
  - src/vwap_revert/cli.py
  - tests/data_pipeline/test_quality.py
  - tests/data_pipeline/test_cache.py
autonomous: true
requirements:
  - DATA-06
  - DATA-08
must_haves:
  truths:
    - "A full normalized dataset can be cached once to partitioned Parquet and reloaded as the phase-1 source of truth within the roadmap performance target."
    - "Audit artifacts explicitly report missing days, missing intervals, holidays, and unusable-day conditions."
    - "The phase output includes both the canonical cache and the reports needed to trust or inspect it."
  artifacts:
    - path: "src/vwap_revert/data_pipeline/cache.py"
      provides: "Partitioned Parquet writes and cache-backed reads"
      exports: ["write_canonical_cache", "scan_canonical_cache"]
    - path: "src/vwap_revert/data_pipeline/quality.py"
      provides: "Gap detection and quality-report generation"
      exports: ["detect_day_gaps", "detect_intraday_gaps", "build_quality_report"]
    - path: "src/vwap_revert/cli.py"
      provides: "Command-line build entrypoint for phase-1 normalization"
      exports: ["main"]
    - path: "tests/data_pipeline/test_cache.py"
      provides: "Automated coverage for DATA-06 cache layout"
    - path: "tests/data_pipeline/test_quality.py"
      provides: "Automated coverage for DATA-08 gap and holiday detection"
  key_links:
    - from: "src/vwap_revert/data_pipeline/cache.py"
      to: "src/vwap_revert/data_pipeline/sessions.py"
      via: "session-labeled normalized data is written partitioned by trading_date"
      pattern: "partition_cols|trading_date"
    - from: "src/vwap_revert/data_pipeline/quality.py"
      to: "src/vwap_revert/data_pipeline/contracts.py"
      via: "contract-filtered front-month days feed day-gap and interval-gap checks"
      pattern: "front_symbol|gap"
    - from: "src/vwap_revert/cli.py"
      to: "src/vwap_revert/data_pipeline/cache.py"
      via: "build command orchestrates normalization and cache/report writes"
      pattern: "write_canonical_cache|build_quality_report"
---

<objective>
Finish Phase 1 by adding canonical cache writes, data-gap reporting, and a command-line build entrypoint that produces the phase artifacts in one reproducible flow.

Purpose: Turn the lower-level ingestion/session modules into an auditable ETL build that downstream phases can reuse directly.
Output: Partitioned Parquet cache, gap/holiday quality reports, and a CLI command that materializes the phase-1 artifacts.
</objective>

<execution_context>
@C:/Users/pranav/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pranav/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-pipeline-foundation/01-CONTEXT.md
@.planning/phases/01-data-pipeline-foundation/01-RESEARCH.md
@.planning/phases/01-data-pipeline-foundation/01-VALIDATION.md
@.planning/phases/01-data-pipeline-foundation/01-01-PLAN.md
@.planning/phases/01-data-pipeline-foundation/01-02-PLAN.md

<interfaces>
From `src/vwap_revert/data_pipeline/contracts.py`:
```python
def build_daily_contract_map(df: pl.DataFrame) -> pl.DataFrame:
    ...

def apply_contract_map(df: pl.DataFrame, contract_map: pl.DataFrame) -> pl.DataFrame:
    ...
```

From `src/vwap_revert/data_pipeline/sessions.py`:
```python
def add_eastern_timestamps(df: pl.DataFrame, timestamp_col: str = "ts_recv") -> pl.DataFrame:
    ...

def label_sessions(df: pl.DataFrame) -> pl.DataFrame:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build gap detection and quality-report generation per D-11 and D-12</name>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/01-data-pipeline-foundation/01-CONTEXT.md
    .planning/phases/01-data-pipeline-foundation/01-RESEARCH.md
    src/vwap_revert/data_pipeline/quality.py
    tests/data_pipeline/test_quality.py
  </read_first>
  <files>src/vwap_revert/data_pipeline/quality.py, tests/data_pipeline/test_quality.py</files>
  <behavior>
    - Test 1: Missing business/trading dates are reported separately from a supplied holiday calendar.
    - Test 2: Intraday gaps larger than 300 consecutive seconds inside `rth` are flagged as unusable-day reasons.
    - Test 3: `build_quality_report()` includes counts and example timestamps for recoverable dropped rows.
  </behavior>
  <action>Implement `src/vwap_revert/data_pipeline/quality.py` with `detect_day_gaps(trading_dates: list[date], expected_dates: list[date], holiday_dates: set[date]) -> dict`, `detect_intraday_gaps(df: pl.DataFrame, max_rth_gap_seconds: int = 300) -> pl.DataFrame`, and `build_quality_report(...) -> dict`. Encode the planning threshold from research: a day is unusable when there is no contract map, when RTH has zero rows, or when any RTH gap exceeds `300` consecutive seconds; otherwise preserve the day and report the anomaly. Include output keys `missing_trading_dates`, `holiday_dates`, `unusable_days`, `recoverable_row_issues`, and `examples`. Write `tests/data_pipeline/test_quality.py` covering missing-day vs holiday separation, >300-second RTH gap detection, and report structure for recoverable row drops.</action>
  <acceptance_criteria>
    `src/vwap_revert/data_pipeline/quality.py` contains `max_rth_gap_seconds: int = 300`, `missing_trading_dates`, `holiday_dates`, and `unusable_days`
    `tests/data_pipeline/test_quality.py` contains `test_gap_detection_and_holiday_reporting` and `test_structural_vs_recoverable_errors`
    `python -m pytest tests/data_pipeline/test_quality.py -x` exits successfully
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/data_pipeline/test_quality.py -x</automated>
  </verify>
  <done>The pipeline has explicit, test-backed rules for day gaps, interval gaps, holidays, and unusable-day thresholds, and it can serialize those findings into a reusable quality report.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the canonical partitioned Parquet cache, CLI build command, and cache-reload benchmark per D-07, D-08, and D-09</name>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/01-data-pipeline-foundation/01-CONTEXT.md
    .planning/phases/01-data-pipeline-foundation/01-RESEARCH.md
    src/vwap_revert/data_pipeline/cache.py
    src/vwap_revert/cli.py
    tests/data_pipeline/test_cache.py
  </read_first>
  <files>src/vwap_revert/data_pipeline/cache.py, src/vwap_revert/cli.py, tests/data_pipeline/test_cache.py</files>
  <behavior>
    - Test 1: `write_canonical_cache()` writes a Parquet dataset partitioned by `trading_date`.
    - Test 2: `scan_canonical_cache()` reloads the partitioned dataset as a lazy frame.
    - Test 3: The CLI exposes a `build-phase1-cache` command that writes the canonical cache, contract map, schema summary, and quality report to sibling output paths.
    - Test 4: The CLI records first-build and cache-backed reload durations in a benchmark artifact and flags whether the reload met the `< 30 seconds` roadmap target.
  </behavior>
  <action>Implement `src/vwap_revert/data_pipeline/cache.py` with `write_canonical_cache(df: pl.DataFrame, output_dir: Path) -> None` using `use_pyarrow=True`, `compression="zstd"`, and `pyarrow_options={"partition_cols": ["trading_date"]}`, plus `scan_canonical_cache(output_dir: Path) -> pl.LazyFrame`. Implement `src/vwap_revert/cli.py` with a `main()` entrypoint and an argparse subcommand named `build-phase1-cache` accepting `--csv-root`, `--output-root`, and `--holiday-calendar`. The command should orchestrate manifest discovery, raw scan, contract-map creation, ET/session labeling, quality-report generation, Parquet cache writing, and sibling artifact writes named `contract_roll_map.parquet`, `schema_summary.json`, `data_quality_report.json`, and `cache_benchmark.json` under the chosen output root. In the same command, measure wall-clock seconds for the first CSV-to-Parquet build and a second cache-backed reload, then write JSON keys `first_build_seconds`, `cache_reload_seconds`, and `reload_under_30_seconds`. Write `tests/data_pipeline/test_cache.py` to assert the partitioned `trading_date=` directory layout, lazy reload behavior, and benchmark artifact structure. Keep the CLI focused on Phase 1 only; do not add Phase 2 VWAP logic.</action>
  <acceptance_criteria>
    `src/vwap_revert/data_pipeline/cache.py` contains `partition_cols`, `trading_date`, and `compression="zstd"`
    `src/vwap_revert/cli.py` contains `build-phase1-cache`, `contract_roll_map.parquet`, `schema_summary.json`, `data_quality_report.json`, `cache_benchmark.json`, and `reload_under_30_seconds`
    `tests/data_pipeline/test_cache.py` contains `test_partitioned_cache_layout` and `test_cache_benchmark_artifact_records_reload_target`
    running `python -m vwap_revert.cli build-phase1-cache --csv-root <fixture_csv_root> --output-root <tmp_output>` writes `cache_benchmark.json` with keys `first_build_seconds`, `cache_reload_seconds`, and `reload_under_30_seconds`
    `python -m pytest tests/data_pipeline/test_cache.py tests/data_pipeline/test_quality.py -x` exits successfully
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/data_pipeline/test_cache.py tests/data_pipeline/test_quality.py -x</automated>
  </verify>
  <done>Phase 1 can materialize one canonical normalized Parquet dataset plus the required audit artifacts through a single reproducible CLI command, and it records whether cache-backed reload met the roadmap `< 30 seconds` target.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/data_pipeline/test_cache.py tests/data_pipeline/test_quality.py -x` and then smoke the CLI with a temporary fixture dataset to confirm the canonical cache, sibling artifacts, and `cache_benchmark.json` are written together with a recorded reload pass/fail flag.
</verification>

<success_criteria>
Plan 03 is complete when the repository can build a partitioned phase-1 cache, reload it lazily, emit explicit quality/roll/schema artifacts that document data gaps and market closures, and record benchmark evidence for the reload-under-30-seconds success criterion.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-pipeline-foundation/01-03-SUMMARY.md`
</output>
