---
phase: 01-data-pipeline-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/vwap_revert/data_pipeline/contracts.py
  - src/vwap_revert/data_pipeline/sessions.py
  - tests/data_pipeline/test_contracts.py
  - tests/data_pipeline/test_sessions.py
autonomous: true
requirements:
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
must_haves:
  truths:
    - "Each trading day resolves to one front-month NQ contract using same-day outright trade volume only, even before persisted session labeling exists."
    - "The normalized dataset keeps timestamps in America/New_York with DST-safe trading-date assignment."
    - "Every row exposes reusable session labels so later phases never recompute Globex calendar rules."
  artifacts:
    - path: "src/vwap_revert/data_pipeline/contracts.py"
      provides: "Causal front-month mapping, ET trading-date derivation, and explicit roll artifact"
      exports: ["add_contract_trading_date", "build_daily_contract_map", "apply_contract_map"]
    - path: "src/vwap_revert/data_pipeline/sessions.py"
      provides: "UTC-to-ET conversion and persisted session labels"
      exports: ["add_eastern_timestamps", "label_sessions"]
    - path: "tests/data_pipeline/test_contracts.py"
      provides: "Automated coverage for daily volume-based selection and roll changes"
    - path: "tests/data_pipeline/test_sessions.py"
      provides: "Automated coverage for DST and Globex boundary behavior"
  key_links:
    - from: "src/vwap_revert/data_pipeline/contracts.py"
      to: "src/vwap_revert/data_pipeline/ingest.py"
      via: "trade-only outright rows feed same-day volume aggregation"
      pattern: "scan_candidate_outrights|trade_volume"
    - from: "src/vwap_revert/data_pipeline/sessions.py"
      to: "src/vwap_revert/data_pipeline/contracts.py"
      via: "contract-filtered rows receive ET-local timestamps and trading_date labels"
      pattern: "America/New_York|trading_date"
    - from: "tests/data_pipeline/test_sessions.py"
      to: "src/vwap_revert/data_pipeline/sessions.py"
      via: "DST and session-boundary assertions"
      pattern: "test_dst_transition_dates|test_globex_boundary_labels"
---

<objective>
Implement the causal front-month roll logic and ET session-labeling layer that convert raw candidate rows into daily normalized front-month NQ timeseries.

Purpose: Lock down the two most important phase-1 policies: no-look-ahead contract selection and canonical session semantics.
Output: Contract-map generation, contract-filter application, ET conversion, and persisted trading/session columns with automated tests.
</objective>

<execution_context>
@C:/Users/pranav/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pranav/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-pipeline-foundation/01-CONTEXT.md
@.planning/phases/01-data-pipeline-foundation/01-RESEARCH.md
@.planning/phases/01-data-pipeline-foundation/01-VALIDATION.md
@.planning/phases/01-data-pipeline-foundation/01-01-PLAN.md

<interfaces>
From `src/vwap_revert/data_pipeline/manifest.py`:
```python
class ManifestEntry:
    csv_path: Path
    session_date: date
    stem: str

def build_manifest(root: Path) -> list[ManifestEntry]:
    ...
```

From `src/vwap_revert/data_pipeline/ingest.py`:
```python
def scan_raw_csv(path: Path) -> pl.LazyFrame:
    ...

def scan_candidate_outrights(path: Path) -> pl.LazyFrame:
    ...

def collect_sample_day(path: Path) -> pl.DataFrame:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement same-day outright volume contract selection with in-task ET trading-date derivation per D-01, D-02, D-03, D-04, and D-05</name>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/01-data-pipeline-foundation/01-CONTEXT.md
    .planning/phases/01-data-pipeline-foundation/01-RESEARCH.md
    src/vwap_revert/data_pipeline/manifest.py
    src/vwap_revert/data_pipeline/ingest.py
    src/vwap_revert/data_pipeline/contracts.py
    tests/data_pipeline/test_contracts.py
  </read_first>
  <files>src/vwap_revert/data_pipeline/contracts.py, tests/data_pipeline/test_contracts.py</files>
  <behavior>
    - Test 1: `add_contract_trading_date()` derives ET-local `trading_date` from `ts_recv` using the 18:00 Globex boundary before volume aggregation.
    - Test 2: `build_daily_contract_map()` picks the symbol with the highest same-day outright trade volume for each derived `trading_date`.
    - Test 3: A day with no outright trade rows raises a front-month determination failure instead of producing a silent null mapping.
    - Test 4: A roll day where leadership changes records the new symbol in the contract map instead of blending contracts.
    - Test 5: `apply_contract_map()` returns only the mapped symbol rows for each day and raises when a day has no mapping.
  </behavior>
  <action>Implement `src/vwap_revert/data_pipeline/contracts.py` with `add_contract_trading_date(df: pl.DataFrame, timestamp_col: str = "ts_recv") -> pl.DataFrame`, `build_daily_contract_map(df: pl.DataFrame) -> pl.DataFrame`, and `apply_contract_map(df: pl.DataFrame, contract_map: pl.DataFrame) -> pl.DataFrame`. Inside Task 1 itself, derive ET-local `trading_date` from `ts_recv` using `zoneinfo.ZoneInfo("America/New_York")` and the locked 18:00 ET Globex boundary so contract selection does not depend on Task 2’s persisted session module. Then use only outright `NQ` rows with `side` in `{"A","B"}` for volume aggregation, group by derived `trading_date` and `symbol`, sum `size` into `trade_volume`, sort by `trading_date`, `trade_volume` descending, and `symbol` ascending as the tiebreaker, then emit one row per day with columns `trading_date`, `front_symbol`, `trade_volume`, `is_roll`, and `prior_symbol`. If a day has no eligible outright trade rows, raise `ValueError("Unable to determine front-month contract")` per D-10. `apply_contract_map()` must inner-join on `trading_date`, keep only `symbol == front_symbol`, and raise `ValueError` if any day in the input lacks a contract map. Write `tests/data_pipeline/test_contracts.py` to cover ET trading-date derivation, leadership selection, symbol-change roll recording, no-trade front-month failure, and missing-map failure.</action>
  <acceptance_criteria>
    `src/vwap_revert/data_pipeline/contracts.py` contains `def add_contract_trading_date`, `def build_daily_contract_map`, `def apply_contract_map`, `front_symbol`, and `Unable to determine front-month contract`
    `tests/data_pipeline/test_contracts.py` contains `test_contract_trading_date_uses_globex_boundary`, `test_front_month_selected_by_daily_volume`, `test_roll_map_records_symbol_changes`, `test_front_month_failure_when_no_trade_rows_exist`, and `test_apply_contract_map_requires_daily_mapping`
    `python -m pytest tests/data_pipeline/test_contracts.py -x` exits successfully
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/data_pipeline/test_contracts.py -x</automated>
  </verify>
  <done>The codebase can derive ET trading dates before session persistence, build an auditable front-month map for each day using only same-day outright trade volume, and fail fast when a front-month cannot be determined.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist ET timestamps, trading_date, and session flags per D-04, D-05, and D-06</name>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/01-data-pipeline-foundation/01-CONTEXT.md
    .planning/phases/01-data-pipeline-foundation/01-RESEARCH.md
    src/vwap_revert/data_pipeline/sessions.py
    tests/data_pipeline/test_sessions.py
  </read_first>
  <files>src/vwap_revert/data_pipeline/sessions.py, tests/data_pipeline/test_sessions.py</files>
  <behavior>
    - Test 1: UTC timestamps around March and November DST transitions convert correctly to `America/New_York`.
    - Test 2: Rows from `18:00:00` ET onward assign to the next Globex `trading_date`.
    - Test 3: Session labels produce `overnight`, `rth`, and `maintenance` plus boolean columns `is_overnight`, `is_rth`, and `is_maintenance`.
  </behavior>
  <action>Implement `src/vwap_revert/data_pipeline/sessions.py` with `add_eastern_timestamps(df: pl.DataFrame, timestamp_col: str = "ts_recv") -> pl.DataFrame` and `label_sessions(df: pl.DataFrame) -> pl.DataFrame`. Parse the UTC nanosecond timestamp column into timezone-aware ET output using `zoneinfo.ZoneInfo("America/New_York")`, add `ts_recv_et`, derive `trading_date` with the locked 18:00 ET boundary, classify `session_name` as `maintenance` for `17:00:00-17:59:59`, `overnight` for `18:00:00-09:29:59`, and `rth` for `09:30:00-16:59:59`, and emit boolean flag columns for each session. Write tests in `tests/data_pipeline/test_sessions.py` that explicitly assert expected ET wall times and `trading_date` values across known DST boundaries and Globex session splits.</action>
  <acceptance_criteria>
    `src/vwap_revert/data_pipeline/sessions.py` contains `ZoneInfo("America/New_York")`, `session_name`, `is_rth`, and `def label_sessions`
    `tests/data_pipeline/test_sessions.py` contains `test_dst_transition_dates` and `test_globex_boundary_labels`
    `python -m pytest tests/data_pipeline/test_sessions.py -x` exits successfully
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/data_pipeline/test_sessions.py -x</automated>
  </verify>
  <done>Contract-filtered rows carry canonical ET timestamps, Globex trading dates, and reusable session columns that downstream plans can consume directly.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/data_pipeline/test_contracts.py tests/data_pipeline/test_sessions.py -x` and confirm the contract-map task derives `trading_date` internally, fails fast on unmappable days, and still passes the separate session-labeling tests.
</verification>

<success_criteria>
Plan 02 is complete when daily front-month selection, roll-map output, ET timezone conversion, and row-level session labels satisfy DATA-02 through DATA-05 with automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-pipeline-foundation/01-02-SUMMARY.md`
</output>
