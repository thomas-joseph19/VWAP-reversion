---
phase: 01-data-pipeline-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/vwap_revert/__init__.py
  - src/vwap_revert/data_pipeline/__init__.py
  - src/vwap_revert/data_pipeline/schema.py
  - src/vwap_revert/data_pipeline/manifest.py
  - src/vwap_revert/data_pipeline/ingest.py
  - tests/conftest.py
  - tests/data_pipeline/test_ingest.py
autonomous: true
requirements:
  - DATA-01
  - DATA-07
must_haves:
  truths:
    - "A raw CME BBO CSV day can be loaded through one public ingestion path without ad hoc parsing code."
    - "Only outright NQ candidate rows enter normalization inputs; spread rows stay excluded before downstream logic."
    - "Structural input failures stop the build immediately instead of leaking corrupt inputs into later phases."
  artifacts:
    - path: "src/vwap_revert/data_pipeline/ingest.py"
      provides: "Typed lazy CSV loader and outright-NQ filtering entrypoint"
      exports: ["scan_raw_csv", "scan_candidate_outrights", "collect_sample_day"]
    - path: "src/vwap_revert/data_pipeline/schema.py"
      provides: "Required columns, dtypes, and structural validation helpers"
      exports: ["RAW_SCHEMA", "REQUIRED_COLUMNS", "validate_required_columns", "parse_timestamp_column"]
    - path: "src/vwap_revert/data_pipeline/manifest.py"
      provides: "Deterministic daily CSV discovery and ordering"
      exports: ["build_manifest", "ManifestEntry"]
    - path: "tests/data_pipeline/test_ingest.py"
      provides: "Automated coverage for DATA-01 and DATA-07 behaviors"
  key_links:
    - from: "src/vwap_revert/data_pipeline/manifest.py"
      to: "src/vwap_revert/data_pipeline/ingest.py"
      via: "manifest entries feed scan functions"
      pattern: "build_manifest|scan_raw_csv"
    - from: "src/vwap_revert/data_pipeline/schema.py"
      to: "src/vwap_revert/data_pipeline/ingest.py"
      via: "explicit schema and required-column validation"
      pattern: "RAW_SCHEMA|validate_required_columns"
    - from: "tests/data_pipeline/test_ingest.py"
      to: "src/vwap_revert/data_pipeline/ingest.py"
      via: "pytest assertions against public ingestion functions"
      pattern: "scan_candidate_outrights|collect_sample_day"
---

<objective>
Create the Phase 1 project foundation and raw-ingestion layer so later plans can build contract selection, session logic, cache writes, and quality reporting on stable typed inputs.

Purpose: Establish the canonical package layout, deterministic raw file manifest, and explicit fail-fast input validation for the data pipeline.
Output: A runnable Python package with ingestion primitives and tests covering raw CSV loading, unreadable-input failures, and structural timestamp/schema validation.
</objective>

<execution_context>
@C:/Users/pranav/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pranav/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-data-pipeline-foundation/01-CONTEXT.md
@.planning/phases/01-data-pipeline-foundation/01-RESEARCH.md
@.planning/phases/01-data-pipeline-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the Python package, tooling, and deterministic manifest contract</name>
  <read_first>
    .planning/PROJECT.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/01-data-pipeline-foundation/01-CONTEXT.md
    .planning/phases/01-data-pipeline-foundation/01-RESEARCH.md
    pyproject.toml
    src/vwap_revert/__init__.py
    src/vwap_revert/data_pipeline/__init__.py
    src/vwap_revert/data_pipeline/manifest.py
  </read_first>
  <files>pyproject.toml, src/vwap_revert/__init__.py, src/vwap_revert/data_pipeline/__init__.py, src/vwap_revert/data_pipeline/manifest.py, tests/conftest.py</files>
  <action>Create a `pyproject.toml` for a package named `vwap-revert` targeting Python `>=3.14` with runtime dependencies `polars>=1.39.3,<1.40`, `pyarrow>=23.0.1,<24`, `numpy>=2.4.4,<3`, and `tzdata>=2025.3`; add an optional `dev` dependency group containing `pytest>=9.0.2,<10` and `ruff>=0.13,<0.14`. Configure pytest so `python -m pytest` discovers `tests/`. Create `src/vwap_revert/__init__.py` and `src/vwap_revert/data_pipeline/__init__.py` exporting the public data-pipeline modules. In `src/vwap_revert/data_pipeline/manifest.py`, implement a deterministic `ManifestEntry` dataclass with fields `csv_path`, `session_date`, and `stem`, plus `build_manifest(root: Path) -> list[ManifestEntry]` that scans `*.csv`, derives the date from the filename or file stem, sorts ascending by date then name, and raises `FileNotFoundError` when the root is missing and `ValueError` when no CSVs are found. Add `tests/conftest.py` with shared fixture helpers for temporary CSV roots and small inline sample files.</action>
  <acceptance_criteria>
    `pyproject.toml` contains the strings `polars>=1.39.3,<1.40`, `tzdata>=2025.3`, and `[tool.pytest.ini_options]`
    `src/vwap_revert/data_pipeline/manifest.py` contains `class ManifestEntry`, `def build_manifest`, and `raise ValueError("No CSV files found`
    `src/vwap_revert/data_pipeline/__init__.py` exports `manifest`
    `tests/conftest.py` defines at least one `@pytest.fixture`
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/data_pipeline/test_ingest.py -x</automated>
  </verify>
  <done>Package metadata exists, pytest discovery is configured, and the raw CSV manifest contract is implemented with deterministic ordering and explicit missing-input failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement typed CSV ingestion and full D-10 fail-fast validation per D-10 and D-11</name>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/01-data-pipeline-foundation/01-CONTEXT.md
    .planning/phases/01-data-pipeline-foundation/01-RESEARCH.md
    src/vwap_revert/data_pipeline/manifest.py
    src/vwap_revert/data_pipeline/schema.py
    src/vwap_revert/data_pipeline/ingest.py
    tests/conftest.py
    tests/data_pipeline/test_ingest.py
  </read_first>
  <files>src/vwap_revert/data_pipeline/schema.py, src/vwap_revert/data_pipeline/ingest.py, tests/data_pipeline/test_ingest.py</files>
  <behavior>
    - Test 1: A sample raw CSV with the required 17 Databento columns loads through `collect_sample_day()` and returns only outright `NQ` symbols, excluding spread symbols like `NQH4-NQM4`.
    - Test 2: Missing required columns raise a structural validation exception before collection.
    - Test 3: An unreadable or missing CSV path fails fast with `FileNotFoundError` before lazy collection continues.
    - Test 4: Invalid `ts_recv` timestamp values fail fast with a timestamp-parse exception instead of becoming null silently.
    - Test 5: Blank `price` and `ts_event` on `side='N'` rows remain tolerated instead of being treated as fatal schema corruption.
  </behavior>
  <action>In `src/vwap_revert/data_pipeline/schema.py`, define `RAW_SCHEMA` for the 17 feed columns from `PROJECT.md`, `REQUIRED_COLUMNS`, `validate_required_columns(columns: list[str]) -> None` that raises `ValueError` listing the missing column names, and `parse_timestamp_column(expr: pl.Expr, column_name: str) -> pl.Expr` that parses UTC nanosecond timestamps and raises on invalid values instead of coercing them to null. In `src/vwap_revert/data_pipeline/ingest.py`, implement `scan_raw_csv(path: Path) -> pl.LazyFrame`, `scan_candidate_outrights(path: Path) -> pl.LazyFrame`, and `collect_sample_day(path: Path) -> pl.DataFrame`. Fail fast per D-10 by checking `path.exists()` and raising `FileNotFoundError` for unreadable/missing files, enforcing required columns before further transforms, and forcing `ts_recv` parse validation through `parse_timestamp_column()` before collection. Filter to symbols that start with `NQ` and do not contain `-`, keep both `ts_recv` and `ts_event`, and do not reject blank `price`/`ts_event` for `side='N'`. Create `tests/data_pipeline/test_ingest.py` with direct fixtures and assertions for the five behaviors above so DATA-01 and the D-10 parts of DATA-07 are green at the end of the plan.</action>
  <acceptance_criteria>
    `src/vwap_revert/data_pipeline/schema.py` contains `RAW_SCHEMA = {`, `def validate_required_columns`, and `def parse_timestamp_column`
    `src/vwap_revert/data_pipeline/ingest.py` contains `def scan_raw_csv`, `def scan_candidate_outrights`, `.str.starts_with("NQ")`, and `FileNotFoundError`
    `tests/data_pipeline/test_ingest.py` contains `test_reads_sample_day`, `test_missing_required_columns_raise`, `test_missing_csv_path_raises_file_not_found`, `test_invalid_ts_recv_raises_parse_failure`, and `test_side_n_rows_allow_blank_trade_fields`
    `python -m pytest tests/data_pipeline/test_ingest.py -x` exits successfully
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/data_pipeline/test_ingest.py -x</automated>
  </verify>
  <done>The codebase can read raw Databento CSV samples through a stable typed interface, exclude spread rows early, fail fast on missing columns, unreadable files, and timestamp parse failures, and still tolerate quote-only row blanks.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/data_pipeline/test_ingest.py -x` and confirm ingestion passes while fail-fast tests for missing columns, missing files, and invalid timestamps all trip before any later phase logic is needed.
</verification>

<success_criteria>
Plan 01 is complete when the repository has a Python package and test harness, raw CSVs are discoverable through a deterministic manifest, and ingestion primitives satisfy DATA-01 plus the D-10 fail-fast portions of DATA-07 on sample fixtures.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-pipeline-foundation/01-01-SUMMARY.md`
</output>
