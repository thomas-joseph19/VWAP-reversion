---
wave: 1
depends_on: []
files_modified:
  - src/vwap_revert/indicators/setup_detection.py
autonomous: true
status: complete
requirements_addressed:
  - SGNL-04
  - SGNL-05
---

# Plan 09-01: Indicator Upgrades (Rejection & Confluence)

Upgrade the setup detector to include a causal rejection gate and a multi-factor confluence score.

## Tasks

<task>
<objective>Add rejection detection primitives to row-level enrichment.</objective>
<read_first>
- src/vwap_revert/indicators/setup_detection.py
</read_first>
<acceptance_criteria>
- New columns `seconds_since_extreme` and `rejection_displacement` are present in enriched DataFrame.
- `seconds_since_extreme` correctly resets when a new session high/low is formed.
- `rejection_displacement` correctly measures distance from the session extreme in ticks.
</acceptance_criteria>
<action>
In `attach_setup_detection_features`:
1. Calculate `session_high` and `session_low` using `cum_max()` and `cum_min()`.
2. Compute `seconds_since_extreme` by grouping rows by the count of changes in session extreme.
3. Compute `rejection_displacement` as `session_high - bid_px_00` (for shorts) and `ask_px_00 - session_low` (for longs).
</action>
</task>

<task>
<objective>Implement the mandatory rejection gate for setup emission.</objective>
<read_first>
- src/vwap_revert/indicators/setup_detection.py
</read_first>
<acceptance_criteria>
- `setup_event_emitted` only becomes true when a `setup_candidate_passes` row also satisfies rejection conditions (N=60s, M=8 ticks).
- Setup candidates that timeout (300s) without rejection are discarded.
</acceptance_criteria>
<action>
1. Define a `rejection_confirmed` boolean: `(seconds_since_extreme >= 60) & (rejection_displacement >= 2.0)`.
2. Track "setup active" state: A boolean that turns on when `setup_candidate_passes` first hits and stays on until rejection hits or 300s pass.
3. Update `setup_event_emitted` to trigger on the first row where `setup_active` and `rejection_confirmed` are both True.
</action>
</task>

<task>
<objective>Implement Phase 09 Confluence Scoring.</objective>
<read_first>
- src/vwap_revert/indicators/setup_detection.py
- src/vwap_revert/indicators/regime.py
</read_first>
<acceptance_criteria>
- `confluence_score` column (0-100) added to enriched artifact and setup log.
- Score correctly weights Sigma (40), Structure (40), and HTF Alignment (20).
</acceptance_criteria>
<action>
In `attach_setup_detection_features`:
1. **Sigma Score (0-40):** Scale `setup_sigma_signed.abs()` between 1.7 (0) and 3.0 (40).
2. **Structure Score (0-40):** Count unique level types within threshold (Daily VA, Overnight VA, HTF Balance). Each type provides 20 pts (cap at 40).
3. **Alignment Score (0-20):** Check if price is extended from `weekly_vwap` and `monthly_vwap` in the same direction as the daily setup. 10 pts per alignment.
4. Sum components into `confluence_score`.
</action>
</task>

## Verification
- Run `attach_setup_detection_features` on sample data and verify `confluence_score` and rejection-based event timing.
- Export validation CSV to confirm events fire later than previous phases (due to rejection lag).
