---
phase: 03-simple-structural-levels
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/vwap_revert/indicators/__init__.py
  - src/vwap_revert/indicators/structural_levels.py
  - tests/indicators/test_structural_levels.py
autonomous: true
requirements:
  - STRC-01
  - STRC-07
must_haves:
  truths:
    - "Each trading_date row can access only the immediately prior completed RTH session's VAH, VAL, and POC."
    - "If the immediately prior trading_date is missing or unusable, current-session rows carry null structural levels plus explicit quality metadata."
    - "Rows expose numeric distances, nearest-level metadata, and inclusive <= 10.0-point proximity flags against prior-day levels."
  artifacts:
    - path: "src/vwap_revert/indicators/structural_levels.py"
      provides: "Deterministic prior-day profile computation and row-aligned structural enrichment"
      exports:
        - "compute_prior_day_structural_levels"
        - "attach_prior_day_structural_levels"
    - path: "tests/indicators/test_structural_levels.py"
      provides: "Deterministic unit coverage for value-area math, missing-session handling, and proximity enrichment"
      contains:
        - "test_prior_day_levels_use_rth_trade_rows_only"
        - "test_missing_immediately_prior_session_emits_null_levels"
        - "test_proximity_features_use_inclusive_threshold"
  key_links:
    - from: "src/vwap_revert/data_pipeline/cache.py"
      to: "src/vwap_revert/indicators/structural_levels.py"
      via: "Phase 1 canonical schema and trading_date/is_rth semantics"
      pattern: "trading_date|is_rth|side|price|size"
    - from: "src/vwap_revert/indicators/structural_levels.py"
      to: "tests/indicators/test_structural_levels.py"
      via: "Exact helper and output-column expectations"
      pattern: "prior_rth_vah|prior_rth_val|prior_rth_poc|nearest_structural_level"
---

<objective>
Create the Phase 03 indicator core that computes deterministic prior-day RTH value-area levels and enriches each row with reusable structural proximity features.

Purpose: satisfy `STRC-01` and the row-level feature half of `STRC-07` without leaking same-session information.
Output: a tested `structural_levels.py` module exported from `vwap_revert.indicators`.
</objective>

<execution_context>
@C:/Users/pranav/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pranav/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-simple-structural-levels/03-CONTEXT.md
@.planning/phases/03-simple-structural-levels/03-RESEARCH.md
@.planning/phases/02-daily-vwap-engine/02-01-SUMMARY.md
@src/vwap_revert/data_pipeline/cache.py
@src/vwap_revert/indicators/__init__.py
@src/vwap_revert/indicators/vwap.py
@tests/indicators/test_vwap.py

<interfaces>
From `src/vwap_revert/data_pipeline/cache.py`:

```python
def scan_canonical_cache(output_dir: Path) -> pl.LazyFrame: ...
```

From `src/vwap_revert/indicators/vwap.py`:

```python
def _require_columns(df: pl.DataFrame, required: set[str]) -> None: ...
def attach_daily_vwap_bands(df: pl.DataFrame) -> pl.DataFrame: ...
```

Current indicator package export surface:

```python
from . import vwap

__all__ = ["vwap"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing unit coverage for deterministic prior-day value-area math and row-level structural features</name>
  <files>tests/indicators/test_structural_levels.py</files>
  <read_first>
    - tests/indicators/test_vwap.py
    - src/vwap_revert/indicators/vwap.py
    - .planning/phases/03-simple-structural-levels/03-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: a prior session built from RTH trade rows only produces exact `prior_rth_vah`, `prior_rth_val`, and `prior_rth_poc` values, ignoring quote-only rows, per D-01, D-02, D-04, D-05, and D-06.
    - Test 2: when the immediately previous `trading_date` is missing or has zero trade volume, the next session receives null prior-day levels with `levels_available == false` and `quality_status` equal to `missing_prior_session` or `unusable_prior_session`, per D-03 and D-11.
    - Test 3: proximity uses `proximity_threshold_points=10.0` with inclusive `<=` comparisons and emits exact columns `distance_to_prior_rth_vah`, `distance_to_prior_rth_val`, `distance_to_prior_rth_poc`, `nearest_structural_level`, `nearest_structural_distance`, and `structural_levels_within_threshold`, per D-08 and D-09.
  </behavior>
  <action>Create `tests/indicators/test_structural_levels.py` with fixture rows spanning at least three `trading_date` values. Use exact expected column names `source_trading_date`, `prior_rth_vah`, `prior_rth_val`, `prior_rth_poc`, `levels_available`, `quality_status`, `profile_trade_rows`, `profile_total_volume`, `structural_reference_price`, `distance_to_prior_rth_vah`, `distance_to_prior_rth_val`, `distance_to_prior_rth_poc`, `near_prior_rth_vah`, `near_prior_rth_val`, `near_prior_rth_poc`, `nearest_structural_level`, `nearest_structural_distance`, and `structural_levels_within_threshold`. Import the yet-to-be-created functions `compute_prior_day_structural_levels` and `attach_prior_day_structural_levels` from `vwap_revert.indicators.structural_levels`. Include one fixture where POC tie candidates have equal volume so the expected tie break is "closest to session VWAP center, then lower price", using exact candidate prices that make the expected POC unambiguous.</action>
  <acceptance_criteria>
    - `tests/indicators/test_structural_levels.py` contains `from vwap_revert.indicators.structural_levels import attach_prior_day_structural_levels, compute_prior_day_structural_levels`
    - `tests/indicators/test_structural_levels.py` contains the strings `missing_prior_session`, `unusable_prior_session`, and `structural_levels_within_threshold`
    - `python -m pytest tests/indicators/test_structural_levels.py -q` fails before implementation because `vwap_revert.indicators.structural_levels` or the target functions do not yet exist
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_structural_levels.py -q</automated>
  </verify>
  <done>Failing unit tests define the exact Phase 03 module API, output columns, tie-break behavior, missing-session behavior, and proximity semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the structural-level indicator module and export it from the package</name>
  <files>src/vwap_revert/indicators/structural_levels.py, src/vwap_revert/indicators/__init__.py</files>
  <read_first>
    - src/vwap_revert/indicators/__init__.py
    - src/vwap_revert/indicators/vwap.py
    - src/vwap_revert/data_pipeline/cache.py
    - tests/indicators/test_structural_levels.py
    - .planning/phases/03-simple-structural-levels/03-CONTEXT.md
  </read_first>
  <action>Create `src/vwap_revert/indicators/structural_levels.py` with these public functions: `compute_prior_day_structural_levels(df: pl.DataFrame) -> pl.DataFrame` and `attach_prior_day_structural_levels(df: pl.DataFrame, proximity_threshold_points: float = 10.0) -> pl.DataFrame`. Require the Phase 1 columns `trading_date`, `ts_recv`, `is_rth`, `side`, `price`, and `size`, and also support `bid_px_00` and `ask_px_00` when present. Filter the profile source to `is_rth == True` and `side in {"A", "B"}` only, per D-01 and D-02. Aggregate traded volume on native `price` levels per `trading_date`, compute session VWAP as the fair-value center for D-05 tie breaks, pick `prior_rth_poc` by max traded volume then smallest `(abs(price - session_vwap), price)`, and expand value area to at least 70% of session volume by stepping outward from POC to the adjacent price level with greater incremental volume, breaking equal-volume ties by the lower price. Emit a session-level frame keyed by current `trading_date` with columns `source_trading_date`, `prior_rth_vah`, `prior_rth_val`, `prior_rth_poc`, `levels_available`, `quality_status`, `profile_trade_rows`, and `profile_total_volume`; map each current day to only the immediately previous `trading_date`, and if that exact prior day is missing or has zero trade rows, leave the three level columns null and set `quality_status` to `missing_prior_session` or `unusable_prior_session` per D-03 and D-11. In `attach_prior_day_structural_levels`, left-join those session levels back onto the full row stream with a stable row index like Phase 2, compute `structural_reference_price` as `price` on trade rows, otherwise `(bid_px_00 + ask_px_00) / 2.0` when both quotes exist, otherwise `bid_px_00`, otherwise `ask_px_00`. Then compute signed distances `structural_reference_price - level`, booleans `near_prior_rth_vah`, `near_prior_rth_val`, `near_prior_rth_poc` using `abs(distance) <= proximity_threshold_points`, nearest-level label values exactly `prior_rth_vah`, `prior_rth_val`, or `prior_rth_poc`, nearest distance as the minimum absolute distance, and `structural_levels_within_threshold` as the integer count of true near flags. Update `src/vwap_revert/indicators/__init__.py` so `__all__` contains both `vwap` and `structural_levels`.</action>
  <acceptance_criteria>
    - `src/vwap_revert/indicators/structural_levels.py` contains `def compute_prior_day_structural_levels`
    - `src/vwap_revert/indicators/structural_levels.py` contains `def attach_prior_day_structural_levels`
    - `src/vwap_revert/indicators/structural_levels.py` contains the exact strings `prior_rth_vah`, `prior_rth_val`, `prior_rth_poc`, `missing_prior_session`, and `unusable_prior_session`
    - `src/vwap_revert/indicators/__init__.py` contains `from . import structural_levels, vwap`
    - `python -m pytest tests/indicators/test_structural_levels.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_structural_levels.py -q</automated>
  </verify>
  <done>The repo exposes a deterministic Phase 03 indicator module with passing unit coverage for prior-day RTH value-area math and row-aligned proximity features.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/indicators/test_structural_levels.py -q` after the module exists. Confirm the produced API and column names match the task text exactly because Plan 02 will build artifacts and CLI wiring against those names.
</verification>

<success_criteria>
Phase 03 has a reusable structural indicator API that computes deterministic prior-day `VAH`/`VAL`/`POC`, preserves strict causality, and emits row-level proximity columns that later phases can filter directly.
</success_criteria>

<output>
After completion, create `.planning/phases/03-simple-structural-levels/03-01-SUMMARY.md`
</output>
