---
phase: 03-simple-structural-levels
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/vwap_revert/cli.py
  - src/vwap_revert/indicators/structural_levels.py
  - tests/indicators/test_structural_levels_validation.py
autonomous: true
requirements:
  - STRC-01
  - STRC-07
must_haves:
  truths:
    - "A single CLI command rebuilds both the per-session prior-day structural artifact and the row-aligned enriched artifact from the canonical cache."
    - "Validation exports isolate explicit trading dates for manual chart checks against prior-day RTH VAH, VAL, and POC."
    - "The artifact schema preserves numeric structural columns for direct use in later setup-detection and analytics phases."
  artifacts:
    - path: "src/vwap_revert/cli.py"
      provides: "Phase 03 build subcommand"
      contains:
        - "build-phase3-structural-levels"
    - path: "src/vwap_revert/indicators/structural_levels.py"
      provides: "Artifact writers for session-level, enriched, and validation outputs"
      exports:
        - "write_structural_levels_artifacts"
        - "write_structural_validation_export"
    - path: "tests/indicators/test_structural_levels_validation.py"
      provides: "CLI and artifact-emission integration coverage"
      contains:
        - "test_structural_validation_export_contains_expected_columns"
        - "test_cli_writes_phase3_structural_artifacts"
  key_links:
    - from: "src/vwap_revert/cli.py"
      to: "src/vwap_revert/indicators/structural_levels.py"
      via: "build_phase3_structural_levels entrypoint"
      pattern: "build_phase3_structural_levels|write_structural_validation_export"
    - from: "src/vwap_revert/indicators/structural_levels.py"
      to: "tests/indicators/test_structural_levels_validation.py"
      via: "artifact filenames and validation schema"
      pattern: "phase3_structural_levels.parquet|phase3_structural_enriched.parquet|structural_validation_export.csv"
---

<objective>
Wire the tested Phase 03 structural indicator into the repo's artifact and CLI pattern so structural levels can be rebuilt, audited, and consumed by later phases without ad hoc scripts.

Purpose: finish the persistence and command-surface work needed for downstream execution and manual validation.
Output: artifact writers, validation export logic, and a `build-phase3-structural-levels` CLI subcommand.
</objective>

<execution_context>
@C:/Users/pranav/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pranav/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-simple-structural-levels/03-CONTEXT.md
@.planning/phases/03-simple-structural-levels/03-RESEARCH.md
@.planning/phases/02-daily-vwap-engine/02-02-SUMMARY.md
@src/vwap_revert/cli.py
@src/vwap_revert/data_pipeline/cache.py
@src/vwap_revert/indicators/structural_levels.py
@src/vwap_revert/indicators/vwap.py
@tests/indicators/test_vwap_validation.py

<interfaces>
From `src/vwap_revert/cli.py`:

```python
def build_phase2_vwap(cache_root: Path, output_root: Path, validation_dates: list[str]) -> Path: ...
def build_parser() -> argparse.ArgumentParser: ...
```

From `src/vwap_revert/indicators/vwap.py`:

```python
def write_enriched_vwap_artifact(enriched_df: pl.DataFrame, output_dir: Path) -> Path: ...
def write_vwap_validation_export(enriched_df: pl.DataFrame, output_dir: Path, validation_dates: list[str]) -> None: ...
```

Plan 01 contract:

```python
def compute_prior_day_structural_levels(df: pl.DataFrame) -> pl.DataFrame: ...
def attach_prior_day_structural_levels(df: pl.DataFrame, proximity_threshold_points: float = 10.0) -> pl.DataFrame: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add artifact writers and integration coverage for Phase 03 structural outputs</name>
  <files>src/vwap_revert/indicators/structural_levels.py, tests/indicators/test_structural_levels_validation.py</files>
  <read_first>
    - src/vwap_revert/indicators/structural_levels.py
    - src/vwap_revert/indicators/vwap.py
    - tests/indicators/test_vwap_validation.py
    - .planning/phases/03-simple-structural-levels/03-CONTEXT.md
  </read_first>
  <action>Extend `src/vwap_revert/indicators/structural_levels.py` with artifact constants `phase3_structural_levels.parquet`, `phase3_structural_enriched.parquet`, `structural_validation_export.csv`, and `validation_sessions.json`. Add `write_structural_levels_artifacts(df: pl.DataFrame, output_dir: Path, proximity_threshold_points: float = 10.0) -> tuple[Path, Path]` that computes the session-level frame via `compute_prior_day_structural_levels`, the row-aligned frame via `attach_prior_day_structural_levels`, writes both parquet files into `output_dir`, and returns both paths. Add `write_structural_validation_export(enriched_df: pl.DataFrame, output_dir: Path, validation_dates: list[str]) -> None` that writes `validation/structural_validation_export.csv` containing exactly `trading_date`, `ts_recv_et`, `structural_reference_price`, `source_trading_date`, `prior_rth_vah`, `prior_rth_val`, `prior_rth_poc`, `distance_to_prior_rth_vah`, `distance_to_prior_rth_val`, `distance_to_prior_rth_poc`, `nearest_structural_level`, `nearest_structural_distance`, `structural_levels_within_threshold`, `levels_available`, and `quality_status`, plus `validation/validation_sessions.json` with keys `validation_dates`, `session_artifact`, `enriched_artifact`, `comparison_export`, and `row_count`. Create `tests/indicators/test_structural_levels_validation.py` mirroring the Phase 2 validation-test shape, including one direct artifact-writer test and one CLI-facing fixture-ready test that expects those exact filenames and manifest keys.</action>
  <acceptance_criteria>
    - `src/vwap_revert/indicators/structural_levels.py` contains `phase3_structural_levels.parquet`, `phase3_structural_enriched.parquet`, and `structural_validation_export.csv`
    - `tests/indicators/test_structural_levels_validation.py` contains `validation_sessions.json`
    - `tests/indicators/test_structural_levels_validation.py` contains the exact column string `nearest_structural_level`
    - `python -m pytest tests/indicators/test_structural_levels_validation.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_structural_levels_validation.py -q</automated>
  </verify>
  <done>Phase 03 has deterministic artifact writers and automated coverage for output filenames, validation schema, and manifest metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Phase 03 CLI subcommand that rebuilds structural artifacts from the canonical cache</name>
  <files>src/vwap_revert/cli.py, tests/indicators/test_structural_levels_validation.py</files>
  <read_first>
    - src/vwap_revert/cli.py
    - src/vwap_revert/data_pipeline/cache.py
    - src/vwap_revert/indicators/structural_levels.py
    - tests/indicators/test_structural_levels_validation.py
    - .planning/phases/02-daily-vwap-engine/02-02-SUMMARY.md
  </read_first>
  <action>Modify `src/vwap_revert/cli.py` to import `write_structural_levels_artifacts` and `write_structural_validation_export`. Add `_parse_phase3_validation_dates(validation_dates: list[str]) -> list[str]` that de-duplicates and ISO-validates values and requires at least one `--validation-date`. Add `build_phase3_structural_levels(cache_root: Path, output_root: Path, validation_dates: list[str], proximity_threshold_points: float = 10.0) -> Path` that loads `scan_canonical_cache(cache_root).collect()`, writes both parquet artifacts, reloads or reuses the enriched frame, writes validation exports, and returns `output_root / "phase3_structural_enriched.parquet"`. Extend `build_parser()` with a `build-phase3-structural-levels` subcommand requiring `--cache-root`, `--output-root`, one or more `--validation-date`, and optional `--proximity-threshold-points` defaulting to `10.0`. Update `main()` to dispatch that command. Extend the CLI integration test so `main([...])` with `build-phase3-structural-levels` exits `0` and creates `phase3_structural_levels.parquet`, `phase3_structural_enriched.parquet`, `validation/structural_validation_export.csv`, and `validation/validation_sessions.json`.</action>
  <acceptance_criteria>
    - `src/vwap_revert/cli.py` contains `build-phase3-structural-levels`
    - `src/vwap_revert/cli.py` contains `--proximity-threshold-points`
    - `src/vwap_revert/cli.py` contains `def build_phase3_structural_levels`
    - `python -m pytest tests/indicators/test_structural_levels_validation.py -q` passes
    - `python -m pytest tests/indicators -q` passes
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_structural_levels_validation.py -q</automated>
  </verify>
  <done>The repo exposes a Phase 03 build command that deterministically rebuilds session-level structural outputs, enriched row-level outputs, and manual-validation artifacts from the canonical cache.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/indicators/test_structural_levels_validation.py -q` after artifact wiring, then `python -m pytest tests/indicators -q` as the phase integration check. The CLI path must emit the four named Phase 03 files without any manual post-processing.
</verification>

<success_criteria>
Phase 03 can be rebuilt from one CLI command, produces both structural parquet artifacts plus validation exports, and leaves downstream phases with explicit numeric columns instead of ad hoc reports.
</success_criteria>

<output>
After completion, create `.planning/phases/03-simple-structural-levels/03-02-SUMMARY.md`
</output>
