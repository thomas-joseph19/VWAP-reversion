---
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/vwap_revert/analytics.py
  - src/vwap_revert/cli.py
autonomous: true
requirements_addressed:
  - ANLY-07
  - ANLY-08
---

# Plan 10-02: Equity Curve & Final Backtest Report

Implement cumulative equity tracking and a unified backtest summary artifact.

## Tasks

<task>
<objective>Implement Equity Curve calculation and persistence.</objective>
<read_first>
- src/vwap_revert/analytics.py
</read_first>
<acceptance_criteria>
- `phase10_equity_curve.parquet` emitted.
- Contains `entry_ts` and `cumulative_pnl_dollars`.
</acceptance_criteria>
<action>
In `write_core_analytics_artifacts`:
1. Calculate `cumulative_pnl_dollars` using `cum_sum()` on `net_dollars`.
2. Write the resulting DataFrame to Parquet.
</action>
</task>

<task>
<objective>Update CLI to produce full Phase 10 analytics suite.</objective>
<read_first>
- src/vwap_revert/cli.py
- src/vwap_revert/analytics.py
</read_first>
<acceptance_criteria>
- `build-phase9-integration` (or new phase 10 command) produces the nested `phase10_analytics_report.json`.
- Contains all breakdowns and overall metrics.
</acceptance_criteria>
<action>
1. Update `write_core_analytics_artifacts` to return paths for ALL Phase 10 artifacts.
2. Update the CLI main loop to correctly surface these findings.
</action>
</task>

## Verification
- Confirm `phase10_equity_curve.parquet` has monotonically increasing row counts as trades are added.
- Verify JSON report has no Null values for PF and Sharpe where trade counts are sufficient.
