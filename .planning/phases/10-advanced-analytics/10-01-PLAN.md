---
wave: 1
depends_on: []
files_modified:
  - src/vwap_revert/analytics.py
autonomous: true
requirements_addressed:
  - ANLY-04
  - ANLY-05
  - ANLY-06
---

# Plan 10-01: Advanced Performance Breakdowns

Implement multi-dimensional breakdown of backtest performance by regime, sigma band, and time-of-day.

## Tasks

<task>
<objective>Add dimension-bucketing helpers to trade_log preparation.</objective>
<read_first>
- src/vwap_revert/analytics.py
</read_first>
<acceptance_criteria>
- `prepare_trade_log` outputs new columns: `sigma_band`, `time_bucket_15m`.
- `sigma_band` categories: "1.7-2.2", "2.2-3.0", "3.0+".
- `time_bucket_15m` correctly truncates `entry_ts`.
</acceptance_criteria>
<action>
Modify `prepare_trade_log` in `analytics.py`:
1. Use `pl.when().then()` to bucket `sigma_at_entry`.
2. Use `pl.col("entry_ts").dt.truncate("15m").dt.time()` for time bucketing.
</action>
</task>

<task>
<objective>Implement a vectorized breakdown summary generator.</objective>
<read_first>
- src/vwap_revert/analytics.py
</read_first>
<acceptance_criteria>
- `compute_performance_breakdowns` function returns a dictionary keyed by dimension.
- Each dimension containing its own sub-performance metrics (win rate, PF, net$).
</acceptance_criteria>
<action>
1. Define `compute_performance_breakdowns(trade_log, dimension_col)`.
2. Use `group_by(dimension_col)` and aggregate relevant metrics.
3. Call this for `regime_label`, `sigma_band`, and `time_bucket_15m`.
</action>
</task>

## Verification
- Run analytics on a sample trade log and verify the nested JSON report contains the expected dimensions.
