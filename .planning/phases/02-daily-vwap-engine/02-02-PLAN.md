---
phase: 02-daily-vwap-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/vwap_revert/cli.py
  - src/vwap_revert/indicators/vwap.py
  - tests/indicators/test_vwap_validation.py
autonomous: true
requirements:
  - VWAP-01
  - VWAP-02
  - VWAP-06
must_haves:
  truths:
    - "Phase 2 produces a reusable indicator artifact aligned to the canonical dataset rather than an ad hoc in-memory-only result."
    - "Manual chart validation uses a deterministic export covering at least five sessions and the exact VWAP plus sigma-band columns under review."
    - "The command surface for building Phase 2 artifacts follows the existing project CLI pattern."
  artifacts:
    - path: "src/vwap_revert/cli.py"
      provides: "Phase 2 CLI command(s) for building daily VWAP outputs and validation exports"
      exports: ["main"]
    - path: "src/vwap_revert/indicators/vwap.py"
      provides: "Artifact-writing helpers for enriched indicator data and validation-session exports"
      exports: ["attach_daily_vwap_bands", "write_vwap_validation_export"]
    - path: "tests/indicators/test_vwap_validation.py"
      provides: "Automated coverage for validation-export schema and CLI artifact emission"
  key_links:
    - from: "src/vwap_revert/cli.py"
      to: "src/vwap_revert/indicators/vwap.py"
      via: "CLI orchestrates canonical-cache load, indicator enrichment, and validation export writing"
      pattern: "attach_daily_vwap_bands|write_vwap_validation_export"
    - from: ".planning/phases/02-daily-vwap-engine/02-CONTEXT.md"
      to: "tests/indicators/test_vwap_validation.py"
      via: "locked decision D-12 requires five-session validation coverage and 0.25-point manual comparison tolerance"
      pattern: "five manually chosen sessions|0.25 NQ points"
---

<objective>
Turn the Phase 2 indicator engine into a reproducible build surface by adding artifact writers, a CLI workflow, and a validation export tailored for TradingView or NinjaTrader comparison.

Purpose: Satisfy the output and chart-validation acceptance path after the core math is already locked.
Output: A Phase 2 CLI command plus integration tests that write an enriched indicator dataset and a comparison-friendly validation export for at least five sessions.
</objective>

<execution_context>
@C:/Users/pranav/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pranav/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-daily-vwap-engine/02-CONTEXT.md
@.planning/phases/02-daily-vwap-engine/02-RESEARCH.md
@.planning/phases/02-daily-vwap-engine/02-VALIDATION.md
@.planning/phases/02-daily-vwap-engine/02-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write reusable Phase 2 artifacts for enriched indicators and manual comparison sessions per D-10 through D-12</name>
  <read_first>
    .planning/phases/02-daily-vwap-engine/02-CONTEXT.md
    .planning/phases/02-daily-vwap-engine/02-RESEARCH.md
    src/vwap_revert/indicators/vwap.py
    tests/indicators/test_vwap_validation.py
  </read_first>
  <files>src/vwap_revert/indicators/vwap.py, tests/indicators/test_vwap_validation.py</files>
  <action>Extend `src/vwap_revert/indicators/vwap.py` with `write_vwap_validation_export(enriched_df: pl.DataFrame, output_dir: Path, validation_dates: list[str]) -> None`. Write one reusable parquet or CSV artifact containing the enriched row-aligned Phase 2 dataset and a second comparison-friendly export for only the requested validation dates with columns `trading_date`, `ts_recv_et`, `price`, `size`, `daily_vwap`, `daily_sigma`, `daily_vwap_upper_1s`, `daily_vwap_lower_1s`, `daily_vwap_upper_2s`, `daily_vwap_lower_2s`, `daily_vwap_upper_3s`, `daily_vwap_lower_3s`, `daily_vwap_upper_4s`, and `daily_vwap_lower_4s`. Also write a small manifest file recording the selected validation dates so manual chart review is reproducible. In `tests/indicators/test_vwap_validation.py`, add `test_validation_export_contains_comparison_columns` proving the export exists and contains the exact comparison schema plus the selected session manifest.</action>
  <acceptance_criteria>
    `src/vwap_revert/indicators/vwap.py` contains `def write_vwap_validation_export`, `validation_dates`, and the exact strings `daily_vwap_upper_4s` and `daily_vwap_lower_4s`
    `tests/indicators/test_vwap_validation.py` contains `test_validation_export_contains_comparison_columns`
    `python -m pytest tests/indicators/test_vwap_validation.py::test_validation_export_contains_comparison_columns -x` exits successfully
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_vwap_validation.py::test_validation_export_contains_comparison_columns -x</automated>
  </verify>
  <done>Phase 2 has a deterministic artifact set: one enriched indicator dataset for downstream phases and one comparison export plus session manifest for manual chart validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the Phase 2 CLI command that builds indicator and validation outputs from the canonical cache</name>
  <read_first>
    .planning/phases/02-daily-vwap-engine/02-CONTEXT.md
    src/vwap_revert/cli.py
    src/vwap_revert/data_pipeline/cache.py
    src/vwap_revert/indicators/vwap.py
    tests/indicators/test_vwap_validation.py
  </read_first>
  <files>src/vwap_revert/cli.py, tests/indicators/test_vwap_validation.py</files>
  <action>Update `src/vwap_revert/cli.py` so the argparse command surface includes a new subcommand named `build-phase2-vwap` accepting `--cache-root`, `--output-root`, and one or more `--validation-date YYYY-MM-DD` values. The command must load the Phase 1 canonical cache via `scan_canonical_cache(...).collect()`, call `attach_daily_vwap_bands(...)`, write the reusable enriched artifact, then call `write_vwap_validation_export(...)` with the provided validation dates. Reuse the existing `main()` entrypoint pattern and keep Phase 1 behavior unchanged. In `tests/indicators/test_vwap_validation.py`, add `test_cli_writes_indicator_and_validation_artifacts` that exercises the command against a small fixture cache and asserts the enriched output, comparison export, and validation-date manifest are all written.</action>
  <acceptance_criteria>
    `src/vwap_revert/cli.py` contains `build-phase2-vwap`, `--cache-root`, `--output-root`, and `--validation-date`
    `tests/indicators/test_vwap_validation.py` contains `test_cli_writes_indicator_and_validation_artifacts`
    `python -m pytest tests/indicators/test_vwap_validation.py -x` exits successfully
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_vwap_validation.py -x</automated>
  </verify>
  <done>The repository can build Phase 2 outputs from the canonical cache through one reproducible CLI command, satisfying the workflow needed for manual chart validation and downstream reuse.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/indicators/test_vwap_validation.py -x` and then smoke the new CLI against a fixture cache to confirm both the enriched indicator artifact and the five-session comparison export are emitted from one command surface.
</verification>

<success_criteria>
Plan 02 is complete when Phase 2 exposes a reproducible CLI workflow that writes reusable indicator outputs plus a deterministic chart-validation export, satisfying VWAP-06 without changing the locked core math.
</success_criteria>

<output>
After completion, create `.planning/phases/02-daily-vwap-engine/02-02-SUMMARY.md`
</output>
