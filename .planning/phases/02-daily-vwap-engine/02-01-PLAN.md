---
phase: 02-daily-vwap-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vwap_revert/indicators/__init__.py
  - src/vwap_revert/indicators/vwap.py
  - tests/indicators/test_vwap.py
autonomous: true
requirements:
  - VWAP-01
  - VWAP-02
must_haves:
  truths:
    - "Daily VWAP resets at the 9:30 AM ET RTH open for each trading_date and uses only actual trade rows."
    - "Sigma bands are computed with the volume-weighted expanding variance identity, not any rolling-window helper."
    - "The indicator columns join back onto the canonical session dataset without leaking future observations."
  artifacts:
    - path: "src/vwap_revert/indicators/vwap.py"
      provides: "Daily VWAP and 1σ-4σ band computation over the Phase 1 canonical dataset"
      exports: ["compute_daily_vwap", "attach_daily_vwap_bands"]
    - path: "src/vwap_revert/indicators/__init__.py"
      provides: "Indicator module exports for downstream phases"
      exports: ["vwap"]
    - path: "tests/indicators/test_vwap.py"
      provides: "Automated coverage for trade-only RTH VWAP math, sigma-band correctness, and row-aligned join behavior"
  key_links:
    - from: "src/vwap_revert/data_pipeline/cache.py"
      to: "src/vwap_revert/indicators/vwap.py"
      via: "canonical parquet cache is the Phase 2 input surface"
      pattern: "scan_canonical_cache|compute_daily_vwap"
    - from: "src/vwap_revert/data_pipeline/sessions.py"
      to: "src/vwap_revert/indicators/vwap.py"
      via: "existing ET timestamps and RTH labels drive the 9:30 ET anchor and session gating"
      pattern: "ts_recv_et|is_rth|trading_date"
    - from: "tests/indicators/test_vwap.py"
      to: "src/vwap_revert/indicators/vwap.py"
      via: "fixture-based math assertions against the public indicator API"
      pattern: "compute_daily_vwap|attach_daily_vwap_bands"
---

<objective>
Build the reusable Phase 2 indicator engine that computes daily RTH VWAP plus volume-weighted expanding 1σ-4σ bands and returns them aligned to the canonical session dataset.

Purpose: Lock the math and causal dataflow before any CLI, export, or manual chart-validation work is added.
Output: An `indicators/vwap.py` module with public computation helpers and automated tests for anchor, trade filtering, sigma math, and row alignment.
</objective>

<execution_context>
@C:/Users/pranav/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pranav/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-daily-vwap-engine/02-CONTEXT.md
@.planning/phases/02-daily-vwap-engine/02-RESEARCH.md
@.planning/phases/02-daily-vwap-engine/02-VALIDATION.md

<interfaces>
From `src/vwap_revert/data_pipeline/cache.py`:
```python
def scan_canonical_cache(output_dir: Path) -> pl.LazyFrame:
    ...
```

From `src/vwap_revert/data_pipeline/sessions.py`:
```python
def label_sessions(df: pl.DataFrame) -> pl.DataFrame:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the indicator module and implement trade-only RTH daily VWAP per D-01 through D-06</name>
  <read_first>
    .planning/phases/02-daily-vwap-engine/02-CONTEXT.md
    .planning/phases/02-daily-vwap-engine/02-RESEARCH.md
    src/vwap_revert/data_pipeline/cache.py
    src/vwap_revert/data_pipeline/sessions.py
    src/vwap_revert/cli.py
    src/vwap_revert/indicators/__init__.py
    src/vwap_revert/indicators/vwap.py
    tests/indicators/test_vwap.py
  </read_first>
  <files>src/vwap_revert/indicators/__init__.py, src/vwap_revert/indicators/vwap.py, tests/indicators/test_vwap.py</files>
  <action>Create `src/vwap_revert/indicators/__init__.py` exporting the `vwap` module. In `src/vwap_revert/indicators/vwap.py`, implement `compute_daily_vwap(df: pl.DataFrame) -> pl.DataFrame` that requires Phase 1 session columns, filters to rows where `is_rth` is true and `side` is `A` or `B`, groups by `trading_date`, sorts by `ts_recv`, computes cumulative `price * size` and cumulative `size`, and emits row-level columns `daily_vwap`, `cum_volume`, and `cum_notional` for the trade rows only. Treat the first eligible RTH trade as the first observation for that session; do not synthesize pre-trade values. Write `tests/indicators/test_vwap.py` with fixtures and tests named `test_daily_vwap_uses_rth_trade_rows_only` and `test_daily_vwap_resets_per_trading_date` that prove overnight rows are ignored, quote-only rows are ignored as inputs, and the first RTH trade sets the first session VWAP value.</action>
  <acceptance_criteria>
    `src/vwap_revert/indicators/vwap.py` contains `def compute_daily_vwap`, `is_rth`, `side`, `daily_vwap`, `cum_volume`, and `cum_notional`
    `tests/indicators/test_vwap.py` contains `test_daily_vwap_uses_rth_trade_rows_only` and `test_daily_vwap_resets_per_trading_date`
    `python -m pytest tests/indicators/test_vwap.py::test_daily_vwap_uses_rth_trade_rows_only tests/indicators/test_vwap.py::test_daily_vwap_resets_per_trading_date -x` exits successfully
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_vwap.py::test_daily_vwap_uses_rth_trade_rows_only tests/indicators/test_vwap.py::test_daily_vwap_resets_per_trading_date -x</automated>
  </verify>
  <done>The repository has a public daily VWAP computation helper that respects the locked RTH-only, trade-only anchor policy and resets cleanly by `trading_date`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add volume-weighted expanding sigma bands and join-back behavior per D-07 through D-09</name>
  <read_first>
    .planning/phases/02-daily-vwap-engine/02-CONTEXT.md
    .planning/phases/02-daily-vwap-engine/02-RESEARCH.md
    src/vwap_revert/indicators/vwap.py
    tests/indicators/test_vwap.py
  </read_first>
  <files>src/vwap_revert/indicators/vwap.py, tests/indicators/test_vwap.py</files>
  <action>Extend `src/vwap_revert/indicators/vwap.py` with `attach_daily_vwap_bands(df: pl.DataFrame) -> pl.DataFrame`. Reuse the trade-row working frame from Task 1, compute cumulative `sum(volume * price^2)` as `cum_sq_notional`, derive `daily_sigma` with `sqrt(max(cum_sq_notional / cum_volume - daily_vwap**2, 0.0))`, and emit `daily_vwap_upper_1s` through `daily_vwap_upper_4s` plus matching lower-band columns. Join the trade-row indicator columns back onto the full input frame on `trading_date` and `ts_recv`, then forward-fill `daily_vwap`, `daily_sigma`, and all band columns within each `trading_date` so quote-only rows inherit the latest causal value without becoming inputs themselves. In `tests/indicators/test_vwap.py`, add `test_sigma_bands_use_volume_weighted_expanding_formula` with hand-worked expected values and `test_indicator_columns_join_back_to_session_rows` proving quote-only rows forward-fill and no future trade can change earlier values.</action>
  <acceptance_criteria>
    `src/vwap_revert/indicators/vwap.py` contains `def attach_daily_vwap_bands`, `cum_sq_notional`, `daily_sigma`, `daily_vwap_upper_4s`, `daily_vwap_lower_4s`, and `forward_fill`
    `tests/indicators/test_vwap.py` contains `test_sigma_bands_use_volume_weighted_expanding_formula` and `test_indicator_columns_join_back_to_session_rows`
    `python -m pytest tests/indicators/test_vwap.py -x` exits successfully
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_vwap.py -x</automated>
  </verify>
  <done>The indicator engine now produces a causal, row-aligned dataset containing daily VWAP, expanding sigma, and 1σ-4σ bands ready for downstream setup detection.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/indicators/test_vwap.py -x` and confirm the suite proves trade-only RTH anchoring, correct cumulative sigma math, and quote-row forward-fill without future leakage.
</verification>

<success_criteria>
Plan 01 is complete when the repository exposes a reusable daily VWAP engine that satisfies VWAP-01 and VWAP-02 with automated tests for anchor correctness, sigma-band math, and row-aligned causal output.
</success_criteria>

<output>
After completion, create `.planning/phases/02-daily-vwap-engine/02-01-SUMMARY.md`
</output>

