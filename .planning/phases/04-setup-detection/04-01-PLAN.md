---
phase: 04-setup-detection
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/vwap_revert/indicators/__init__.py
  - src/vwap_revert/indicators/setup_detection.py
  - tests/indicators/test_setup_detection.py
autonomous: true
requirements:
  - VWAP-03
  - SGNL-01
  - SGNL-02
must_haves:
  truths:
    - "Rows inside the configured 09:30:00-11:30:00 ET window expose explicit eligibility flags, while rows outside the window remain present but ineligible."
    - "Rows expose a signed sigma-distance from VWAP using the shared actionable price and only qualify as extended when the inclusive absolute range is between 1.7 and 3.0 sigma."
    - "The detector emits exactly one setup event on the False-to-True transition of the combined predicate per trading_date, with direction derived from sigma sign."
  artifacts:
    - path: "src/vwap_revert/indicators/setup_detection.py"
      provides: "Row-level setup enrichment and transition-based event extraction"
      exports:
        - "attach_setup_detection_features"
        - "extract_setup_events"
    - path: "tests/indicators/test_setup_detection.py"
      provides: "Deterministic unit coverage for time-window gating, sigma thresholds, and episode transitions"
      contains:
        - "test_time_window_passes_only_inside_inclusive_bounds"
        - "test_signed_sigma_distance_uses_inclusive_absolute_thresholds"
        - "test_combined_predicate_emits_one_event_per_contiguous_episode"
  key_links:
    - from: "src/vwap_revert/indicators/vwap.py"
      to: "src/vwap_revert/indicators/setup_detection.py"
      via: "daily_vwap and daily_sigma reused without recomputation"
      pattern: "daily_vwap|daily_sigma"
    - from: "src/vwap_revert/indicators/structural_levels.py"
      to: "src/vwap_revert/indicators/setup_detection.py"
      via: "shared actionable-price and structural proximity columns"
      pattern: "structural_reference_price|structural_levels_within_threshold|nearest_structural_level"
---

<objective>
Create the Phase 04 detector core that composes Phase 2 VWAP state and Phase 3 structural proximity into row-level setup features plus transition-based candidate events.

Purpose: satisfy `VWAP-03`, `SGNL-01`, and `SGNL-02` as a deterministic composition layer without introducing trade-entry logic or regime-aware thresholds.
Output: a tested `setup_detection.py` module exported from `vwap_revert.indicators`.
</objective>

<execution_context>
@C:/Users/pranav/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pranav/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-setup-detection/04-CONTEXT.md
@.planning/phases/04-setup-detection/04-RESEARCH.md
@.planning/phases/02-daily-vwap-engine/02-01-SUMMARY.md
@.planning/phases/03-simple-structural-levels/03-01-SUMMARY.md
@src/vwap_revert/indicators/__init__.py
@src/vwap_revert/indicators/vwap.py
@src/vwap_revert/indicators/structural_levels.py
@tests/indicators/test_vwap.py
@tests/indicators/test_structural_levels.py

<interfaces>
From `src/vwap_revert/indicators/vwap.py`:

```python
def attach_daily_vwap_bands(df: pl.DataFrame) -> pl.DataFrame: ...
```

Relevant Phase 2 columns:

```python
"daily_vwap"
"daily_sigma"
```

From `src/vwap_revert/indicators/structural_levels.py`:

```python
def attach_prior_day_structural_levels(df: pl.DataFrame, proximity_threshold_points: float = 10.0) -> pl.DataFrame: ...
```

Relevant Phase 3 columns:

```python
"structural_reference_price"
"nearest_structural_level"
"nearest_structural_distance"
"structural_levels_within_threshold"
```

Current indicator package export surface:

```python
from . import structural_levels, vwap

__all__ = ["structural_levels", "vwap"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing detector-core tests for time-window gating, sigma thresholds, and transition emission</name>
  <files>tests/indicators/test_setup_detection.py</files>
  <read_first>
    - tests/indicators/test_vwap.py
    - tests/indicators/test_structural_levels.py
    - .planning/phases/04-setup-detection/04-CONTEXT.md
    - .planning/phases/04-setup-detection/04-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: rows at exactly `09:30:00` and `11:30:00` ET pass `time_window_passes`, rows before or after fail, and the predicate reads `ts_recv_et` rather than UTC, per D-01 through D-03.
    - Test 2: signed sigma distance uses `structural_reference_price`, returns null when `daily_vwap` or `daily_sigma` is null or `daily_sigma == 0`, and `vwap_extension_passes` is true only when `abs(setup_sigma_signed)` is inclusively between `1.7` and `3.0`, per D-04 through D-06.
    - Test 3: `setup_event_emitted` triggers only on the first row where `time_window_passes`, `vwap_extension_passes`, and `structural_confluence_passes` are all true, resets within the same day when any constituent flag turns false, and also resets across `trading_date` boundaries, per D-07 through D-09.
  </behavior>
  <action>Create `tests/indicators/test_setup_detection.py` with a fixture frame spanning at least two `trading_date` values and at least one quote-only row. The fixture must include exact columns `trading_date`, `ts_recv`, `ts_recv_et`, `daily_vwap`, `daily_sigma`, `structural_reference_price`, `structural_levels_within_threshold`, `nearest_structural_level`, `nearest_structural_distance`, `bid_px_00`, and `ask_px_00`. Import the yet-to-be-created functions `attach_setup_detection_features` and `extract_setup_events` from `vwap_revert.indicators.setup_detection`. Freeze these exact output column names in assertions: `time_window_passes`, `setup_sigma_signed`, `vwap_extension_passes`, `structural_confluence_passes`, `setup_candidate_passes`, `setup_event_emitted`, and `setup_direction`. Use exact expected direction values `short` for positive sigma and `long` for negative sigma. Include one case at `1.7`, one at `3.0`, one at `1.69`, and one where a new day starts with a qualifying row so the per-day transition reset is explicit.</action>
  <acceptance_criteria>
    - `tests/indicators/test_setup_detection.py` contains `from vwap_revert.indicators.setup_detection import attach_setup_detection_features, extract_setup_events`
    - `tests/indicators/test_setup_detection.py` contains the exact strings `time_window_passes`, `setup_sigma_signed`, `setup_event_emitted`, and `setup_direction`
    - `tests/indicators/test_setup_detection.py` contains the literal direction assertions `== "short"` and `== "long"`
    - `python -m pytest tests/indicators/test_setup_detection.py -q` fails before implementation because the module or functions do not yet exist
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_setup_detection.py -q</automated>
  </verify>
  <done>Failing tests define the exact Phase 04 detector API, predicate columns, inclusive boundary behavior, and event-transition semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the setup-detection indicator module and export it from the package</name>
  <files>src/vwap_revert/indicators/setup_detection.py, src/vwap_revert/indicators/__init__.py</files>
  <read_first>
    - src/vwap_revert/indicators/__init__.py
    - src/vwap_revert/indicators/vwap.py
    - src/vwap_revert/indicators/structural_levels.py
    - tests/indicators/test_setup_detection.py
    - .planning/phases/04-setup-detection/04-CONTEXT.md
  </read_first>
  <action>Create `src/vwap_revert/indicators/setup_detection.py` with two public functions: `attach_setup_detection_features(df: pl.DataFrame, window_start: time = time(9, 30), window_end: time = time(11, 30), min_sigma: float = 1.7, max_sigma: float = 3.0) -> pl.DataFrame` and `extract_setup_events(enriched_df: pl.DataFrame) -> pl.DataFrame`. Reuse existing row-aligned columns rather than recomputing upstream indicators, per D-02: require `trading_date`, `ts_recv`, `ts_recv_et`, `daily_vwap`, `daily_sigma`, `structural_reference_price`, `structural_levels_within_threshold`, `nearest_structural_level`, and `nearest_structural_distance`, with optional `bid_px_00` and `ask_px_00`. In `attach_setup_detection_features`, compute `time_window_passes` from `ts_recv_et.dt.time().is_between(window_start, window_end, closed="both")`; compute `setup_sigma_signed` as `(structural_reference_price - daily_vwap) / daily_sigma` only when `daily_vwap` is not null, `daily_sigma` is not null, and `daily_sigma != 0`; otherwise keep it null. Compute `vwap_extension_passes` as false when `setup_sigma_signed` is null, else true when `abs(setup_sigma_signed)` is inclusively between `min_sigma` and `max_sigma`, per D-04 through D-06. Compute `structural_confluence_passes` as `structural_levels_within_threshold >= 1`, per D-07. Compute `setup_candidate_passes` as the AND of the three gating booleans. Compute `setup_direction` as `short` when `setup_sigma_signed > 0`, `long` when `setup_sigma_signed < 0`, else null, per D-09. Compute `setup_event_emitted` as `setup_candidate_passes & ~setup_candidate_passes.shift(1).fill_null(False).over("trading_date")`, per D-08. Implement `extract_setup_events` as a projection over rows where `setup_event_emitted` is true, preserving exact columns `trading_date`, `ts_recv`, `ts_recv_et`, `structural_reference_price`, `daily_vwap`, `daily_sigma`, `setup_sigma_signed`, `setup_direction`, `nearest_structural_level`, `nearest_structural_distance`, `structural_levels_within_threshold`, `bid_px_00`, and `ask_px_00` when present. Update `src/vwap_revert/indicators/__init__.py` so `__all__` contains `setup_detection`, `structural_levels`, and `vwap`.</action>
  <acceptance_criteria>
    - `src/vwap_revert/indicators/setup_detection.py` contains `def attach_setup_detection_features`
    - `src/vwap_revert/indicators/setup_detection.py` contains `def extract_setup_events`
    - `src/vwap_revert/indicators/setup_detection.py` contains the exact strings `time_window_passes`, `setup_sigma_signed`, `setup_candidate_passes`, and `setup_event_emitted`
    - `src/vwap_revert/indicators/__init__.py` contains `from . import setup_detection, structural_levels, vwap`
    - `python -m pytest tests/indicators/test_setup_detection.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_setup_detection.py -q</automated>
  </verify>
  <done>The repo exposes a deterministic Phase 04 detector API with passing unit coverage for ET gating, VWAP-extension math, and one-event-per-episode emission.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/indicators/test_setup_detection.py -q` after the module exists. Confirm the row-level feature columns and the event-log projection names are exact, because Plan 02 will persist artifacts and CLI output against this contract.
</verification>

<success_criteria>
Phase 04 has a reusable detector core that keeps all rows for auditing, makes ineligibility explicit, and emits one causal setup event for each qualifying excursion episode.
</success_criteria>

<output>
After completion, create `.planning/phases/04-setup-detection/04-01-SUMMARY.md`
</output>
