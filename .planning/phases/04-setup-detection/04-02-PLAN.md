---
phase: 04-setup-detection
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/vwap_revert/cli.py
  - src/vwap_revert/indicators/setup_detection.py
  - tests/indicators/test_setup_detection_validation.py
autonomous: true
requirements:
  - SGNL-03
must_haves:
  truths:
    - "A single CLI command rebuilds the row-aligned setup-enriched parquet and the compact one-row-per-setup log from the canonical cache."
    - "Each emitted setup row carries the full Phase 04 handoff context: timestamp, trading date, actionable price, VWAP inputs, sigma distance, structural metadata, bid/ask, and null regime placeholders."
    - "Validation exports isolate selected sessions in a deterministic preview format so later manual review can inspect setup timestamps and context without reshaping parquet files."
  artifacts:
    - path: "src/vwap_revert/cli.py"
      provides: "Phase 04 build subcommand"
      contains:
        - "build-phase4-setup-detection"
    - path: "src/vwap_revert/indicators/setup_detection.py"
      provides: "Artifact writers for enriched setup output, compact setup log, and validation preview"
      exports:
        - "write_setup_detection_artifacts"
        - "write_setup_validation_export"
    - path: "tests/indicators/test_setup_detection_validation.py"
      provides: "CLI and artifact-emission coverage for Phase 04 outputs"
      contains:
        - "test_setup_validation_export_contains_expected_columns"
        - "test_cli_writes_phase4_setup_artifacts"
  key_links:
    - from: "src/vwap_revert/cli.py"
      to: "src/vwap_revert/indicators/setup_detection.py"
      via: "build_phase4_setup_detection entrypoint"
      pattern: "build_phase4_setup_detection|write_setup_detection_artifacts|write_setup_validation_export"
    - from: "src/vwap_revert/indicators/setup_detection.py"
      to: "tests/indicators/test_setup_detection_validation.py"
      via: "artifact filenames and setup-log schema"
      pattern: "phase4_setup_enriched.parquet|phase4_setup_log.parquet|setup_validation_export.csv"
---

<objective>
Wire the tested Phase 04 detector into the repo's artifact and CLI pattern so setup signals can be rebuilt, inspected, and handed off directly to trade simulation.

Purpose: satisfy `SGNL-03` with deterministic setup-log output and keep Phase 04 rebuilds consistent with earlier phases.
Output: artifact writers, validation-export logic, and a `build-phase4-setup-detection` CLI subcommand.
</objective>

<execution_context>
@C:/Users/pranav/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pranav/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-setup-detection/04-CONTEXT.md
@.planning/phases/04-setup-detection/04-RESEARCH.md
@.planning/phases/02-daily-vwap-engine/02-02-SUMMARY.md
@.planning/phases/03-simple-structural-levels/03-02-SUMMARY.md
@src/vwap_revert/cli.py
@src/vwap_revert/data_pipeline/cache.py
@src/vwap_revert/indicators/setup_detection.py
@src/vwap_revert/indicators/structural_levels.py
@src/vwap_revert/indicators/vwap.py
@tests/indicators/test_vwap_validation.py
@tests/indicators/test_structural_levels_validation.py

<interfaces>
From Plan 01 contract:

```python
def attach_setup_detection_features(
    df: pl.DataFrame,
    window_start: time = time(9, 30),
    window_end: time = time(11, 30),
    min_sigma: float = 1.7,
    max_sigma: float = 3.0,
) -> pl.DataFrame: ...

def extract_setup_events(enriched_df: pl.DataFrame) -> pl.DataFrame: ...
```

Expected event-log columns:

```python
"trading_date"
"ts_recv"
"ts_recv_et"
"structural_reference_price"
"daily_vwap"
"daily_sigma"
"setup_sigma_signed"
"setup_direction"
"nearest_structural_level"
"nearest_structural_distance"
"structural_levels_within_threshold"
"bid_px_00"
"ask_px_00"
```

From `src/vwap_revert/cli.py`:

```python
def build_phase2_vwap(cache_root: Path, output_root: Path, validation_dates: list[str]) -> Path: ...
def build_phase3_structural_levels(
    cache_root: Path,
    output_root: Path,
    validation_dates: list[str],
    proximity_threshold_points: float = 10.0,
) -> Path: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 04 artifact writers and validation coverage for enriched rows and compact setup logs</name>
  <files>src/vwap_revert/indicators/setup_detection.py, tests/indicators/test_setup_detection_validation.py</files>
  <read_first>
    - src/vwap_revert/indicators/setup_detection.py
    - src/vwap_revert/indicators/vwap.py
    - src/vwap_revert/indicators/structural_levels.py
    - tests/indicators/test_vwap_validation.py
    - tests/indicators/test_structural_levels_validation.py
    - .planning/phases/04-setup-detection/04-CONTEXT.md
  </read_first>
  <action>Extend `src/vwap_revert/indicators/setup_detection.py` with artifact constants `phase4_setup_enriched.parquet`, `phase4_setup_log.parquet`, `setup_validation_export.csv`, and `validation_sessions.json`. Add `write_setup_detection_artifacts(df: pl.DataFrame, output_dir: Path, window_start: time = time(9, 30), window_end: time = time(11, 30), min_sigma: float = 1.7, max_sigma: float = 3.0) -> tuple[pl.DataFrame, Path, Path]` that enriches the input with `attach_setup_detection_features`, extracts setup events with `extract_setup_events`, appends null placeholder columns `regime_label` and `regime_reason` to the compact log, writes the enriched frame to `phase4_setup_enriched.parquet`, writes the compact event log to `phase4_setup_log.parquet`, and returns the enriched Phase 04 frame together with both artifact paths so downstream callers can reuse the exact post-write dataset instead of recomputing it. Add `write_setup_validation_export(enriched_df: pl.DataFrame, output_dir: Path, validation_dates: list[str]) -> None` that filters selected `trading_date` values, sorts by `trading_date` and `ts_recv_et`, and writes `validation/setup_validation_export.csv` containing exactly `trading_date`, `ts_recv_et`, `structural_reference_price`, `daily_vwap`, `daily_sigma`, `setup_sigma_signed`, `time_window_passes`, `vwap_extension_passes`, `structural_confluence_passes`, `setup_candidate_passes`, `setup_event_emitted`, `setup_direction`, `nearest_structural_level`, `nearest_structural_distance`, `structural_levels_within_threshold`, `bid_px_00`, and `ask_px_00`, plus `validation/validation_sessions.json` with keys `validation_dates`, `enriched_artifact`, `setup_log_artifact`, `comparison_export`, and `row_count`. Create `tests/indicators/test_setup_detection_validation.py` with one direct artifact-writer test and one integration-ready CLI artifact expectation test covering those exact filenames, manifest keys, the presence of null `regime_label` and `regime_reason` columns in the setup log, and the returned enriched frame being passed unchanged into `write_setup_validation_export`.</action>
  <acceptance_criteria>
    - `src/vwap_revert/indicators/setup_detection.py` contains `phase4_setup_enriched.parquet`, `phase4_setup_log.parquet`, and `setup_validation_export.csv`
    - `src/vwap_revert/indicators/setup_detection.py` contains `regime_label` and `regime_reason`
    - `tests/indicators/test_setup_detection_validation.py` contains `validation_sessions.json`
    - `tests/indicators/test_setup_detection_validation.py` contains the exact column string `setup_event_emitted`
    - `python -m pytest tests/indicators/test_setup_detection_validation.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_setup_detection_validation.py -q</automated>
  </verify>
  <done>Phase 04 has deterministic artifact writers and automated coverage for the enriched parquet, compact setup log, validation CSV, and manifest metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Phase 04 CLI subcommand that rebuilds setup-detection artifacts from the canonical cache</name>
  <files>src/vwap_revert/cli.py, tests/indicators/test_setup_detection_validation.py</files>
  <read_first>
    - src/vwap_revert/cli.py
    - src/vwap_revert/data_pipeline/cache.py
    - src/vwap_revert/indicators/vwap.py
    - src/vwap_revert/indicators/structural_levels.py
    - src/vwap_revert/indicators/setup_detection.py
    - tests/indicators/test_setup_detection_validation.py
    - .planning/phases/03-simple-structural-levels/03-02-SUMMARY.md
  </read_first>
  <action>Modify `src/vwap_revert/cli.py` to import `attach_daily_vwap_bands`, `attach_prior_day_structural_levels`, `write_setup_detection_artifacts`, and `write_setup_validation_export`. Add `_parse_phase4_validation_dates(validation_dates: list[str]) -> list[str]` that de-duplicates ISO dates and requires at least one `--validation-date`. Add `build_phase4_setup_detection(cache_root: Path, output_root: Path, validation_dates: list[str], proximity_threshold_points: float = 10.0, min_sigma: float = 1.7, max_sigma: float = 3.0) -> Path` that loads `scan_canonical_cache(cache_root).collect()`, enriches it with `attach_daily_vwap_bands`, then `attach_prior_day_structural_levels(..., proximity_threshold_points=proximity_threshold_points)`, then destructures `enriched_phase4_df, enriched_path, setup_log_path = write_setup_detection_artifacts(..., min_sigma=min_sigma, max_sigma=max_sigma)`, and passes that exact `enriched_phase4_df` into `write_setup_validation_export` so the validation export consumes the same post-write Phase 04 frame instead of inferring or reloading it from artifact paths. Return `setup_log_path`. Extend `build_parser()` with a `build-phase4-setup-detection` subcommand requiring `--cache-root`, `--output-root`, one or more `--validation-date`, optional `--proximity-threshold-points` default `10.0`, optional `--min-sigma` default `1.7`, and optional `--max-sigma` default `3.0`. Update `main()` to dispatch that command. Extend the CLI integration test so `main([...])` with `build-phase4-setup-detection` exits `0`, creates `phase4_setup_enriched.parquet`, `phase4_setup_log.parquet`, `validation/setup_validation_export.csv`, and `validation/validation_sessions.json`, and proves `write_setup_validation_export` receives the same in-memory frame returned by `write_setup_detection_artifacts`.</action>
  <acceptance_criteria>
    - `src/vwap_revert/cli.py` contains `build-phase4-setup-detection`
    - `src/vwap_revert/cli.py` contains `--min-sigma` and `--max-sigma`
    - `src/vwap_revert/cli.py` contains `def build_phase4_setup_detection`
    - `python -m pytest tests/indicators -q` passes
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators -q</automated>
  </verify>
  <done>The repo exposes a Phase 04 build command that deterministically rebuilds the setup-enriched dataset, compact event log, and validation preview from the canonical cache and upstream enrichers.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/indicators/test_setup_detection_validation.py -q` after artifact and CLI wiring, then `python -m pytest tests/indicators -q` as the regression gate. The CLI path must emit the four named Phase 04 files without any manual post-processing.
</verification>

<success_criteria>
Phase 04 can be rebuilt from one CLI command, produces a reusable row-level setup artifact plus a compact event log with the required context fields, and hands Phase 5 a stable signal surface.
</success_criteria>

<output>
After completion, create `.planning/phases/04-setup-detection/04-02-SUMMARY.md`
</output>
