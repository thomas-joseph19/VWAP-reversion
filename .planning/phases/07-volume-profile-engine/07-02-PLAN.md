---
phase: 07-volume-profile-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/vwap_revert/indicators/volume_profile.py
  - tests/indicators/test_volume_profile.py
autonomous: true
requirements:
  - STRC-02
  - STRC-04
  - STRC-05
  - STRC-06
must_haves:
  truths:
    - "Each canonical row receives the active overnight, prior-day, and HTF structural references without leaking same-day developing RTH information."
    - "The enriched Phase 7 dataset preserves the existing `prior_rth_*` contract while adding richer level distances, nearest-level labels, and explicit quality/status fields."
    - "The unified `phase7_nearest_structural_level` contract evaluates active prior-day, overnight, and HTF references together so later phases can consume one scalar nearest-level surface."
  artifacts:
    - path: "src/vwap_revert/indicators/volume_profile.py"
      provides: "Row-aligned Phase 7 enrichment and artifact helpers built on the core profile summaries"
      exports:
        - "attach_volume_profile_levels"
        - "write_volume_profile_artifacts"
    - path: "tests/indicators/test_volume_profile.py"
      provides: "Unit coverage for row-level enrichment, nearest-level labeling, and HTF balance-area quality fields"
      contains:
        - "test_enriched_output_carries_htf_balance_edges_and_quality_status"
  key_links:
    - from: "src/vwap_revert/indicators/structural_levels.py"
      to: "src/vwap_revert/indicators/volume_profile.py"
      via: "Phase 7 enrichment preserves `prior_rth_*` compatibility by joining on top of the Phase 3 structural contract"
      pattern: "attach_prior_day_structural_levels|prior_rth_vah|prior_rth_val|prior_rth_poc"
    - from: "src/vwap_revert/indicators/volume_profile.py"
      to: "src/vwap_revert/data_pipeline/cache.py"
      via: "row-level outputs are joined back onto the canonical row stream keyed by `trading_date`"
      pattern: "trading_date|join|phase7_nearest_structural_level"
    - from: ".planning/phases/07-volume-profile-engine/07-CONTEXT.md"
      to: "src/vwap_revert/indicators/volume_profile.py"
      via: "locked decisions define the exact active references and compatibility guarantees"
      pattern: "D-08|D-09|D-12"
---

<objective>
Add the Phase 7 row-aligned enrichment layer and artifact writers so later phases can consume richer structural references without reshaping existing contracts.

Purpose: satisfy the downstream-facing part of `STRC-06` while preserving Phase 3 compatibility and exposing concrete overnight and HTF structural distances.
Output: a tested enriched Phase 7 dataset plus deterministic profile-family parquet artifacts.
</objective>

<execution_context>
@C:/Users/pranav/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pranav/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-volume-profile-engine/07-CONTEXT.md
@.planning/phases/07-volume-profile-engine/07-RESEARCH.md
@.planning/phases/07-volume-profile-engine/07-VALIDATION.md
@.planning/phases/07-volume-profile-engine/07-01-PLAN.md
@src/vwap_revert/indicators/volume_profile.py
@src/vwap_revert/indicators/structural_levels.py
@tests/indicators/test_volume_profile.py

<interfaces>
From `src/vwap_revert/indicators/volume_profile.py` after 07-01:
```python
def build_daily_rth_profiles(df: pl.DataFrame, bucket_size: float = 0.25) -> pl.DataFrame: ...
def build_overnight_profiles(df: pl.DataFrame, bucket_size: float = 0.25) -> pl.DataFrame: ...
def build_htf_profiles(
    daily_profiles: pl.DataFrame,
    lookback_sessions: int = 180,
    min_full_window_sessions: int = 20,
) -> pl.DataFrame: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend unit coverage for enriched row-level structural references and balance-area quality fields</name>
  <files>tests/indicators/test_volume_profile.py</files>
  <read_first>
    - tests/indicators/test_volume_profile.py
    - src/vwap_revert/indicators/volume_profile.py
    - src/vwap_revert/indicators/structural_levels.py
    - .planning/phases/07-volume-profile-engine/07-CONTEXT.md
    - .planning/phases/07-volume-profile-engine/07-VALIDATION.md
  </read_first>
  <action>Extend `tests/indicators/test_volume_profile.py` with the exact tests `test_enriched_output_carries_htf_balance_edges_and_quality_status` and `test_attach_volume_profile_levels_preserves_phase3_columns_and_assigns_nearest_labels`. In the enrichment test, build a small canonical-row fixture that includes both trade rows and quote-only rows, call the future `attach_volume_profile_levels` helper, and assert exact row-level columns `overnight_poc`, `overnight_vah`, `overnight_val`, `htf_poc`, `htf_vah`, `htf_val`, `htf_source_session_count`, `htf_window_complete`, `htf_roll_mixed_window`, `htf_nearest_lvn_above`, `htf_nearest_lvn_below`, `phase7_nearest_structural_level`, `phase7_nearest_structural_distance`, `phase7_levels_within_threshold`, and `phase7_quality_status`. In the compatibility test, assert the enriched frame still contains exact Phase 3 columns `prior_rth_vah`, `prior_rth_val`, `prior_rth_poc`, `nearest_structural_level`, and `nearest_structural_distance` while adding exact new distance columns `distance_to_prior_rth_poc`, `distance_to_prior_rth_vah`, `distance_to_prior_rth_val`, `distance_to_overnight_poc`, `distance_to_overnight_vah`, `distance_to_overnight_val`, `distance_to_htf_poc`, `distance_to_htf_vah`, and `distance_to_htf_val`. Assert that `phase7_nearest_structural_level` returns one of the exact labels `prior_rth_poc`, `prior_rth_vah`, `prior_rth_val`, `overnight_poc`, `overnight_vah`, `overnight_val`, `htf_poc`, `htf_vah`, `htf_val`, `htf_lvn_above`, or `htf_lvn_below`.</action>
  <acceptance_criteria>
    - `tests/indicators/test_volume_profile.py` contains the exact test names `test_enriched_output_carries_htf_balance_edges_and_quality_status` and `test_attach_volume_profile_levels_preserves_phase3_columns_and_assigns_nearest_labels`
    - `tests/indicators/test_volume_profile.py` contains the exact strings `htf_source_session_count`, `htf_window_complete`, `htf_roll_mixed_window`, `phase7_nearest_structural_level`, and `phase7_quality_status`
    - `tests/indicators/test_volume_profile.py` contains the exact strings `distance_to_prior_rth_poc`, `distance_to_overnight_poc`, `distance_to_htf_poc`, `htf_nearest_lvn_above`, and `htf_nearest_lvn_below`
    - `python -m pytest tests/indicators/test_volume_profile.py -q` fails before enrichment is implemented
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_volume_profile.py -q</automated>
  </verify>
  <done>Unit coverage freezes the exact enriched-column contract, compatibility columns, and nearest-level labeling behavior for Phase 7.</done>
</task>

<task type="auto">
  <name>Task 2: Implement row-aligned Phase 7 enrichment and deterministic profile-family artifacts</name>
  <files>src/vwap_revert/indicators/volume_profile.py, tests/indicators/test_volume_profile.py</files>
  <read_first>
    - src/vwap_revert/indicators/volume_profile.py
    - tests/indicators/test_volume_profile.py
    - src/vwap_revert/indicators/structural_levels.py
    - .planning/phases/07-volume-profile-engine/07-CONTEXT.md
    - .planning/phases/07-volume-profile-engine/07-RESEARCH.md
  </read_first>
  <action>Extend `src/vwap_revert/indicators/volume_profile.py` with artifact-name constants `PHASE7_DAILY_ARTIFACT_NAME = "phase7_daily_profiles.parquet"`, `PHASE7_OVERNIGHT_ARTIFACT_NAME = "phase7_overnight_profiles.parquet"`, `PHASE7_HTF_ARTIFACT_NAME = "phase7_htf_profiles.parquet"`, and `PHASE7_ENRICHED_ARTIFACT_NAME = "phase7_volume_profile_enriched.parquet"`. Add `attach_volume_profile_levels(df: pl.DataFrame, proximity_threshold_points: float = 10.0, bucket_size: float = 0.25, htf_lookback_sessions: int = 180) -> pl.DataFrame` that first calls `attach_prior_day_structural_levels(df, proximity_threshold_points=proximity_threshold_points)` to preserve the Phase 3 contract, then joins active overnight and HTF profile summaries on exact `trading_date`. Compute a row-level `phase7_reference_price` using exact precedence `price` for trade rows, `(bid_px_00 + ask_px_00) / 2.0` when both quotes exist, `bid_px_00` when only bid exists, and `ask_px_00` when only ask exists. Add exact scalar columns `overnight_poc`, `overnight_vah`, `overnight_val`, `htf_poc`, `htf_vah`, `htf_val`, `distance_to_prior_rth_poc`, `distance_to_prior_rth_vah`, `distance_to_prior_rth_val`, `distance_to_overnight_poc`, `distance_to_overnight_vah`, `distance_to_overnight_val`, `distance_to_htf_poc`, `distance_to_htf_vah`, `distance_to_htf_val`, `htf_nearest_lvn_above`, `htf_nearest_lvn_below`, `overnight_quality_status`, `htf_quality_status`, `htf_source_session_count`, `htf_window_complete`, `htf_roll_mixed_window`, and `phase7_quality_status`. Set `phase7_quality_status` to the exact strings `ok` when both overnight and HTF levels are available, `missing_overnight` when overnight levels are null, `missing_htf` when HTF levels are null, and `missing_both` when both families are unavailable. Add `phase7_nearest_structural_level`, `phase7_nearest_structural_distance`, and `phase7_levels_within_threshold` across the exact candidate set `prior_rth_poc`, `prior_rth_vah`, `prior_rth_val`, `overnight_poc`, `overnight_vah`, `overnight_val`, `htf_poc`, `htf_vah`, `htf_val`, `htf_lvn_above`, and `htf_lvn_below`, with lower absolute distance winning and the exact tie-break order listed here. Use this exact tie-break order: `prior_rth_poc`, `prior_rth_vah`, `prior_rth_val`, `overnight_poc`, `overnight_vah`, `overnight_val`, `htf_poc`, `htf_vah`, `htf_val`, `htf_lvn_above`, `htf_lvn_below`. Add `write_volume_profile_artifacts(df: pl.DataFrame, output_dir: Path, proximity_threshold_points: float = 10.0, bucket_size: float = 0.25, htf_lookback_sessions: int = 180) -> tuple[Path, Path, Path, Path]` that writes the daily, overnight, HTF, and enriched parquet artifacts using the exact filenames above and returns the four paths in that order.</action>
  <acceptance_criteria>
    - `src/vwap_revert/indicators/volume_profile.py` contains `def attach_volume_profile_levels` and `def write_volume_profile_artifacts`
    - `src/vwap_revert/indicators/volume_profile.py` contains the exact strings `phase7_daily_profiles.parquet`, `phase7_overnight_profiles.parquet`, `phase7_htf_profiles.parquet`, and `phase7_volume_profile_enriched.parquet`
    - `src/vwap_revert/indicators/volume_profile.py` contains the exact strings `phase7_reference_price`, `phase7_quality_status`, `missing_overnight`, `missing_htf`, `missing_both`, `distance_to_prior_rth_poc`, `prior_rth_poc`, `prior_rth_vah`, `prior_rth_val`, `htf_nearest_lvn_above`, and `htf_nearest_lvn_below`
    - `src/vwap_revert/indicators/volume_profile.py` contains `attach_prior_day_structural_levels`
    - `python -m pytest tests/indicators/test_volume_profile.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_volume_profile.py -q</automated>
  </verify>
  <done>The Phase 7 indicator module can enrich canonical rows with overnight and HTF structural references while preserving existing prior-day columns and writing deterministic parquet artifacts.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/indicators/test_volume_profile.py -q`. Confirm the enriched output retains `prior_rth_*` columns and adds scalar HTF balance-area and nearest-LVN fields rather than only list columns.
</verification>

<success_criteria>
Phase 7 exposes a stable enriched-data contract and deterministic artifact writer that later phases can consume directly without recomputing profile logic.
</success_criteria>

<output>
After completion, create `.planning/phases/07-volume-profile-engine/07-02-SUMMARY.md`
</output>
