---
phase: 07-volume-profile-engine
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - src/vwap_revert/indicators/volume_profile.py
  - src/vwap_revert/cli.py
  - tests/indicators/test_volume_profile_validation.py
autonomous: true
requirements:
  - STRC-02
  - STRC-03
  - STRC-04
  - STRC-05
  - STRC-06
must_haves:
  truths:
    - "One Phase 7 CLI command rebuilds all daily, overnight, HTF, enriched, and validation outputs directly from the canonical cache."
    - "Validation exports show overnight levels, prior-day daily levels, HTF balance edges, and LVN references for selected dates in one deterministic CSV."
    - "The Phase 7 output directory is self-describing through a manifest that records artifact names, validation dates, bucket size, and HTF window settings."
  artifacts:
    - path: "src/vwap_revert/indicators/volume_profile.py"
      provides: "Validation-export writer for Phase 7 outputs"
      exports:
        - "write_volume_profile_validation_export"
    - path: "src/vwap_revert/cli.py"
      provides: "Phase 7 CLI build command"
      contains:
        - "build-phase7-volume-profile"
    - path: "tests/indicators/test_volume_profile_validation.py"
      provides: "CLI and artifact integration coverage for Phase 7 outputs"
      contains:
        - "test_cli_writes_phase7_volume_profile_artifacts"
  key_links:
    - from: "src/vwap_revert/cli.py"
      to: "src/vwap_revert/indicators/volume_profile.py"
      via: "Phase 7 command scans the canonical cache and delegates all artifact writing to the indicator module"
      pattern: "build_phase7_volume_profile|write_volume_profile_artifacts|write_volume_profile_validation_export"
    - from: ".planning/phases/07-volume-profile-engine/07-VALIDATION.md"
      to: "tests/indicators/test_volume_profile_validation.py"
      via: "validation strategy names the exact CLI and artifact outputs that Phase 7 must emit"
      pattern: "07-03-01|phase7_daily_profiles.parquet|volume_profile_validation_export.csv"
    - from: "src/vwap_revert/data_pipeline/cache.py"
      to: "src/vwap_revert/cli.py"
      via: "the canonical cache remains the sole build input boundary"
      pattern: "scan_canonical_cache"
---

<objective>
Complete Phase 7 by wiring deterministic artifact emission and one CLI command that rebuilds the entire volume-profile surface from the canonical cache.

Purpose: finish the executable Phase 7 contract so manual validation and later phases can consume a stable output directory.
Output: CLI wiring, validation-export writing, and integration tests for the full Phase 7 build path.
</objective>

<execution_context>
@C:/Users/pranav/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pranav/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-volume-profile-engine/07-CONTEXT.md
@.planning/phases/07-volume-profile-engine/07-RESEARCH.md
@.planning/phases/07-volume-profile-engine/07-VALIDATION.md
@.planning/phases/07-volume-profile-engine/07-02-PLAN.md
@src/vwap_revert/data_pipeline/cache.py
@src/vwap_revert/indicators/volume_profile.py
@src/vwap_revert/cli.py
@tests/indicators/test_structural_levels_validation.py

<interfaces>
From `src/vwap_revert/indicators/volume_profile.py` after 07-02:
```python
def write_volume_profile_artifacts(
    df: pl.DataFrame,
    output_dir: Path,
    proximity_threshold_points: float = 10.0,
    bucket_size: float = 0.25,
    htf_lookback_sessions: int = 180,
) -> tuple[Path, Path, Path, Path]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add integration tests for the Phase 7 CLI, parquet artifacts, and validation manifest</name>
  <files>tests/indicators/test_volume_profile_validation.py</files>
  <read_first>
    - tests/indicators/test_structural_levels_validation.py
    - src/vwap_revert/indicators/volume_profile.py
    - src/vwap_revert/cli.py
    - .planning/phases/07-volume-profile-engine/07-CONTEXT.md
    - .planning/phases/07-volume-profile-engine/07-VALIDATION.md
  </read_first>
  <action>Create `tests/indicators/test_volume_profile_validation.py` with a temporary canonical-cache fixture written via `write_parquet(..., use_pyarrow=True, pyarrow_options={"partition_cols": ["trading_date"]})` so the test matches the real Phase 1 input boundary. Add the exact integration test `test_cli_writes_phase7_volume_profile_artifacts`. That test must call `main([...])` with the exact subcommand `build-phase7-volume-profile`, `--cache-root`, `--output-root`, one or more `--validation-date` values, `--bucket-size 0.25`, and `--htf-lookback-sessions 180`. Assert that exact files `phase7_daily_profiles.parquet`, `phase7_overnight_profiles.parquet`, `phase7_htf_profiles.parquet`, `phase7_volume_profile_enriched.parquet`, `validation/volume_profile_validation_export.csv`, and `validation/validation_sessions.json` exist. Load the validation CSV and assert it contains exact columns `trading_date`, `ts_recv_et`, `prior_rth_poc`, `prior_rth_vah`, `prior_rth_val`, `overnight_poc`, `overnight_vah`, `overnight_val`, `htf_poc`, `htf_vah`, `htf_val`, `htf_nearest_lvn_above`, `htf_nearest_lvn_below`, `phase7_nearest_structural_level`, `phase7_nearest_structural_distance`, `phase7_quality_status`, `htf_source_session_count`, and `htf_roll_mixed_window`. Assert the validation manifest contains exact keys `validation_dates`, `bucket_size`, `htf_lookback_sessions`, `daily_artifact`, `overnight_artifact`, `htf_artifact`, `enriched_artifact`, `validation_export`, and `row_count`.</action>
  <acceptance_criteria>
    - `tests/indicators/test_volume_profile_validation.py` contains the exact test name `test_cli_writes_phase7_volume_profile_artifacts`
    - `tests/indicators/test_volume_profile_validation.py` contains `build-phase7-volume-profile`
    - `tests/indicators/test_volume_profile_validation.py` contains the exact strings `phase7_daily_profiles.parquet`, `phase7_overnight_profiles.parquet`, `phase7_htf_profiles.parquet`, `phase7_volume_profile_enriched.parquet`, and `volume_profile_validation_export.csv`
    - `python -m pytest tests/indicators/test_volume_profile_validation.py -q` fails before the CLI and validation-export layer is implemented
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_volume_profile_validation.py -q</automated>
  </verify>
  <done>The integration suite freezes the Phase 7 artifact names, CLI arguments, validation-export schema, and manifest keys.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the Phase 7 validation export and CLI rebuild command</name>
  <files>src/vwap_revert/indicators/volume_profile.py, src/vwap_revert/cli.py, tests/indicators/test_volume_profile_validation.py</files>
  <read_first>
    - src/vwap_revert/indicators/volume_profile.py
    - src/vwap_revert/cli.py
    - tests/indicators/test_volume_profile_validation.py
    - src/vwap_revert/data_pipeline/cache.py
    - .planning/phases/07-volume-profile-engine/07-CONTEXT.md
    - .planning/phases/07-volume-profile-engine/07-RESEARCH.md
  </read_first>
  <action>Extend `src/vwap_revert/indicators/volume_profile.py` with `VOLUME_PROFILE_VALIDATION_EXPORT_NAME = "volume_profile_validation_export.csv"` and `VOLUME_PROFILE_VALIDATION_MANIFEST_NAME = "validation_sessions.json"`. Add `write_volume_profile_validation_export(enriched_df: pl.DataFrame, output_dir: Path, validation_dates: list[str], bucket_size: float = 0.25, htf_lookback_sessions: int = 180) -> None` that creates `validation/`, filters `enriched_df` by exact `trading_date` membership in `validation_dates`, sorts by `trading_date` and `ts_recv_et`, and writes `validation/volume_profile_validation_export.csv` with the exact columns `trading_date`, `ts_recv_et`, `prior_rth_poc`, `prior_rth_vah`, `prior_rth_val`, `overnight_poc`, `overnight_vah`, `overnight_val`, `htf_poc`, `htf_vah`, `htf_val`, `htf_nearest_lvn_above`, `htf_nearest_lvn_below`, `phase7_nearest_structural_level`, `phase7_nearest_structural_distance`, `phase7_quality_status`, `htf_source_session_count`, and `htf_roll_mixed_window`. Write `validation/validation_sessions.json` with exact keys `validation_dates`, `bucket_size`, `htf_lookback_sessions`, `daily_artifact`, `overnight_artifact`, `htf_artifact`, `enriched_artifact`, `validation_export`, and `row_count`, using the exact artifact filenames from 07-02. Modify `src/vwap_revert/cli.py` to import `attach_volume_profile_levels`, `write_volume_profile_artifacts`, and `write_volume_profile_validation_export`, add `_parse_phase7_validation_dates(validation_dates: list[str]) -> list[str]`, add `build_phase7_volume_profile(cache_root: Path, output_root: Path, validation_dates: list[str], proximity_threshold_points: float = 10.0, bucket_size: float = 0.25, htf_lookback_sessions: int = 180) -> Path`, and wire a `build-phase7-volume-profile` subcommand with required `--cache-root`, `--output-root`, and one or more `--validation-date` arguments plus optional `--proximity-threshold-points`, `--bucket-size`, and `--htf-lookback-sessions`. The command must read the canonical cache via `scan_canonical_cache(cache_root).collect()`, call `write_volume_profile_artifacts(...)`, call `write_volume_profile_validation_export(...)` on the enriched parquet contents, and return the path `output_root / "phase7_volume_profile_enriched.parquet"`.</action>
  <acceptance_criteria>
    - `src/vwap_revert/indicators/volume_profile.py` contains `def write_volume_profile_validation_export`
    - `src/vwap_revert/indicators/volume_profile.py` contains `volume_profile_validation_export.csv` and `validation_sessions.json`
    - `src/vwap_revert/indicators/volume_profile.py` contains the exact strings `bucket_size`, `htf_lookback_sessions`, `daily_artifact`, `overnight_artifact`, `htf_artifact`, and `enriched_artifact`
    - `src/vwap_revert/cli.py` contains `build-phase7-volume-profile`
    - `src/vwap_revert/cli.py` contains `--cache-root`, `--output-root`, `--validation-date`, `--bucket-size`, and `--htf-lookback-sessions`
    - `python -m pytest tests/indicators/test_volume_profile.py tests/indicators/test_volume_profile_validation.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_volume_profile.py tests/indicators/test_volume_profile_validation.py -q</automated>
  </verify>
  <done>The repo can rebuild every Phase 7 artifact from the canonical cache through one CLI command, with deterministic validation exports and manifest metadata for manual chart checks.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/indicators/test_volume_profile.py tests/indicators/test_volume_profile_validation.py -q`. Confirm the validation CSV is a filtered subset of `phase7_volume_profile_enriched.parquet` and the manifest records `bucket_size` `0.25` plus `htf_lookback_sessions` `180`.
</verification>

<success_criteria>
Phase 7 is executable end to end: one CLI command rebuilds daily, overnight, HTF, enriched, and validation outputs from the canonical cache with deterministic filenames and metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/07-volume-profile-engine/07-03-SUMMARY.md`
</output>
