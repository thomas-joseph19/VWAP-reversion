---
phase: 07-volume-profile-engine
plan: 04
type: execute
wave: 4
depends_on:
  - 07-03
files_modified:
  - src/vwap_revert/indicators/volume_profile.py
  - tests/indicators/test_volume_profile.py
  - tests/indicators/test_volume_profile_validation.py
autonomous: true
requirements:
  - STRC-04
  - STRC-06
gap_closure: true
must_haves:
  truths:
    - "HTF 180-session composites remain populated across quarterly front-month rolls while still using only completed sessions strictly earlier than the target trading date."
    - "Mixed-roll mature windows emit usable HTF POC, VAH, and VAL on the current session's contract axis instead of empty histograms and null balance edges."
    - "Row-level Phase 7 enrichment keeps HTF balance edges available on mixed-roll sessions so the richer HTF structure can replace the simple prior-day proxy."
  artifacts:
    - path: "src/vwap_revert/indicators/volume_profile.py"
      provides: "Roll-aware HTF composite assembly and row-level HTF balance-edge propagation"
      exports:
        - "build_htf_profiles"
        - "attach_volume_profile_levels"
    - path: "tests/indicators/test_volume_profile.py"
      provides: "Unit coverage for the mixed-roll HTF alignment policy and row-level HTF availability"
      contains:
        - "test_htf_composite_aligns_mixed_roll_windows_onto_current_contract_axis"
        - "test_attach_volume_profile_levels_keeps_mixed_roll_htf_edges_available"
    - path: "tests/indicators/test_volume_profile_validation.py"
      provides: "Integration coverage for mixed-roll HTF outputs through the Phase 7 CLI"
      contains:
        - "test_cli_writes_phase7_volume_profile_artifacts"
  key_links:
    - from: "tests/indicators/test_volume_profile.py"
      to: "src/vwap_revert/indicators/volume_profile.py"
      via: "Mixed-roll HTF unit fixtures define the exact bridge policy and expected current-axis outputs"
      pattern: "test_htf_composite_aligns_mixed_roll_windows_onto_current_contract_axis|roll_adjustment_applied|roll_anchor_symbol"
    - from: "src/vwap_revert/indicators/volume_profile.py"
      to: "tests/indicators/test_volume_profile_validation.py"
      via: "CLI validation must show populated HTF balance edges for mixed-roll validation dates"
      pattern: "htf_poc|htf_vah|htf_val|htf_roll_mixed_window|phase7_quality_status"
    - from: ".planning/phases/07-volume-profile-engine/07-VERIFICATION.md"
      to: "src/vwap_revert/indicators/volume_profile.py"
      via: "The verifier gap requires replacing null mixed-roll HTF windows with a usable quarterly-roll policy"
      pattern: "usable 180-session HTF composite|consistently available HTF balance edges"
---

<objective>
Close the Phase 7 verifier blocker by replacing the null-on-mixed-roll HTF policy with a deterministic quarterly-roll bridge that still preserves strict prior-session causality.

Purpose: satisfy the remaining blocked requirements `STRC-04` and `STRC-06` without reopening completed overnight, daily, LVN, artifact, or CLI scope.
Output: updated HTF core logic and tests that produce usable mixed-roll composites plus row-level HTF balance edges on mature windows.
</objective>

<execution_context>
@C:/Users/pranav/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pranav/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-volume-profile-engine/07-CONTEXT.md
@.planning/phases/07-volume-profile-engine/07-RESEARCH.md
@.planning/phases/07-volume-profile-engine/07-VERIFICATION.md
@.planning/phases/07-volume-profile-engine/07-VALIDATION.md
@.planning/phases/07-volume-profile-engine/07-01-SUMMARY.md
@.planning/phases/07-volume-profile-engine/07-02-SUMMARY.md
@.planning/phases/07-volume-profile-engine/07-03-SUMMARY.md
@src/vwap_revert/indicators/volume_profile.py
@tests/indicators/test_volume_profile.py
@tests/indicators/test_volume_profile_validation.py

<interfaces>
From `src/vwap_revert/indicators/volume_profile.py`:
```python
def build_htf_profiles(
    daily_profiles: pl.DataFrame,
    lookback_sessions: int = 180,
    min_full_window_sessions: int = 20,
) -> pl.DataFrame: ...

def attach_volume_profile_levels(
    df: pl.DataFrame,
    proximity_threshold_points: float = 10.0,
    bucket_size: float = 0.25,
    htf_lookback_sessions: int = 180,
) -> pl.DataFrame: ...
```

Current blocker to replace:
```python
roll_mixed_window = len(source_front_symbols) > 1
if roll_mixed_window:
    rows.append(row)
    continue
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite the mixed-roll HTF tests to require a usable bridged composite and available balance edges</name>
  <files>tests/indicators/test_volume_profile.py, tests/indicators/test_volume_profile_validation.py</files>
  <read_first>
    - tests/indicators/test_volume_profile.py
    - tests/indicators/test_volume_profile_validation.py
    - src/vwap_revert/indicators/volume_profile.py
    - .planning/phases/07-volume-profile-engine/07-VERIFICATION.md
    - .planning/phases/07-volume-profile-engine/07-RESEARCH.md
    - .planning/phases/07-volume-profile-engine/07-03-SUMMARY.md
  </read_first>
  <action>Replace the existing mixed-roll nulling assertions with a deterministic roll-bridged policy. In `tests/indicators/test_volume_profile.py`, rename `test_htf_composite_nulls_levels_when_roll_window_contains_multiple_front_symbols` to the exact name `test_htf_composite_aligns_mixed_roll_windows_onto_current_contract_axis`. Keep the existing `_mixed_roll_daily_profiles()` fixture shape, but assert that for trading date `2023-09-20` with `lookback_sessions=2` and `min_full_window_sessions=2` the HTF row still reports `roll_mixed_window is True` while also reporting `roll_adjustment_applied is True`, `roll_anchor_symbol == "NQZ3"`, `source_session_count == 2`, `window_complete is True`, `bucket_prices == [15100.0, 15100.25]`, `bucket_volumes == [22.0, 38.0]`, `htf_poc == 15100.25`, `htf_vah == 15100.25`, `htf_val == 15100.0`, and `quality_status == "full_window"`. Add a second exact unit test named `test_attach_volume_profile_levels_keeps_mixed_roll_htf_edges_available` that builds a canonical-row fixture for `2023-09-20` using the same two-source mixed-roll history and asserts the enriched rows carry non-null `htf_poc`, `htf_vah`, and `htf_val`, keep `htf_roll_mixed_window is True`, set `phase7_quality_status == "ok"`, and expose the exact audit columns `roll_adjustment_applied` and `roll_anchor_symbol` after the HTF join. In `tests/indicators/test_volume_profile_validation.py`, keep the same CLI test name `test_cli_writes_phase7_volume_profile_artifacts`, but change the mixed-roll validation assertions so the `2023-09-20` rows still have `htf_roll_mixed_window == True` while `htf_poc`, `htf_vah`, and `htf_val` are all non-null and `phase7_quality_status` is exactly `ok`. Do not broaden the validation schema or artifact list beyond what already exists.</action>
  <acceptance_criteria>
    - `tests/indicators/test_volume_profile.py` contains the exact test names `test_htf_composite_aligns_mixed_roll_windows_onto_current_contract_axis` and `test_attach_volume_profile_levels_keeps_mixed_roll_htf_edges_available`
    - `tests/indicators/test_volume_profile.py` contains the exact strings `roll_adjustment_applied`, `roll_anchor_symbol`, `bucket_prices == [15100.0, 15100.25]`, `bucket_volumes == [22.0, 38.0]`, and `quality_status == "full_window"`
    - `tests/indicators/test_volume_profile_validation.py` contains `phase7_quality_status` and no longer contains the exact string `assert mixed_roll_rows["htf_poc"].is_null().all()`
    - `python -m pytest tests/indicators/test_volume_profile.py tests/indicators/test_volume_profile_validation.py -q` fails before implementation because the code still nulls mixed-roll HTF windows and does not expose the new audit fields
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_volume_profile.py tests/indicators/test_volume_profile_validation.py -q</automated>
  </verify>
  <done>The tests now define the required quarterly-roll bridge behavior and reject the old null-on-mixed-roll HTF policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement roll-bridged HTF composites and keep mixed-roll HTF edges populated through enrichment</name>
  <files>src/vwap_revert/indicators/volume_profile.py, tests/indicators/test_volume_profile.py, tests/indicators/test_volume_profile_validation.py</files>
  <read_first>
    - src/vwap_revert/indicators/volume_profile.py
    - tests/indicators/test_volume_profile.py
    - tests/indicators/test_volume_profile_validation.py
    - .planning/phases/07-volume-profile-engine/07-CONTEXT.md
    - .planning/phases/07-volume-profile-engine/07-RESEARCH.md
    - .planning/phases/07-volume-profile-engine/07-VERIFICATION.md
  </read_first>
  <action>Modify `build_htf_profiles(...)` so it no longer short-circuits mixed-roll windows to empty histograms. Keep D-07 and D-08 intact by using only strictly prior completed daily profiles, but add a deterministic bridge onto the target session's contract axis: when a prior-window row's `front_symbol` differs from the target date's `front_symbol`, shift every bucket in that source histogram by the cumulative sum of roll-boundary deltas encountered between the source row and the target row, where each roll-boundary delta is the exact difference `current_daily_rth_poc - prior_daily_rth_poc` at the completed-session symbol-change boundary in `daily_profiles`. Use those shifted bucket prices when aggregating the window, so the existing `_mixed_roll_daily_profiles()` fixture yields the exact combined buckets `[15100.0, 15100.25]` with volumes `[22.0, 38.0]` on `2023-09-20`. Add the exact HTF output columns `roll_adjustment_applied` and `roll_anchor_symbol`; set `roll_adjustment_applied` to `True` when any source histogram was shifted and `roll_anchor_symbol` to the target row's `front_symbol`. Keep `roll_mixed_window` and `source_front_symbols` as audit metadata, but set `quality_status` based only on history completeness (`insufficient_history`, `partial_window`, `full_window`) once the bridged composite exists. Update any internal histogram helper needed so bucket shifts happen before summation rather than after level extraction. Then extend `attach_volume_profile_levels(...)` to join the new HTF audit fields onto the enriched dataset, preserve `phase7_quality_status == "ok"` whenever mixed-roll rows now have non-null `htf_poc`, and do not regress existing overnight, daily, LVN, artifact, or validation-export contracts. Keep the existing validation CSV schema unchanged; the change is the populated HTF values on mixed-roll dates, not a new artifact surface.</action>
  <acceptance_criteria>
    - `src/vwap_revert/indicators/volume_profile.py` contains the exact strings `roll_adjustment_applied`, `roll_anchor_symbol`, `current_daily_rth_poc`, and `prior_daily_rth_poc`
    - `src/vwap_revert/indicators/volume_profile.py` no longer contains a branch equivalent to `if roll_mixed_window: rows.append(row); continue`
    - `src/vwap_revert/indicators/volume_profile.py` still contains `roll_mixed_window`, `source_front_symbols`, `htf_poc`, `htf_vah`, `htf_val`, and `phase7_quality_status`
    - `python -m pytest tests/indicators/test_volume_profile.py tests/indicators/test_volume_profile_validation.py -q` passes
    - `python -m pytest tests/indicators/test_structural_levels.py tests/indicators/test_structural_levels_validation.py tests/indicators/test_setup_detection.py tests/indicators/test_setup_detection_validation.py tests/indicators/test_trade_simulation.py tests/indicators/test_trade_simulation_validation.py tests/indicators/test_core_analytics.py tests/indicators/test_core_analytics_validation.py tests/indicators/test_volume_profile.py tests/indicators/test_volume_profile_validation.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_volume_profile.py tests/indicators/test_volume_profile_validation.py -q</automated>
  </verify>
  <done>Mixed-roll HTF windows now emit deterministic bridged composites and row-level HTF balance edges remain available on mature quarterly-roll sessions.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/indicators/test_volume_profile.py tests/indicators/test_volume_profile_validation.py -q`, then run the broader downstream regression command from the verifier report. Confirm the mixed-roll `2023-09-20` validation rows still show `htf_roll_mixed_window == True` but now carry non-null `htf_poc`, `htf_vah`, and `htf_val`.
</verification>

<success_criteria>
Phase 7 no longer fails on normal quarterly-roll HTF windows: the 180-session HTF composite stays causal and usable, and HTF balance edges are consistently available to downstream consumers.
</success_criteria>

<output>
After completion, create `.planning/phases/07-volume-profile-engine/07-04-SUMMARY.md`
</output>
