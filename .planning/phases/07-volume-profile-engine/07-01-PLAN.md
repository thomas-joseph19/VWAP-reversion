---
phase: 07-volume-profile-engine
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/vwap_revert/indicators/volume_profile.py
  - src/vwap_revert/indicators/__init__.py
  - tests/indicators/test_volume_profile.py
autonomous: true
requirements:
  - STRC-02
  - STRC-03
  - STRC-04
  - STRC-05
must_haves:
  truths:
    - "Completed overnight sessions produce same-day overnight VAH, VAL, and POC from Globex trade rows only."
    - "Completed RTH sessions produce deterministic one-tick daily histograms and prior-session structural summaries without using same-day developing profiles."
    - "The HTF composite for trading date `T` uses only completed daily RTH profiles strictly earlier than `T`."
    - "LVN outputs are deterministic and stable enough that small one-bucket noise does not create spurious nodes."
  artifacts:
    - path: "src/vwap_revert/indicators/volume_profile.py"
      provides: "Core Phase 7 profile builders for overnight, daily, HTF, and LVN extraction"
      exports:
        - "build_daily_rth_profiles"
        - "build_overnight_profiles"
        - "build_htf_profiles"
        - "detect_lvn_prices"
    - path: "tests/indicators/test_volume_profile.py"
      provides: "Unit coverage for histogram construction, causality, and LVN stability"
      contains:
        - "test_overnight_profile_uses_completed_globex_rows_only"
        - "test_daily_profiles_build_tick_aligned_histograms_and_prior_day_levels"
        - "test_htf_composite_excludes_current_session_and_tracks_window_coverage"
        - "test_lvn_detection_prefers_stable_local_minima"
  key_links:
    - from: "src/vwap_revert/data_pipeline/sessions.py"
      to: "src/vwap_revert/indicators/volume_profile.py"
      via: "Phase 7 reuses `trading_date`, `is_overnight`, and `is_rth` instead of redefining session windows"
      pattern: "trading_date|is_overnight|is_rth"
    - from: "src/vwap_revert/indicators/structural_levels.py"
      to: "src/vwap_revert/indicators/volume_profile.py"
      via: "Phase 7 inherits the Phase 3 deterministic POC and 70% value-area tie-break rules"
      pattern: "_compute_session_levels|poc|vah|val"
    - from: ".planning/phases/07-volume-profile-engine/07-CONTEXT.md"
      to: "src/vwap_revert/indicators/volume_profile.py"
      via: "locked Phase 7 decisions define causal profile families and one-tick bucketing"
      pattern: "D-01|D-02|D-04|D-05|D-06|D-07"
---

<objective>
Establish the tested Phase 7 indicator core for completed-session volume profiles before any artifact or CLI wiring is added.

Purpose: satisfy the math and causality requirements behind `STRC-02`, `STRC-03`, `STRC-04`, and `STRC-05`.
Output: a new `volume_profile.py` module with deterministic overnight, daily, HTF, and LVN primitives exported for later artifact and CLI plans.
</objective>

<execution_context>
@C:/Users/pranav/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pranav/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-volume-profile-engine/07-CONTEXT.md
@.planning/phases/07-volume-profile-engine/07-RESEARCH.md
@.planning/phases/07-volume-profile-engine/07-VALIDATION.md
@src/vwap_revert/data_pipeline/cache.py
@src/vwap_revert/data_pipeline/contracts.py
@src/vwap_revert/data_pipeline/sessions.py
@src/vwap_revert/indicators/structural_levels.py
@src/vwap_revert/indicators/__init__.py
@tests/indicators/test_structural_levels.py

<interfaces>
From `src/vwap_revert/data_pipeline/cache.py`:
```python
def scan_canonical_cache(output_dir: Path) -> pl.LazyFrame: ...
```

From `src/vwap_revert/indicators/structural_levels.py`:
```python
def compute_prior_day_structural_levels(df: pl.DataFrame) -> pl.DataFrame: ...
def attach_prior_day_structural_levels(
    df: pl.DataFrame,
    proximity_threshold_points: float = 10.0,
) -> pl.DataFrame: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing unit tests for overnight, daily, HTF, and LVN profile primitives</name>
  <files>tests/indicators/test_volume_profile.py</files>
  <read_first>
    - tests/indicators/test_structural_levels.py
    - src/vwap_revert/indicators/structural_levels.py
    - src/vwap_revert/data_pipeline/sessions.py
    - .planning/phases/07-volume-profile-engine/07-CONTEXT.md
    - .planning/phases/07-volume-profile-engine/07-RESEARCH.md
    - .planning/phases/07-volume-profile-engine/07-VALIDATION.md
  </read_first>
  <action>Create `tests/indicators/test_volume_profile.py` and import the not-yet-implemented symbols `build_daily_rth_profiles`, `build_overnight_profiles`, `build_htf_profiles`, and `detect_lvn_prices` from `vwap_revert.indicators.volume_profile`. Build small fixture DataFrames with exact columns `trading_date`, `ts_recv`, `ts_recv_et`, `is_overnight`, `is_rth`, `side`, `price`, `size`, and `front_symbol` so the tests freeze the Phase 7 input contract. Add the exact tests `test_overnight_profile_uses_completed_globex_rows_only`, `test_daily_profiles_build_tick_aligned_histograms_and_prior_day_levels`, `test_htf_composite_excludes_current_session_and_tracks_window_coverage`, and `test_lvn_detection_prefers_stable_local_minima`. In the overnight test, assert the resulting row for trading date `2023-06-15` has exact columns `overnight_poc`, `overnight_vah`, `overnight_val`, `bucket_size`, `profile_trade_rows`, and `quality_status`, and assert that RTH rows do not contribute to overnight volume. In the daily-profile test, assert bucket prices are snapped to exact `0.25` increments and the summary row includes exact columns `prior_rth_poc`, `prior_rth_vah`, `prior_rth_val`, `bucket_prices`, and `bucket_volumes`. In the HTF test, assert the row for trading date `2023-06-16` excludes `2023-06-16` daily data from the composite, includes exact columns `htf_poc`, `htf_vah`, `htf_val`, `source_session_count`, `window_target_sessions`, `window_complete`, `roll_mixed_window`, and `source_front_symbols`, and marks `window_complete` false when fewer than 180 prior sessions exist. In the LVN test, call `detect_lvn_prices` directly with bucket prices `[100.0, 100.25, 100.5, 100.75, 101.0]` and volumes `[120.0, 40.0, 130.0, 42.0, 125.0]`, then assert the exact output list is `[100.25, 100.75]` when `min_prominence_ratio=0.20`.</action>
  <acceptance_criteria>
    - `tests/indicators/test_volume_profile.py` contains `from vwap_revert.indicators.volume_profile import build_daily_rth_profiles, build_overnight_profiles, build_htf_profiles, detect_lvn_prices`
    - `tests/indicators/test_volume_profile.py` contains the exact test names `test_overnight_profile_uses_completed_globex_rows_only`, `test_daily_profiles_build_tick_aligned_histograms_and_prior_day_levels`, `test_htf_composite_excludes_current_session_and_tracks_window_coverage`, and `test_lvn_detection_prefers_stable_local_minima`
    - `tests/indicators/test_volume_profile.py` contains the exact strings `source_session_count`, `window_target_sessions`, `window_complete`, `roll_mixed_window`, and `source_front_symbols`
    - `python -m pytest tests/indicators/test_volume_profile.py -q` fails before implementation because `vwap_revert.indicators.volume_profile` or its exported symbols do not yet exist
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_volume_profile.py -q</automated>
  </verify>
  <done>Failing unit tests freeze the exact Phase 7 profile-builder API, causal window rules, tick-grid bucketing, and deterministic LVN behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the Phase 7 profile core and export it from the indicators package</name>
  <files>src/vwap_revert/indicators/volume_profile.py, src/vwap_revert/indicators/__init__.py, tests/indicators/test_volume_profile.py</files>
  <read_first>
    - src/vwap_revert/indicators/__init__.py
    - src/vwap_revert/indicators/structural_levels.py
    - tests/indicators/test_volume_profile.py
    - src/vwap_revert/data_pipeline/contracts.py
    - .planning/phases/07-volume-profile-engine/07-CONTEXT.md
    - .planning/phases/07-volume-profile-engine/07-RESEARCH.md
  </read_first>
  <action>Create `src/vwap_revert/indicators/volume_profile.py` with module constants `DEFAULT_BUCKET_SIZE = 0.25`, `DEFAULT_VALUE_AREA_FRACTION = 0.70`, `DEFAULT_HTF_LOOKBACK_SESSIONS = 180`, and `DEFAULT_LVN_MIN_PROMINENCE_RATIO = 0.20`. Reuse the Phase 3 deterministic tie-break rules from `structural_levels.py` for `POC`, `VAH`, and `VAL`, but apply them to completed-session histogram rows built from trade-bearing rows only per D-01, D-04, and D-05. Add `detect_lvn_prices(bucket_prices: list[float], bucket_volumes: list[float], min_prominence_ratio: float = 0.20) -> list[float]` that smooths the histogram with the exact kernel `[0.25, 0.5, 0.25]`, identifies strict local minima, and only keeps minima where `min(neighbor_left, neighbor_right) - center >= max(bucket_volumes) * min_prominence_ratio`. Add `build_daily_rth_profiles(df: pl.DataFrame, bucket_size: float = 0.25) -> pl.DataFrame` that filters exact `is_rth == True` and `side in {"A","B"}`, snaps price buckets with `round(price / bucket_size) * bucket_size`, and returns one row per `trading_date` with exact columns `trading_date`, `profile_family`, `source_trading_date`, `bucket_size`, `bucket_prices`, `bucket_volumes`, `daily_rth_poc`, `daily_rth_vah`, `daily_rth_val`, `prior_rth_poc`, `prior_rth_vah`, `prior_rth_val`, `lvn_prices`, `profile_trade_rows`, `profile_total_volume`, `quality_status`, and `front_symbol`. Set `profile_family` to the exact string `daily_rth`, `source_trading_date` equal to `trading_date`, and keep `prior_rth_*` as the immediately previous completed RTH session summary per D-12. Add `build_overnight_profiles(df: pl.DataFrame, bucket_size: float = 0.25) -> pl.DataFrame` that filters exact `is_overnight == True` and `side in {"A","B"}` and returns one row per `trading_date` with exact columns `trading_date`, `profile_family`, `source_trading_date`, `bucket_size`, `bucket_prices`, `bucket_volumes`, `overnight_poc`, `overnight_vah`, `overnight_val`, `lvn_prices`, `profile_trade_rows`, `profile_total_volume`, `quality_status`, and `front_symbol`, where `source_trading_date` equals the same session's `trading_date`. Add `build_htf_profiles(daily_profiles: pl.DataFrame, lookback_sessions: int = 180, min_full_window_sessions: int = 20) -> pl.DataFrame` that builds one row per current `trading_date` from strictly prior daily rows on the unadjusted front-month price axis, sums aligned bucket volumes across the prior window, and emits exact columns `trading_date`, `profile_family`, `window_start_date`, `window_end_date`, `bucket_size`, `bucket_prices`, `bucket_volumes`, `htf_poc`, `htf_vah`, `htf_val`, `lvn_prices`, `source_session_count`, `window_target_sessions`, `window_complete`, `roll_mixed_window`, `source_front_symbols`, and `quality_status`. Set `profile_family` to `htf_180d`, `window_target_sessions` to `180`, `window_complete` true only when `source_session_count == 180`, `roll_mixed_window` true when more than one distinct `front_symbol` appears in the source window, `quality_status` to `insufficient_history` when `source_session_count < 20`, `partial_window` when `20 <= source_session_count < 180`, and `full_window` when `source_session_count == 180`. Update `src/vwap_revert/indicators/__init__.py` so `volume_profile` is imported and included in `__all__`.</action>
  <acceptance_criteria>
    - `src/vwap_revert/indicators/volume_profile.py` contains `DEFAULT_BUCKET_SIZE = 0.25`, `DEFAULT_HTF_LOOKBACK_SESSIONS = 180`, and `DEFAULT_LVN_MIN_PROMINENCE_RATIO = 0.20`
    - `src/vwap_revert/indicators/volume_profile.py` contains `def build_daily_rth_profiles`, `def build_overnight_profiles`, `def build_htf_profiles`, and `def detect_lvn_prices`
    - `src/vwap_revert/indicators/volume_profile.py` contains the exact strings `daily_rth_poc`, `overnight_poc`, `htf_poc`, `roll_mixed_window`, `source_front_symbols`, `insufficient_history`, `partial_window`, and `full_window`
    - `src/vwap_revert/indicators/__init__.py` contains `volume_profile`
    - `python -m pytest tests/indicators/test_volume_profile.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_volume_profile.py -q</automated>
  </verify>
  <done>The repo exposes a tested Phase 7 volume-profile core that builds causal overnight, daily, and HTF summaries plus deterministic LVN outputs on a one-tick grid.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/indicators/test_volume_profile.py -q`. Confirm the HTF row for any trading date never includes that same date as a source session and that LVN detection stays stable on the explicit five-bucket fixture.
</verification>

<success_criteria>
Phase 7 has a reusable indicator core for completed-session overnight, daily, and 180-day HTF profiles with deterministic LVN detection and explicit quality metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/07-volume-profile-engine/07-01-SUMMARY.md`
</output>
