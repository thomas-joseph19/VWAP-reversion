---
phase: 05-trade-simulation-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vwap_revert/indicators/trade_simulation.py
  - src/vwap_revert/indicators/__init__.py
  - tests/indicators/test_trade_simulation.py
autonomous: true
requirements:
  - TSIM-01
  - TSIM-02
  - TSIM-03
  - TSIM-04
  - TSIM-05
  - TSIM-07
  - TSIM-08
must_haves:
  truths:
    - "A Phase 4 setup event opens a trade immediately on the same timestamp using the setup direction."
    - "Each completed trade exits by exactly one baseline reason: `target_vwap`, `stop_loss`, or `session_end`."
    - "Bid/ask-side fills, configurable extra impact, and round-trip commission are all reflected in per-trade net P&L."
    - "At most one trade is live at any time, and overlapping setup events are logged as skipped."
    - "Open trades are forced flat on the first row at or after 16:00:00 ET when no target or stop fired earlier."
  artifacts:
    - path: "src/vwap_revert/indicators/trade_simulation.py"
      provides: "Phase 5 replay config, side-aware fill helpers, and sequential trade simulator."
      min_lines: 180
    - path: "tests/indicators/test_trade_simulation.py"
      provides: "Executable coverage for entry, exit, stop, friction, overlap, and session-end rules."
      min_lines: 160
    - path: "src/vwap_revert/indicators/__init__.py"
      provides: "Public export of the trade_simulation module."
      contains: "trade_simulation"
  key_links:
    - from: "src/vwap_revert/indicators/trade_simulation.py"
      to: "phase4_setup_log.parquet rows"
      via: "chronological setup replay keyed by `trading_date` and `ts_recv`"
      pattern: "setup_direction|ts_recv|trading_date"
    - from: "src/vwap_revert/indicators/trade_simulation.py"
      to: "phase4_setup_enriched.parquet rows"
      via: "post-entry row scan using `daily_vwap`, `bid_px_00`, and `ask_px_00`"
      pattern: "daily_vwap|bid_px_00|ask_px_00"
    - from: "tests/indicators/test_trade_simulation.py"
      to: "src/vwap_revert/indicators/trade_simulation.py"
      via: "pytest cases for immediate entry, adverse-first exits, and one-position gating"
      pattern: "test_.*trade_simulation"
---

<objective>
Implement the tested Phase 5 replay core that turns Phase 4 setup events into one-position-at-a-time trades with deterministic fills, stops, and session-end exits.

Purpose: Convert the setup log into a mechanically replayable trade stream without re-deriving signals, so Phase 6 can analyze actual trades instead of setup candidates.
Output: `trade_simulation.py` plus a focused core test suite that locks the replay contract.
</objective>

<execution_context>
@C:/Users/pranav/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pranav/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-trade-simulation-engine/05-CONTEXT.md
@.planning/phases/05-trade-simulation-engine/05-RESEARCH.md
@.planning/phases/04-setup-detection/04-01-SUMMARY.md
@.planning/phases/04-setup-detection/04-02-SUMMARY.md
@src/vwap_revert/indicators/setup_detection.py
@src/vwap_revert/indicators/vwap.py
@src/vwap_revert/indicators/__init__.py
@tests/indicators/test_setup_detection.py

<interfaces>
From `src/vwap_revert/indicators/setup_detection.py`:

```python
def extract_setup_events(enriched_df: pl.DataFrame) -> pl.DataFrame
def write_setup_detection_artifacts(...) -> tuple[Path, Path]
```

Compact Phase 4 setup rows currently expose:

```python
[
    "trading_date",
    "ts_recv",
    "ts_recv_et",
    "structural_reference_price",
    "daily_vwap",
    "daily_sigma",
    "setup_sigma_signed",
    "setup_direction",
    "nearest_structural_level",
    "nearest_structural_distance",
    "structural_levels_within_threshold",
    "bid_px_00",
    "ask_px_00",
]
```

From `src/vwap_revert/cli.py`:

```python
def build_phase4_setup_detection(...) -> Path
def main(argv: list[str] | None = None) -> int
```

From `src/vwap_revert/indicators/vwap.py`:

```python
def _require_columns(df: pl.DataFrame, required: set[str]) -> None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the Phase 5 replay contract tests first</name>
  <files>tests/indicators/test_trade_simulation.py, src/vwap_revert/indicators/trade_simulation.py</files>
  <read_first>
    tests/indicators/test_setup_detection.py
    tests/indicators/test_setup_detection_validation.py
    src/vwap_revert/indicators/setup_detection.py
    src/vwap_revert/indicators/vwap.py
    tests/indicators/test_trade_simulation.py
    src/vwap_revert/indicators/trade_simulation.py
  </read_first>
  <behavior>
    - Test immediate entry on the setup row per D-01, D-02, and D-03 using the setup log timestamp and direction with no extra confirmation bar.
    - Test executable-side target logic per D-04 and D-07: long trades only exit at target when `bid_px_00 >= daily_vwap`; short trades only exit at target when `ask_px_00 <= daily_vwap`.
    - Test spread-crossing and extra impact per D-04 and D-05: long entry fill is `ask_px_00 + additional_slippage_points`, short entry fill is `bid_px_00 - additional_slippage_points`, long exit fill is `bid_px_00 - additional_slippage_points`, short exit fill is `ask_px_00 + additional_slippage_points`.
    - Test net P&L per D-06 with `round_trip_commission=5.0`, `point_value=20.0`, and `tick_size=0.25`.
    - Test one-position gating per D-10 and D-11 by feeding two overlapping setups and asserting the second is recorded with `skip_reason == "position_active"`.
    - Test stop-loss and same-row collision handling per D-08 and research guidance: use `stop_loss_points=20.0` and assert that when stop and target are both true on one row the exit reason is `stop_loss`.
    - Test forced session exit per D-12 using the first row with `ts_recv_et.time() >= 16:00:00` and `exit_reason == "session_end"`.
  </behavior>
  <action>Create `tests/indicators/test_trade_simulation.py` with deterministic Polars fixtures that include Phase 4 setup-log columns plus replay-path quote rows. Add explicit tests named `test_enters_immediately_from_setup_event`, `test_vwap_target_uses_executable_quote_side`, `test_fill_prices_apply_bbo_side_and_additional_impact`, `test_round_trip_commission_reduces_net_pnl`, `test_overlapping_setups_are_skipped_while_position_active`, `test_stop_loss_uses_executable_mae_and_adverse_first_ordering`, and `test_session_end_forces_exit_at_1600_et`. Import the planned public API from `vwap_revert.indicators.trade_simulation` with exact names `SimulationConfig`, `simulate_trades`, `entry_fill_price`, and `exit_fill_price`. Leave the module under test absent or incomplete so this test file fails first.</action>
  <acceptance_criteria>
    - `tests/indicators/test_trade_simulation.py` contains the exact string `def test_enters_immediately_from_setup_event`.
    - `tests/indicators/test_trade_simulation.py` contains the exact string `def test_stop_loss_uses_executable_mae_and_adverse_first_ordering`.
    - `tests/indicators/test_trade_simulation.py` contains the exact string `skip_reason == "position_active"`.
    - `tests/indicators/test_trade_simulation.py` imports `SimulationConfig`, `simulate_trades`, `entry_fill_price`, and `exit_fill_price` from `vwap_revert.indicators.trade_simulation`.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_trade_simulation.py -x</automated>
  </verify>
  <done>The repo has a failing red-phase test suite that names every required Phase 5 core behavior and uses the exact baseline defaults `stop_loss_points=20.0`, `round_trip_commission=5.0`, `additional_slippage_points=0.0`, and `session_exit_time=16:00:00`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the sequential trade simulator and export it</name>
  <files>src/vwap_revert/indicators/trade_simulation.py, src/vwap_revert/indicators/__init__.py, tests/indicators/test_trade_simulation.py</files>
  <read_first>
    src/vwap_revert/indicators/setup_detection.py
    src/vwap_revert/indicators/vwap.py
    src/vwap_revert/indicators/__init__.py
    tests/indicators/test_trade_simulation.py
    src/vwap_revert/indicators/trade_simulation.py
  </read_first>
  <action>Create `src/vwap_revert/indicators/trade_simulation.py` with a frozen `SimulationConfig` dataclass containing these exact defaults: `stop_loss_points=20.0`, `round_trip_commission=5.0`, `additional_slippage_points=0.0`, `session_exit_time=time(16, 0)`, `point_value=20.0`, `tick_size=0.25`, `tick_value=5.0`, and `contracts=1`. Implement `entry_fill_price(row: dict[str, object], direction: str, additional_slippage_points: float) -> float` and `exit_fill_price(...) -> float` using D-04 side rules exactly. Implement `simulate_trades(setup_log: pl.DataFrame, path_rows: pl.DataFrame, config: SimulationConfig = SimulationConfig()) -> tuple[pl.DataFrame, pl.DataFrame]` that sorts by `trading_date` and `ts_recv`, validates required columns with `_require_columns`, replays the compact setup log per D-03, opens immediately on the setup row per D-01, uses `setup_direction` directly per D-02, scans only rows from the same `trading_date` where `ts_recv >= entry_ts`, checks stop before target when both are true on the same row, applies MAE from executable exit-side quotes, force-exits on the first row whose `ts_recv_et` time is `>= 16:00:00`, records skipped overlapping setups with `skip_reason` equal to `position_active`, and returns two DataFrames: a completed trade log and a skipped-setup log. Include completed-trade columns with these exact names: `trade_id`, `trading_date`, `setup_ts`, `entry_ts`, `exit_ts`, `setup_direction`, `entry_price`, `exit_price`, `target_price`, `stop_price`, `exit_reason`, `gross_points`, `gross_ticks`, `gross_dollars`, `net_dollars`, `mae_points`, `mfe_points`, `duration_seconds`, `setup_sigma_signed`, `nearest_structural_level`, `nearest_structural_distance`, `structural_levels_within_threshold`, `entry_bid_px_00`, `entry_ask_px_00`, `exit_bid_px_00`, `exit_ask_px_00`, `additional_slippage_points`, and `round_trip_commission`. Update `src/vwap_revert/indicators/__init__.py` so `trade_simulation` is imported and listed in `__all__`.</action>
  <acceptance_criteria>
    - `src/vwap_revert/indicators/trade_simulation.py` contains the exact string `class SimulationConfig`.
    - `src/vwap_revert/indicators/trade_simulation.py` contains the exact string `def simulate_trades(`.
    - `src/vwap_revert/indicators/trade_simulation.py` contains the exact strings `exit_reason`, `target_vwap`, `stop_loss`, and `session_end`.
    - `src/vwap_revert/indicators/trade_simulation.py` contains the exact string `skip_reason`.
    - `src/vwap_revert/indicators/__init__.py` contains the exact string `from . import setup_detection, structural_levels, trade_simulation, vwap`.
    - `src/vwap_revert/indicators/__init__.py` contains the exact string `__all__ = ["setup_detection", "structural_levels", "trade_simulation", "vwap"]`.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_trade_simulation.py -x</automated>
  </verify>
  <done>`simulate_trades` produces deterministic completed-trade and skipped-setup DataFrames that satisfy TSIM-01/02/03/04/05/07/08 with the locked Phase 5 decisions and the new core tests pass.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/indicators/test_trade_simulation.py -x` and confirm the suite proves immediate setup-driven entry, quote-side exits, friction modeling, one-position gating, adverse-first stop precedence, and 16:00 ET forced liquidation.
</verification>

<success_criteria>
The repo contains a reusable Phase 5 replay engine that consumes Phase 4 setup events directly, models BBO-side execution with zero extra slippage by default, and returns deterministic trade/skipped outputs with passing automated coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/05-trade-simulation-engine/05-01-SUMMARY.md`
</output>
