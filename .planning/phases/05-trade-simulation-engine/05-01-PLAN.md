---
phase: 05-trade-simulation-engine
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/vwap_revert/simulation.py
  - tests/indicators/test_trade_simulation.py
autonomous: true
requirements:
  - TSIM-01
  - TSIM-03
  - TSIM-04
must_haves:
  truths:
    - "The simulator opens trades immediately from Phase 4 setup events without adding any continuation-failure confirmation layer."
    - "Entry and exit price helpers use executable BBO sides with separate configurable additional impact that defaults to 0.0."
    - "Net P&L is computed from NQ point value and always subtracts the configured round-trip commission exactly once per completed trade."
  artifacts:
    - path: "src/vwap_revert/simulation.py"
      provides: "Phase 5 simulation config, fill helpers, and entry/P&L primitives"
      exports:
        - "SimulationConfig"
        - "entry_fill_price"
        - "exit_fill_price"
        - "pnl_points"
        - "pnl_dollars"
        - "open_trade_from_setup"
    - path: "tests/indicators/test_trade_simulation.py"
      provides: "Deterministic unit coverage for immediate entry, quote-side fills, additional impact, and commission-adjusted P&L"
      contains:
        - "test_enters_immediately_from_setup_event"
        - "test_fill_prices_apply_bbo_side_and_additional_impact"
        - "test_round_trip_commission_reduces_net_pnl"
  key_links:
    - from: "src/vwap_revert/indicators/setup_detection.py"
      to: "src/vwap_revert/simulation.py"
      via: "setup event columns consumed directly without re-derivation"
      pattern: "setup_direction|ts_recv|bid_px_00|ask_px_00|daily_vwap|setup_sigma_signed|nearest_structural_level|nearest_structural_distance|regime_label|regime_reason"
    - from: ".planning/phases/05-trade-simulation-engine/05-CONTEXT.md"
      to: "src/vwap_revert/simulation.py"
      via: "locked decisions for immediate entry and cost model"
      pattern: "D-01|D-04|D-05|D-06"
---

<objective>
Create the Phase 5 simulator contract and cost primitives so the trade engine can open trades from Phase 4 events, price entries and exits from executable BBO sides, and compute net trade economics consistently.

Purpose: satisfy `TSIM-01`, `TSIM-03`, and `TSIM-04` before adding replay-state exit logic.
Output: a tested `simulation.py` module that defines the public config and pricing contract for downstream trade replay.
</objective>

<execution_context>
@C:/Users/pranav/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pranav/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-trade-simulation-engine/05-CONTEXT.md
@.planning/phases/05-trade-simulation-engine/05-RESEARCH.md
@.planning/phases/05-trade-simulation-engine/05-VALIDATION.md
@.planning/phases/04-setup-detection/04-02-SUMMARY.md
@src/vwap_revert/indicators/setup_detection.py
@src/vwap_revert/indicators/vwap.py
@tests/indicators/test_setup_detection.py
@tests/indicators/test_setup_detection_validation.py

<interfaces>
From `src/vwap_revert/indicators/setup_detection.py` compact setup-log contract:

```python
def extract_setup_events(enriched_df: pl.DataFrame) -> pl.DataFrame: ...
```

Expected setup-event columns:

```python
"trading_date"
"ts_recv"
"ts_recv_et"
"daily_vwap"
"daily_sigma"
"setup_sigma_signed"
"setup_direction"
"bid_px_00"
"ask_px_00"
"nearest_structural_level"
"nearest_structural_distance"
"regime_label"
"regime_reason"
```

Research-backed execution rules:

```python
long entry  -> ask_px_00 + additional_slippage_points
short entry -> bid_px_00 - additional_slippage_points
long exit   -> bid_px_00 - additional_slippage_points
short exit  -> ask_px_00 + additional_slippage_points
```

NQ economic constants:

```python
POINT_VALUE = 20.0
TICK_SIZE = 0.25
TICK_VALUE = 5.0
DEFAULT_ROUND_TRIP_COMMISSION = 5.00
DEFAULT_ADDITIONAL_SLIPPAGE_POINTS = 0.0
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for immediate setup-driven entry, quote-side fill pricing, and net P&L math</name>
  <files>tests/indicators/test_trade_simulation.py</files>
  <read_first>
    - tests/indicators/test_setup_detection.py
    - tests/indicators/test_setup_detection_validation.py
    - src/vwap_revert/indicators/setup_detection.py
    - .planning/phases/05-trade-simulation-engine/05-CONTEXT.md
    - .planning/phases/05-trade-simulation-engine/05-RESEARCH.md
    - .planning/phases/05-trade-simulation-engine/05-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: a setup row with `setup_direction == "long"` opens immediately at the same event timestamp using `ask_px_00`, while a setup row with `setup_direction == "short"` opens at `bid_px_00`, per D-01 through D-04.
    - Test 2: `entry_fill_price` and `exit_fill_price` apply additional impact in points on top of the executable quote side and preserve the exact default of `0.0`, per D-04 and D-05.
    - Test 3: `pnl_points` and `pnl_dollars` compute signed gross outcome correctly for long and short trades and subtract the configured round-trip commission exactly once, per D-06.
  </behavior>
  <action>Create `tests/indicators/test_trade_simulation.py` with a compact fixture containing at least one long setup row and one short setup row. The fixture must include exact columns `trading_date`, `ts_recv`, `ts_recv_et`, `setup_direction`, `daily_vwap`, `setup_sigma_signed`, `bid_px_00`, `ask_px_00`, `nearest_structural_level`, `nearest_structural_distance`, `regime_label`, and `regime_reason`. Import the yet-to-be-created symbols `SimulationConfig`, `entry_fill_price`, `exit_fill_price`, `open_trade_from_setup`, `pnl_points`, and `pnl_dollars` from `vwap_revert.simulation`. Freeze these exact defaults in assertions: `round_trip_commission == 5.0`, `additional_slippage_points == 0.0`, `point_value == 20.0`, and `session_exit_time == time(16, 0)`. Include exact fill assertions for a long entry using `ask_px_00`, a short entry using `bid_px_00`, a long exit using `bid_px_00`, and a short exit using `ask_px_00`. Assert that `open_trade_from_setup(...)` preserves `nearest_structural_level`, `nearest_structural_distance`, `regime_label`, and `regime_reason` on the returned open trade contract. Include one assertion where a long trade from `100.25` to `101.25` yields `1.0` gross point and `15.0` net dollars after a `5.0` round-trip commission, and one assertion where a short trade from `100.00` to `99.00` yields the same net dollars.</action>
  <acceptance_criteria>
    - `tests/indicators/test_trade_simulation.py` contains `from vwap_revert.simulation import SimulationConfig, entry_fill_price, exit_fill_price, open_trade_from_setup, pnl_points, pnl_dollars`
    - `tests/indicators/test_trade_simulation.py` contains the exact strings `additional_slippage_points == 0.0`, `round_trip_commission == 5.0`, and `session_exit_time == time(16, 0)`
    - `tests/indicators/test_trade_simulation.py` contains the exact test names `test_enters_immediately_from_setup_event`, `test_fill_prices_apply_bbo_side_and_additional_impact`, and `test_round_trip_commission_reduces_net_pnl`
    - `python -m pytest tests/indicators/test_trade_simulation.py -q` fails before implementation because `vwap_revert.simulation` or the imported symbols do not yet exist
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_trade_simulation.py -q</automated>
  </verify>
  <done>Failing tests define the exact Phase 5 simulator API, default config values, and quote-side fill semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the simulation config and entry/cost primitives in a new simulation module</name>
  <files>src/vwap_revert/simulation.py, tests/indicators/test_trade_simulation.py</files>
  <read_first>
    - src/vwap_revert/indicators/setup_detection.py
    - src/vwap_revert/indicators/vwap.py
    - tests/indicators/test_trade_simulation.py
    - .planning/phases/05-trade-simulation-engine/05-CONTEXT.md
    - .planning/phases/05-trade-simulation-engine/05-RESEARCH.md
  </read_first>
  <action>Create `src/vwap_revert/simulation.py` with a frozen dataclass `SimulationConfig` containing exact defaults `stop_loss_points: float = 20.0`, `round_trip_commission: float = 5.0`, `additional_slippage_points: float = 0.0`, `point_value: float = 20.0`, `tick_size: float = 0.25`, `tick_value: float = 5.0`, and `session_exit_time: time = time(16, 0)`. Add helper functions `entry_fill_price(row: Mapping[str, object], direction: str, additional_slippage_points: float = 0.0) -> float`, `exit_fill_price(row: Mapping[str, object], direction: str, additional_slippage_points: float = 0.0) -> float`, `pnl_points(direction: str, entry_price: float, exit_price: float) -> float`, `pnl_dollars(direction: str, entry_price: float, exit_price: float, point_value: float, round_trip_commission: float) -> float`, and `open_trade_from_setup(setup: Mapping[str, object], config: SimulationConfig) -> dict[str, object]`. In `open_trade_from_setup`, preserve the setup event timestamp as both `entry_ts` and `signal_ts`, copy through `trading_date`, `setup_direction`, `daily_vwap`, `setup_sigma_signed`, `nearest_structural_level`, `nearest_structural_distance`, `regime_label`, and `regime_reason`, and use `entry_fill_price` to compute `entry_price`. Store exact trade fields `direction`, `entry_ts`, `entry_price`, `signal_ts`, `target_price`, `stop_loss_points`, `round_trip_commission`, `additional_slippage_points`, `status`, and `entry_reason`. Set `entry_reason` to the exact string `setup_event` and `status` to the exact string `open`. Raise `ValueError` if required quote columns for the chosen direction are null or if `setup_direction` is not exactly `long` or `short`.</action>
  <acceptance_criteria>
    - `src/vwap_revert/simulation.py` contains `class SimulationConfig`
    - `src/vwap_revert/simulation.py` contains `def entry_fill_price`, `def exit_fill_price`, `def open_trade_from_setup`, `def pnl_points`, and `def pnl_dollars`
    - `src/vwap_revert/simulation.py` contains the exact strings `entry_reason`, `setup_event`, `status`, `open`, `nearest_structural_level`, `nearest_structural_distance`, `regime_label`, and `regime_reason`
    - `python -m pytest tests/indicators/test_trade_simulation.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_trade_simulation.py -q</automated>
  </verify>
  <done>The repo exposes a tested simulation contract with stable config defaults, quote-side fill helpers, and commission-adjusted P&L primitives.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/indicators/test_trade_simulation.py -q` after the module exists. Confirm the simulator contract keeps Phase 4 setup timestamps intact and that default costs are explicit rather than inferred.
</verification>

<success_criteria>
Phase 5 has a reusable simulation contract that opens trades immediately from Phase 4 events, prices fills from executable quotes, and computes net dollars consistently.
</success_criteria>

<output>
After completion, create `.planning/phases/05-trade-simulation-engine/05-01-SUMMARY.md`
</output>
