---
phase: 05-trade-simulation-engine
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/vwap_revert/cli.py
  - src/vwap_revert/simulation.py
  - tests/indicators/test_trade_simulation.py
  - tests/indicators/test_trade_simulation_validation.py
autonomous: true
requirements:
  - TSIM-05
  - TSIM-01
  - TSIM-02
  - TSIM-03
  - TSIM-04
  - TSIM-07
  - TSIM-08
must_haves:
  truths:
    - "Only one position can be open at a time, and any setup emitted while a trade is active is skipped rather than queued or netted."
    - "A single Phase 5 CLI command rebuilds deterministic trade artifacts from the Phase 4 setup log and enriched row-level parquet."
    - "Trade artifacts carry enough fields for Phase 6 analytics: entry and exit timestamps, prices, reason, P&L, MAE, duration, sigma context, and explicit skip diagnostics."
  artifacts:
    - path: "src/vwap_revert/simulation.py"
      provides: "Batch simulation, artifact writer, and skip-diagnostics schema"
      exports:
        - "write_trade_simulation_artifacts"
    - path: "src/vwap_revert/cli.py"
      provides: "Phase 5 build subcommand"
      contains:
        - "build-phase5-trade-simulation"
    - path: "tests/indicators/test_trade_simulation_validation.py"
      provides: "CLI and artifact coverage for Phase 5 outputs"
      contains:
        - "test_cli_writes_phase5_trade_artifacts"
  key_links:
    - from: "src/vwap_revert/cli.py"
      to: "src/vwap_revert/simulation.py"
      via: "Phase 5 command loads Phase 4 artifacts and emits trade-level outputs"
      pattern: "build_phase5_trade_simulation|write_trade_simulation_artifacts"
    - from: ".planning/phases/05-trade-simulation-engine/05-VALIDATION.md"
      to: "tests/indicators/test_trade_simulation.py"
      via: "TSIM-05 overlap coverage mapping for task 05-03-01"
      pattern: "05-03-01|test_overlapping_setups_are_skipped_while_position_active"
---

<objective>
Finish Phase 5 by enforcing one-position-at-a-time sequencing, persisting deterministic trade artifacts, and wiring a CLI command that rebuilds the trade log from Phase 4 outputs.

Purpose: satisfy `TSIM-05` and deliver the complete Phase 5 artifact handoff for Phase 6 analytics while regression-covering the rest of the replay engine.
Output: artifact writers, CLI wiring, overlap-skip behavior, and integration tests for Phase 5 outputs.
</objective>

<execution_context>
@C:/Users/pranav/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pranav/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-trade-simulation-engine/05-CONTEXT.md
@.planning/phases/05-trade-simulation-engine/05-RESEARCH.md
@.planning/phases/05-trade-simulation-engine/05-VALIDATION.md
@.planning/phases/05-trade-simulation-engine/05-01-PLAN.md
@.planning/phases/05-trade-simulation-engine/05-02-PLAN.md
@src/vwap_revert/cli.py
@src/vwap_revert/simulation.py
@src/vwap_revert/indicators/setup_detection.py
@tests/indicators/test_setup_detection_validation.py
@tests/indicators/test_trade_simulation.py

<interfaces>
Phase 4 artifact filenames:

```python
"phase4_setup_enriched.parquet"
"phase4_setup_log.parquet"
```

Required Phase 5 output concepts:

```python
trade log
skip log
validation export
validation manifest
```

Expected command shape in `src/vwap_revert/cli.py`:

```python
build-phase5-trade-simulation
--phase4-root
--output-root
--validation-date
--stop-loss-points
--round-trip-commission
--additional-slippage-points
```

Batch replay contract from Plan 02:

```python
@dataclass(frozen=True)
class SimulationBatchResult:
    trade_log: pl.DataFrame
    skipped_setups: pl.DataFrame

def simulate_trades(
    setup_log: pl.DataFrame,
    path_rows: pl.DataFrame,
    config: SimulationConfig,
) -> SimulationBatchResult: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add overlap-skip integration tests and deterministic trade-artifact writer coverage</name>
  <files>tests/indicators/test_trade_simulation.py, tests/indicators/test_trade_simulation_validation.py, src/vwap_revert/simulation.py</files>
  <read_first>
    - tests/indicators/test_setup_detection_validation.py
    - tests/indicators/test_trade_simulation.py
    - tests/indicators/test_trade_simulation_validation.py
    - src/vwap_revert/simulation.py
    - src/vwap_revert/indicators/setup_detection.py
    - .planning/phases/05-trade-simulation-engine/05-CONTEXT.md
    - .planning/phases/05-trade-simulation-engine/05-VALIDATION.md
  </read_first>
  <action>Extend `tests/indicators/test_trade_simulation.py` with the exact unit test `test_overlapping_setups_are_skipped_while_position_active`. That test must build a `setup_log` fixture with at least two setups on the same `trading_date` where the second setup timestamp occurs before the first trade exits, then assert `simulate_trades(...)` returns a `SimulationBatchResult` whose `trade_log` has exactly one completed trade and whose `skipped_setups` has exactly one skipped setup row with the exact skip reason `position_active`. Keep `tests/indicators/test_trade_simulation_validation.py` focused on the CLI artifact test only. That integration test must expect exact output filenames `phase5_trade_log.parquet`, `phase5_skipped_setups.parquet`, `trade_simulation_validation_export.csv`, and `validation_sessions.json`. The validation export must include exact columns `trading_date`, `entry_ts`, `exit_ts`, `direction`, `entry_price`, `exit_price`, `exit_reason`, `gross_points`, `net_dollars`, `mae_points`, `duration_seconds`, `setup_sigma_signed`, `nearest_structural_level`, `nearest_structural_distance`, `regime_label`, and `regime_reason`. The manifest must include exact keys `phase4_setup_log_artifact`, `phase4_enriched_artifact`, `trade_log_artifact`, `skipped_setups_artifact`, `comparison_export`, `validation_dates`, `row_count`, and `stop_loss_baseline_note`. Extend `src/vwap_revert/simulation.py` as needed to expose a writer entrypoint `write_trade_simulation_artifacts(...)` that consumes `SimulationBatchResult` rather than re-deriving skipped setups inside the artifact layer and persists the exact `stop_loss_baseline_note` string documenting the default stop as a configurable non-optimized baseline.</action>
  <acceptance_criteria>
    - `tests/indicators/test_trade_simulation.py` contains `position_active`
    - `tests/indicators/test_trade_simulation.py` contains the exact test name `test_overlapping_setups_are_skipped_while_position_active`
    - `tests/indicators/test_trade_simulation_validation.py` contains `phase5_trade_log.parquet`, `phase5_skipped_setups.parquet`, and `trade_simulation_validation_export.csv`
    - `tests/indicators/test_trade_simulation_validation.py` contains the exact test name `test_cli_writes_phase5_trade_artifacts`
    - `python -m pytest tests/indicators/test_trade_simulation.py::test_overlapping_setups_are_skipped_while_position_active -x` fails before overlap skip handling is implemented
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_trade_simulation.py::test_overlapping_setups_are_skipped_while_position_active -x</automated>
  </verify>
  <done>The core test suite locks the one-position rule in `tests/indicators/test_trade_simulation.py`, and the validation suite locks the exact Phase 5 artifact filenames and schemas separately.</done>
</task>

<task type="auto">
  <name>Task 2: Implement one-position skip handling, artifact writers, and the Phase 5 CLI subcommand</name>
  <files>src/vwap_revert/simulation.py, src/vwap_revert/cli.py, tests/indicators/test_trade_simulation.py, tests/indicators/test_trade_simulation_validation.py</files>
  <read_first>
    - src/vwap_revert/cli.py
    - src/vwap_revert/simulation.py
    - src/vwap_revert/indicators/setup_detection.py
    - tests/indicators/test_trade_simulation.py
    - tests/indicators/test_trade_simulation_validation.py
    - .planning/phases/05-trade-simulation-engine/05-CONTEXT.md
    - .planning/phases/05-trade-simulation-engine/05-RESEARCH.md
  </read_first>
  <action>Extend `simulate_trades` in `src/vwap_revert/simulation.py` so it fills the already-defined `SimulationBatchResult.skipped_setups` DataFrame: track the exit timestamp of the active trade and, when a later setup has `ts_recv <= active_exit_ts`, append that setup to `skipped_setups` with exact fields `trading_date`, `ts_recv`, `setup_direction`, `setup_sigma_signed`, and `skip_reason`, where `skip_reason` is exactly `position_active`. Keep the return type unchanged from Plan 02: `simulate_trades(...) -> SimulationBatchResult`. Make the overlap behavior satisfy the core unit test `tests/indicators/test_trade_simulation.py::test_overlapping_setups_are_skipped_while_position_active` rather than the validation suite. Add `write_trade_simulation_artifacts(phase4_root: Path, output_root: Path, validation_dates: list[str], config: SimulationConfig) -> tuple[Path, Path]` that reads `phase4_setup_log.parquet` and `phase4_setup_enriched.parquet`, runs `simulate_trades`, writes `result.trade_log` to `phase5_trade_log.parquet`, writes `result.skipped_setups` to `phase5_skipped_setups.parquet`, and writes `validation/trade_simulation_validation_export.csv` plus `validation/validation_sessions.json` using the exact manifest keys frozen in Task 1. Modify `src/vwap_revert/cli.py` to import `SimulationConfig` and `write_trade_simulation_artifacts`, add `_parse_phase5_validation_dates(validation_dates: list[str]) -> list[str]`, add `build_phase5_trade_simulation(phase4_root: Path, output_root: Path, validation_dates: list[str], stop_loss_points: float = 20.0, round_trip_commission: float = 5.0, additional_slippage_points: float = 0.0) -> Path`, and wire a `build-phase5-trade-simulation` subcommand with required `--phase4-root`, `--output-root`, one or more `--validation-date`, optional `--stop-loss-points`, optional `--round-trip-commission`, and optional `--additional-slippage-points`. Return the trade-log path and keep all artifact filenames exact.</action>
  <acceptance_criteria>
    - `src/vwap_revert/simulation.py` contains `SimulationBatchResult`, `trade_log`, `skipped_setups`, and `position_active`
    - `src/vwap_revert/simulation.py` contains `phase5_trade_log.parquet` and `phase5_skipped_setups.parquet`
    - `src/vwap_revert/cli.py` contains `build-phase5-trade-simulation`
    - `src/vwap_revert/cli.py` contains `--stop-loss-points`, `--round-trip-commission`, and `--additional-slippage-points`
    - `python -m pytest tests/indicators/test_trade_simulation.py::test_overlapping_setups_are_skipped_while_position_active -x` passes
    - `python -m pytest tests/indicators/test_trade_simulation_validation.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_trade_simulation.py tests/indicators/test_trade_simulation_validation.py -q</automated>
  </verify>
  <done>The repo can rebuild complete Phase 5 trade artifacts from Phase 4 outputs, enforce one-position-at-a-time sequencing, and expose a deterministic CLI command for downstream analytics.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/indicators/test_trade_simulation.py tests/indicators/test_trade_simulation_validation.py -q` after CLI wiring. Confirm the skip log exists even when empty and that the trade log columns are sufficient for Phase 6 analytics without reopening Phase 5.
</verification>

<success_criteria>
Phase 5 emits a deterministic trade log and skipped-setup log from one CLI command, enforces the single-position rule, and hands Phase 6 a stable artifact contract.
</success_criteria>

<output>
After completion, create `.planning/phases/05-trade-simulation-engine/05-03-SUMMARY.md`
</output>
