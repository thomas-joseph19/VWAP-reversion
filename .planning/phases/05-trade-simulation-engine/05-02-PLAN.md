---
phase: 05-trade-simulation-engine
plan: 02
type: tdd
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/vwap_revert/simulation.py
  - tests/indicators/test_trade_simulation.py
autonomous: true
requirements:
  - TSIM-02
  - TSIM-07
  - TSIM-08
must_haves:
  truths:
    - "Active trades scan the enriched Phase 4 row stream causally from the entry timestamp onward and exit only on the first qualifying target, stop, or session-end condition."
    - "VWAP target checks and MAE stop checks use executable exit-side quotes, not midpoint or last-trade prices."
    - "If a stop and target are both true on the same 1-second row, the simulator exits via the conservative stop-loss outcome."
  artifacts:
    - path: "src/vwap_revert/simulation.py"
      provides: "Single-trade replay logic, batch replay result contract, and exit classification"
      exports:
        - "replay_single_trade"
        - "SimulationBatchResult"
        - "simulate_trades"
    - path: "tests/indicators/test_trade_simulation.py"
      provides: "Deterministic replay coverage for VWAP exits, MAE stop-loss logic, same-row ambiguity, and 16:00 ET session liquidation"
      contains:
        - "test_vwap_target_uses_executable_quote_side"
        - "test_stop_loss_uses_executable_mae_and_adverse_first_ordering"
        - "test_session_end_forces_exit_at_1600_et"
  key_links:
    - from: "src/vwap_revert/indicators/vwap.py"
      to: "src/vwap_revert/simulation.py"
      via: "daily VWAP target is consumed directly from enriched row state"
      pattern: "daily_vwap"
    - from: ".planning/phases/05-trade-simulation-engine/05-RESEARCH.md"
      to: "src/vwap_revert/simulation.py"
      via: "adverse-first tie-break and executable-side exit semantics"
      pattern: "adverse-first|bid_px_00 >= daily_vwap|ask_px_00 <= daily_vwap|16:00:00 ET"
---

<objective>
Add the causal replay loop that manages a single open trade through VWAP target checks, stop-loss enforcement, and 16:00 ET forced liquidation.

Purpose: satisfy `TSIM-02`, `TSIM-07`, and `TSIM-08` on top of the core simulation contract from Plan 01.
Output: tested replay functions that produce complete exit decisions with explicit exit reasons and post-trade economics.
</objective>

<execution_context>
@C:/Users/pranav/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pranav/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-trade-simulation-engine/05-CONTEXT.md
@.planning/phases/05-trade-simulation-engine/05-RESEARCH.md
@.planning/phases/05-trade-simulation-engine/05-VALIDATION.md
@.planning/phases/05-trade-simulation-engine/05-01-PLAN.md
@src/vwap_revert/simulation.py
@src/vwap_revert/indicators/setup_detection.py
@src/vwap_revert/indicators/vwap.py
@tests/indicators/test_trade_simulation.py

<interfaces>
From Plan 01 contract:

```python
def open_trade_from_setup(setup: Mapping[str, object], config: SimulationConfig) -> dict[str, object]: ...
def exit_fill_price(row: Mapping[str, object], direction: str, additional_slippage_points: float = 0.0) -> float: ...
def pnl_dollars(direction: str, entry_price: float, exit_price: float, point_value: float, round_trip_commission: float) -> float: ...
```

Plan 02 must create this shared batch-replay contract for later plans:

```python
@dataclass(frozen=True)
class SimulationBatchResult:
    trade_log: pl.DataFrame
    skipped_setups: pl.DataFrame

def simulate_trades(
    setup_log: pl.DataFrame,
    path_rows: pl.DataFrame,
    config: SimulationConfig,
) -> SimulationBatchResult: ...
```

Replay inputs:

```python
path_rows columns:
"trading_date"
"ts_recv"
"ts_recv_et"
"daily_vwap"
"bid_px_00"
"ask_px_00"
```

Required exit reasons:

```python
"target_vwap"
"stop_loss"
"session_end"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing replay tests for VWAP target exits, stop-loss MAE, and 16:00 ET forced liquidation</name>
  <files>tests/indicators/test_trade_simulation.py</files>
  <read_first>
    - tests/indicators/test_trade_simulation.py
    - src/vwap_revert/simulation.py
    - src/vwap_revert/indicators/vwap.py
    - .planning/phases/05-trade-simulation-engine/05-CONTEXT.md
    - .planning/phases/05-trade-simulation-engine/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: a long trade exits only when `bid_px_00 >= daily_vwap` and a short trade exits only when `ask_px_00 <= daily_vwap`, with exit reason `target_vwap`, per D-04 and D-07.
    - Test 2: a fixed-point stop uses executable adverse movement from the exit side, and when both stop and target are true on the same row the trade exits with reason `stop_loss`, per D-08 and the research recommendation.
    - Test 3: if neither target nor stop occurs before the row at `16:00:00 ET`, the trade exits on that row with reason `session_end`, per D-12.
  </behavior>
  <action>Extend `tests/indicators/test_trade_simulation.py` with a reusable path-row fixture spanning at least one long example day and one short example day. Add exact tests named `test_vwap_target_uses_executable_quote_side`, `test_stop_loss_uses_executable_mae_and_adverse_first_ordering`, and `test_session_end_forces_exit_at_1600_et`. Import the yet-to-be-created `replay_single_trade` helper from `vwap_revert.simulation`. In the target test, create one row where midpoint reaches VWAP but executable exit side does not, and assert no exit occurs until the executable side crosses. In the stop-loss test, use a `stop_loss_points` value of exactly `5.0` and include one row where both `bid_px_00 >= daily_vwap` and the executable adverse move breaches the stop so the exact exit reason must be `stop_loss`. In the session-end test, include rows before `16:00:00`, exactly at `16:00:00`, and after `16:00:00`, then assert the exit timestamp is exactly the `16:00:00` row, not later. Freeze exact exit-reason strings `target_vwap`, `stop_loss`, and `session_end` in assertions.</action>
  <acceptance_criteria>
    - `tests/indicators/test_trade_simulation.py` contains `from vwap_revert.simulation import replay_single_trade`
    - `tests/indicators/test_trade_simulation.py` contains the exact test names `test_vwap_target_uses_executable_quote_side`, `test_stop_loss_uses_executable_mae_and_adverse_first_ordering`, and `test_session_end_forces_exit_at_1600_et`
    - `tests/indicators/test_trade_simulation.py` contains the exact strings `target_vwap`, `stop_loss`, and `session_end`
    - `python -m pytest tests/indicators/test_trade_simulation.py -q` fails before replay logic is implemented
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_trade_simulation.py -q</automated>
  </verify>
  <done>Failing tests define the full exit-policy contract for target, stop, and session-end behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement causal single-trade replay and batch trade simulation over setup events</name>
  <files>src/vwap_revert/simulation.py, tests/indicators/test_trade_simulation.py</files>
  <read_first>
    - src/vwap_revert/simulation.py
    - tests/indicators/test_trade_simulation.py
    - src/vwap_revert/indicators/setup_detection.py
    - .planning/phases/05-trade-simulation-engine/05-CONTEXT.md
    - .planning/phases/05-trade-simulation-engine/05-RESEARCH.md
  </read_first>
  <action>Extend `src/vwap_revert/simulation.py` with `replay_single_trade(trade: Mapping[str, object], path_rows: pl.DataFrame, config: SimulationConfig) -> dict[str, object]`, a frozen dataclass `SimulationBatchResult`, and `simulate_trades(setup_log: pl.DataFrame, path_rows: pl.DataFrame, config: SimulationConfig) -> SimulationBatchResult`. In `replay_single_trade`, sort rows by `trading_date`, `ts_recv`, and scan only rows where `ts_recv >= entry_ts` and `trading_date == trade["trading_date"]`. For long trades, treat executable exit side as `bid_px_00`; target hit when `bid_px_00 >= daily_vwap`; adverse move is `entry_price - bid_px_00`; stop hit when adverse move `>= stop_loss_points`. For short trades, treat executable exit side as `ask_px_00`; target hit when `ask_px_00 <= daily_vwap`; adverse move is `ask_px_00 - entry_price`; stop hit when adverse move `>= stop_loss_points`. If both target and stop are true on the same row, exit via the stop-loss path. On exit, compute `exit_price` with `exit_fill_price`, `gross_points`, `net_dollars`, `exit_reason`, `exit_ts`, and `duration_seconds`; record `mae_points` as the max adverse move seen during replay. If no stop or target triggers, force exit on the first row where `ts_recv_et.time() >= config.session_exit_time`; if no such row exists, raise `ValueError` rather than silently leaving the trade open. In `simulate_trades`, iterate the setup log chronologically, call `open_trade_from_setup`, replay each trade through `replay_single_trade`, and return `SimulationBatchResult(trade_log=..., skipped_setups=...)`. In Plan 02, `trade_log` must be a `pl.DataFrame` sorted by `entry_ts` with exact output columns `trading_date`, `direction`, `signal_ts`, `entry_ts`, `entry_price`, `exit_ts`, `exit_price`, `exit_reason`, `target_price`, `gross_points`, `net_dollars`, `mae_points`, `duration_seconds`, and `setup_sigma_signed`. `skipped_setups` must exist now as an explicit empty-or-populated `pl.DataFrame` with exact columns `trading_date`, `ts_recv`, `setup_direction`, `setup_sigma_signed`, and `skip_reason`, even if Plan 02 only returns it empty until Plan 03 adds `position_active` rows.</action>
  <acceptance_criteria>
    - `src/vwap_revert/simulation.py` contains `def replay_single_trade`
    - `src/vwap_revert/simulation.py` contains `class SimulationBatchResult`
    - `src/vwap_revert/simulation.py` contains `def simulate_trades`
    - `src/vwap_revert/simulation.py` contains the exact strings `trade_log`, `skipped_setups`, `target_vwap`, `stop_loss`, `session_end`, and `mae_points`
    - `src/vwap_revert/simulation.py` contains `ts_recv_et`
    - `python -m pytest tests/indicators/test_trade_simulation.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_trade_simulation.py -q</automated>
  </verify>
  <done>The repo can replay a single trade or a batch of trades causally, with `SimulationBatchResult.trade_log` and `SimulationBatchResult.skipped_setups` established as the shared replay contract for downstream plans.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/indicators/test_trade_simulation.py -q` after the replay logic is implemented. Review the exact exit reason strings and confirm the `16:00:00 ET` cutoff is enforced from `ts_recv_et`, not UTC.
</verification>

<success_criteria>
Phase 5 can replay setup-driven trades causally through the enriched row stream and exit them deterministically on VWAP target, stop-loss, or 16:00 ET session close.
</success_criteria>

<output>
After completion, create `.planning/phases/05-trade-simulation-engine/05-02-SUMMARY.md`
</output>
