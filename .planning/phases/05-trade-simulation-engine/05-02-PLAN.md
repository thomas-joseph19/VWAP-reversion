---
phase: 05-trade-simulation-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/vwap_revert/indicators/trade_simulation.py
  - src/vwap_revert/cli.py
  - tests/indicators/test_trade_simulation_validation.py
autonomous: true
requirements:
  - TSIM-01
  - TSIM-02
  - TSIM-03
  - TSIM-04
  - TSIM-05
  - TSIM-07
  - TSIM-08
must_haves:
  truths:
    - "A single CLI command rebuilds Phase 5 trade-simulation artifacts from the canonical cache."
    - "The written trade log preserves entry, exit, P&L, stop, and structural context needed by Phase 6 analytics."
    - "Skipped overlapping setups are emitted as a separate deterministic artifact instead of being silently discarded."
    - "Validation exports allow manual inspection of trade outcomes for specific sessions."
  artifacts:
    - path: "src/vwap_revert/indicators/trade_simulation.py"
      provides: "Artifact writers and validation-export helpers for Phase 5."
      min_lines: 260
    - path: "src/vwap_revert/cli.py"
      provides: "The `build-phase5-trade-simulation` subcommand and orchestration wiring."
      contains: "build-phase5-trade-simulation"
    - path: "tests/indicators/test_trade_simulation_validation.py"
      provides: "Validation and CLI coverage for Phase 5 outputs."
      min_lines: 170
  key_links:
    - from: "src/vwap_revert/cli.py"
      to: "src/vwap_revert/indicators/trade_simulation.py"
      via: "build_phase5_trade_simulation"
      pattern: "build_phase5_trade_simulation"
    - from: "src/vwap_revert/indicators/trade_simulation.py"
      to: "phase4_setup_log.parquet"
      via: "input artifact load"
      pattern: "phase4_setup_log.parquet"
    - from: "src/vwap_revert/indicators/trade_simulation.py"
      to: "phase5_trade_log.parquet"
      via: "artifact writer"
      pattern: "phase5_trade_log.parquet"
---

<objective>
Wire the tested Phase 5 replay core into deterministic artifacts, validation exports, and a canonical CLI rebuild command.

Purpose: Make Phase 5 reproducible from the canonical cache and produce the trade-level handoff that Phase 6 analytics will consume.
Output: Artifact-writing helpers, CLI wiring, and validation tests for the full Phase 5 build flow.
</objective>

<execution_context>
@C:/Users/pranav/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pranav/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-trade-simulation-engine/05-CONTEXT.md
@.planning/phases/05-trade-simulation-engine/05-RESEARCH.md
@.planning/phases/04-setup-detection/04-02-SUMMARY.md
@.planning/phases/05-trade-simulation-engine/05-01-PLAN.md
@src/vwap_revert/cli.py
@src/vwap_revert/indicators/setup_detection.py
@src/vwap_revert/indicators/trade_simulation.py
@tests/indicators/test_setup_detection_validation.py

<interfaces>
The new Phase 5 CLI should follow the existing pattern:

```python
def build_phase4_setup_detection(
    cache_root: Path,
    output_root: Path,
    validation_dates: list[str],
    proximity_threshold_points: float = 10.0,
    min_sigma: float = 1.7,
    max_sigma: float = 3.0,
) -> Path
```

Phase 5 should expose parallel helpers with these exact public names:

```python
def write_trade_simulation_artifacts(...) -> tuple[Path, Path]
def write_trade_simulation_validation_export(...) -> None
def build_phase5_trade_simulation(...) -> Path
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing validation and CLI coverage for Phase 5 outputs</name>
  <files>tests/indicators/test_trade_simulation_validation.py, src/vwap_revert/cli.py, src/vwap_revert/indicators/trade_simulation.py</files>
  <read_first>
    tests/indicators/test_setup_detection_validation.py
    src/vwap_revert/cli.py
    src/vwap_revert/indicators/setup_detection.py
    src/vwap_revert/indicators/trade_simulation.py
    tests/indicators/test_trade_simulation_validation.py
  </read_first>
  <behavior>
    - Test artifact writer output names `phase5_trade_log.parquet` and `phase5_skipped_setups.parquet`.
    - Test validation export names `validation/trade_simulation_validation_export.csv` and `validation/validation_sessions.json`.
    - Test manifest keys `validation_dates`, `trade_log_artifact`, `skipped_setups_artifact`, `comparison_export`, and `row_count`.
    - Test CLI command name `build-phase5-trade-simulation` and argument names `--cache-root`, `--output-root`, `--validation-date`, `--proximity-threshold-points`, `--min-sigma`, `--max-sigma`, `--stop-loss-points`, `--round-trip-commission`, and `--additional-slippage-points`.
  </behavior>
  <action>Create `tests/indicators/test_trade_simulation_validation.py` by mirroring the Phase 4 validation-test style: build a deterministic canonical cache fixture, run the new CLI through `vwap_revert.cli.main`, and assert that the Phase 5 output directory contains `phase5_trade_log.parquet`, `phase5_skipped_setups.parquet`, `validation/trade_simulation_validation_export.csv`, and `validation/validation_sessions.json`. Assert the validation CSV includes exact columns `trading_date`, `setup_ts`, `entry_ts`, `exit_ts`, `setup_direction`, `entry_price`, `exit_price`, `target_price`, `stop_price`, `exit_reason`, `gross_points`, `net_dollars`, `mae_points`, `duration_seconds`, `setup_sigma_signed`, and `nearest_structural_level`. Make the tests fail first by importing `write_trade_simulation_artifacts`, `write_trade_simulation_validation_export`, and the not-yet-wired CLI branch.</action>
  <acceptance_criteria>
    - `tests/indicators/test_trade_simulation_validation.py` contains the exact string `phase5_trade_log.parquet`.
    - `tests/indicators/test_trade_simulation_validation.py` contains the exact string `phase5_skipped_setups.parquet`.
    - `tests/indicators/test_trade_simulation_validation.py` contains the exact string `build-phase5-trade-simulation`.
    - `tests/indicators/test_trade_simulation_validation.py` contains the exact string `trade_simulation_validation_export.csv`.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_trade_simulation_validation.py -x</automated>
  </verify>
  <done>A failing red-phase validation suite exists for Phase 5 artifact names, manifest schema, comparison-export columns, and CLI wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Phase 5 artifact writers and CLI rebuild wiring</name>
  <files>src/vwap_revert/indicators/trade_simulation.py, src/vwap_revert/cli.py, tests/indicators/test_trade_simulation_validation.py</files>
  <read_first>
    src/vwap_revert/indicators/trade_simulation.py
    src/vwap_revert/cli.py
    src/vwap_revert/indicators/setup_detection.py
    tests/indicators/test_trade_simulation_validation.py
  </read_first>
  <action>Extend `src/vwap_revert/indicators/trade_simulation.py` with exact constants `_TRADE_LOG_ARTIFACT_NAME = "phase5_trade_log.parquet"`, `_SKIPPED_SETUPS_ARTIFACT_NAME = "phase5_skipped_setups.parquet"`, `_VALIDATION_EXPORT_NAME = "trade_simulation_validation_export.csv"`, and `_VALIDATION_MANIFEST_NAME = "validation_sessions.json"`. Implement `write_trade_simulation_artifacts(source_df: pl.DataFrame, output_dir: Path, proximity_threshold_points: float = 10.0, min_sigma: float = 1.7, max_sigma: float = 3.0, config: SimulationConfig = SimulationConfig()) -> tuple[Path, Path]` so it composes upstream phases in order by calling `attach_daily_vwap_bands`, `attach_prior_day_structural_levels`, `write_setup_detection_artifacts`-equivalent setup extraction, then `simulate_trades`, and writes the completed trade log plus skipped-setup log parquet files. Implement `write_trade_simulation_validation_export(trade_log_df: pl.DataFrame, output_dir: Path, validation_dates: list[str]) -> None` to filter `trading_date` to the requested dates, sort by `trading_date` and `entry_ts`, write `validation/trade_simulation_validation_export.csv`, and write a JSON manifest with exact keys `validation_dates`, `trade_log_artifact`, `skipped_setups_artifact`, `comparison_export`, and `row_count`. In `src/vwap_revert/cli.py`, add `_parse_phase5_validation_dates`, `build_phase5_trade_simulation`, a `build-phase5-trade-simulation` subparser, and `main()` dispatch. The CLI must load the canonical cache, call `write_trade_simulation_artifacts`, then call `write_trade_simulation_validation_export` using the trade-log parquet that was just written. Expose exact CLI defaults `--proximity-threshold-points 10.0`, `--min-sigma 1.7`, `--max-sigma 3.0`, `--stop-loss-points 20.0`, `--round-trip-commission 5.0`, and `--additional-slippage-points 0.0`.</action>
  <acceptance_criteria>
    - `src/vwap_revert/indicators/trade_simulation.py` contains the exact string `_TRADE_LOG_ARTIFACT_NAME = "phase5_trade_log.parquet"`.
    - `src/vwap_revert/indicators/trade_simulation.py` contains the exact string `_SKIPPED_SETUPS_ARTIFACT_NAME = "phase5_skipped_setups.parquet"`.
    - `src/vwap_revert/indicators/trade_simulation.py` contains the exact string `def write_trade_simulation_artifacts(`.
    - `src/vwap_revert/indicators/trade_simulation.py` contains the exact string `def write_trade_simulation_validation_export(`.
    - `src/vwap_revert/cli.py` contains the exact string `build-phase5-trade-simulation`.
    - `src/vwap_revert/cli.py` contains the exact strings `--stop-loss-points`, `--round-trip-commission`, and `--additional-slippage-points`.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_trade_simulation_validation.py -x</automated>
  </verify>
  <done>The repo can rebuild Phase 5 artifacts from the canonical cache with one CLI command, write deterministic trade and skipped-setup parquets plus validation outputs, and pass automated validation coverage.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/indicators/test_trade_simulation.py tests/indicators/test_trade_simulation_validation.py -x` and verify that `build-phase5-trade-simulation` emits the expected parquet and validation artifacts with deterministic manifest keys.
</verification>

<success_criteria>
Phase 5 becomes reproducible end-to-end: a canonical-cache CLI rebuild now emits the trade log that Phase 6 will analyze, alongside skipped-setup diagnostics and targeted manual-review exports.
</success_criteria>

<output>
After completion, create `.planning/phases/05-trade-simulation-engine/05-02-SUMMARY.md`
</output>
