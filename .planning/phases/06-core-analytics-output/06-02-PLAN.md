---
phase: 06-core-analytics-output
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/vwap_revert/analytics.py
  - src/vwap_revert/cli.py
  - tests/indicators/test_core_analytics_validation.py
autonomous: true
requirements:
  - ANLY-03
  - ANLY-01
  - ANLY-02
must_haves:
  truths:
    - "A dedicated Phase 6 CLI command consumes a Phase 5 output directory and writes a self-contained analytics output directory without mutating upstream artifacts."
    - "Phase 6 exports a deterministic CSV trade log and a deterministic JSON metrics artifact from the same Phase 5 trade-log snapshot."
    - "Phase 6 writes validation artifacts keyed by selected trading dates so CSV and JSON outputs remain auditable and numerically aligned."
  artifacts:
    - path: "src/vwap_revert/analytics.py"
      provides: "Artifact writer that emits Phase 6 CSV, JSON, validation export, and manifest metadata"
      exports:
        - "write_core_analytics_artifacts"
    - path: "src/vwap_revert/cli.py"
      provides: "Phase 6 build subcommand"
      contains:
        - "build-phase6-core-analytics"
    - path: "tests/indicators/test_core_analytics_validation.py"
      provides: "CLI and artifact validation coverage for Phase 6 outputs"
      contains:
        - "test_cli_writes_phase6_analytics_artifacts"
  key_links:
    - from: "src/vwap_revert/cli.py"
      to: "src/vwap_revert/analytics.py"
      via: "Phase 6 command loads Phase 5 artifacts and delegates deterministic output writing"
      pattern: "build_phase6_core_analytics|write_core_analytics_artifacts"
    - from: ".planning/phases/06-core-analytics-output/06-VALIDATION.md"
      to: "tests/indicators/test_core_analytics_validation.py"
      via: "validation export and artifact schema coverage for ANLY-03"
      pattern: "06-02-01|06-02-02"
---

<objective>
Finish Phase 6 by writing the deterministic CSV/JSON artifact layer and wiring a dedicated CLI command that rebuilds analytics outputs from Phase 5 trade artifacts.

Purpose: satisfy `ANLY-03` and complete the phase-ready output contract for later analytics decomposition work.
Output: a Phase 6 artifact writer, CLI command, and integration tests that prove the exported files stay aligned.
</objective>

<execution_context>
@C:/Users/pranav/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pranav/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-core-analytics-output/06-CONTEXT.md
@.planning/phases/06-core-analytics-output/06-RESEARCH.md
@.planning/phases/06-core-analytics-output/06-VALIDATION.md
@.planning/phases/06-core-analytics-output/06-01-PLAN.md
@src/vwap_revert/analytics.py
@src/vwap_revert/cli.py
@src/vwap_revert/simulation.py
@tests/indicators/test_trade_simulation_validation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add integration tests for deterministic Phase 6 CSV, JSON, and validation artifacts</name>
  <files>tests/indicators/test_core_analytics_validation.py</files>
  <read_first>
    - tests/indicators/test_trade_simulation_validation.py
    - src/vwap_revert/analytics.py
    - src/vwap_revert/cli.py
    - .planning/phases/06-core-analytics-output/06-CONTEXT.md
    - .planning/phases/06-core-analytics-output/06-VALIDATION.md
  </read_first>
  <action>Create `tests/indicators/test_core_analytics_validation.py` with a fixture Phase 5 output directory containing an exact `phase5_trade_log.parquet` file written from a small DataFrame that includes at least two completed trades on the same validation date. Add the exact integration test `test_cli_writes_phase6_analytics_artifacts`. That test must call `main([...])` with the exact subcommand `build-phase6-core-analytics`, `--phase5-root`, `--output-root`, and one or more `--validation-date` values. Assert that exact files `phase6_trade_log.csv`, `phase6_metrics.json`, `validation/core_analytics_validation_export.csv`, and `validation/validation_sessions.json` exist. Load the CSV and JSON outputs and assert the CSV contains exact columns `trading_date`, `entry_ts`, `exit_ts`, `entry_price`, `exit_price`, `pnl_points`, `pnl_ticks`, `net_dollars`, `duration_seconds`, `sigma_at_entry`, `nearest_structural_level`, `regime_label`, and `regime_reason`. Assert the metrics JSON contains exact keys `trade_count`, `wins`, `losses`, `win_rate`, `average_win_dollars`, `average_loss_dollars`, `average_win_loss_ratio`, `profit_factor`, `max_drawdown_dollars`, `sharpe_ratio`, and `total_net_dollars`. Assert the validation manifest contains exact keys `phase5_trade_log_artifact`, `trade_log_csv_artifact`, `metrics_json_artifact`, `validation_export`, `validation_dates`, `row_count`, `tick_size`, and `sharpe_convention`.</action>
  <acceptance_criteria>
    - `tests/indicators/test_core_analytics_validation.py` contains the exact test name `test_cli_writes_phase6_analytics_artifacts`
    - `tests/indicators/test_core_analytics_validation.py` contains `build-phase6-core-analytics`
    - `tests/indicators/test_core_analytics_validation.py` contains the exact strings `phase6_trade_log.csv`, `phase6_metrics.json`, and `core_analytics_validation_export.csv`
    - `python -m pytest tests/indicators/test_core_analytics_validation.py -q` fails before the CLI/artifact layer is implemented
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_core_analytics_validation.py -q</automated>
  </verify>
  <done>The integration suite freezes the exact Phase 6 artifact names, export schema, and metrics/manifest keys.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Phase 6 artifact writing and the dedicated CLI build command</name>
  <files>src/vwap_revert/analytics.py, src/vwap_revert/cli.py, tests/indicators/test_core_analytics_validation.py</files>
  <read_first>
    - src/vwap_revert/analytics.py
    - src/vwap_revert/cli.py
    - tests/indicators/test_core_analytics_validation.py
    - src/vwap_revert/simulation.py
    - .planning/phases/06-core-analytics-output/06-CONTEXT.md
    - .planning/phases/06-core-analytics-output/06-RESEARCH.md
  </read_first>
  <action>Extend `src/vwap_revert/analytics.py` with `write_core_analytics_artifacts(phase5_root: Path, output_root: Path, validation_dates: list[str], tick_size: float = 0.25) -> tuple[Path, Path]`. That function must read `phase5_trade_log.parquet`, call `prepare_trade_log(...)` and `compute_performance_metrics(...)`, write the prepared frame to `phase6_trade_log.csv`, write the metrics dict to `phase6_metrics.json`, and create `validation/core_analytics_validation_export.csv` plus `validation/validation_sessions.json`. The validation export must filter rows by exact `trading_date` membership in `validation_dates`, sort by `trading_date` and `entry_ts`, and include the same prepared columns used in the main CSV. The validation manifest must include exact keys `phase5_trade_log_artifact`, `trade_log_csv_artifact`, `metrics_json_artifact`, `validation_export`, `validation_dates`, `row_count`, `tick_size`, and `sharpe_convention`, where `sharpe_convention` is the exact string `mean(net_dollars) / sample_std(net_dollars) * sqrt(trade_count); null if trade_count < 2 or std == 0`. Modify `src/vwap_revert/cli.py` to import `write_core_analytics_artifacts`, add `_parse_phase6_validation_dates(validation_dates: list[str]) -> list[str]`, add `build_phase6_core_analytics(phase5_root: Path, output_root: Path, validation_dates: list[str]) -> Path`, and wire a `build-phase6-core-analytics` subcommand with required `--phase5-root`, `--output-root`, and one or more `--validation-date` arguments. Keep the command behavior parallel to the existing phase builders and return the `phase6_trade_log.csv` path.</action>
  <acceptance_criteria>
    - `src/vwap_revert/analytics.py` contains `def write_core_analytics_artifacts`
    - `src/vwap_revert/analytics.py` contains `phase6_trade_log.csv`, `phase6_metrics.json`, and `core_analytics_validation_export.csv`
    - `src/vwap_revert/analytics.py` contains the exact string `mean(net_dollars) / sample_std(net_dollars) * sqrt(trade_count); null if trade_count < 2 or std == 0`
    - `src/vwap_revert/cli.py` contains `build-phase6-core-analytics`
    - `src/vwap_revert/cli.py` contains `--phase5-root`, `--output-root`, and `--validation-date`
    - `python -m pytest tests/indicators/test_core_analytics.py tests/indicators/test_core_analytics_validation.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_core_analytics.py tests/indicators/test_core_analytics_validation.py -q</automated>
  </verify>
  <done>The repo can rebuild Phase 6 analytics artifacts from Phase 5 outputs using a dedicated CLI command, with CSV/JSON exports and validation metadata staying numerically aligned.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/indicators/test_core_analytics.py tests/indicators/test_core_analytics_validation.py -q` after CLI wiring. Confirm `trade_count` in `phase6_metrics.json` matches the number of rows in `phase6_trade_log.csv` and the validation export is a filtered subset of that same prepared trade log.
</verification>

<success_criteria>
Phase 6 emits deterministic CSV and JSON outputs from the Phase 5 trade log through a first-class CLI command, with validation artifacts that make the results easy to audit.
</success_criteria>

<output>
After completion, create `.planning/phases/06-core-analytics-output/06-02-SUMMARY.md`
</output>
