---
phase: 06-core-analytics-output
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/vwap_revert/analytics.py
  - tests/indicators/test_core_analytics.py
autonomous: true
requirements:
  - ANLY-01
  - ANLY-02
must_haves:
  truths:
    - "Phase 6 derives its analytics-ready trade log directly from `phase5_trade_log.parquet` without replaying Phase 5."
    - "The exported trade-level analytics surface includes explicit P&L in ticks and dollars while preserving prices, timestamps, sigma-at-entry, structural level, and regime context."
    - "Core performance metrics are deterministic, cost-aware, and explicit about undefined edge cases such as no losses, no wins, too few trades, or zero variance."
  artifacts:
    - path: "src/vwap_revert/analytics.py"
      provides: "Phase 6 trade-log enrichment and deterministic performance-metric computation"
      exports:
        - "prepare_trade_log"
        - "compute_performance_metrics"
    - path: "tests/indicators/test_core_analytics.py"
      provides: "Unit coverage for analytics-ready trade-log fields and metric formulas"
      contains:
        - "test_prepare_trade_log_adds_ticks_and_preserves_phase5_context"
        - "test_compute_performance_metrics_handles_wins_losses_drawdown_and_sharpe_edges"
  key_links:
    - from: "src/vwap_revert/simulation.py"
      to: "src/vwap_revert/analytics.py"
      via: "Phase 5 trade-log schema becomes the sole Phase 6 analytics input"
      pattern: "phase5_trade_log.parquet|gross_points|net_dollars|duration_seconds|setup_sigma_signed|nearest_structural_level|regime_label"
    - from: ".planning/phases/06-core-analytics-output/06-CONTEXT.md"
      to: "src/vwap_revert/analytics.py"
      via: "locked decisions for source-of-truth input contract and deterministic metric definitions"
      pattern: "D-01|D-03|D-04|D-05|D-06"
---

<objective>
Create the Phase 6 analytics core so the repo can transform the Phase 5 trade log into an analytics-ready export schema and compute the roadmap's baseline performance metrics deterministically.

Purpose: satisfy `ANLY-01` and `ANLY-02` before artifact writing or CLI integration.
Output: a tested `analytics.py` module that prepares trade-log rows for export and computes core summary metrics from the same Phase 5 snapshot.
</objective>

<execution_context>
@C:/Users/pranav/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pranav/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-core-analytics-output/06-CONTEXT.md
@.planning/phases/06-core-analytics-output/06-RESEARCH.md
@.planning/phases/06-core-analytics-output/06-VALIDATION.md
@.planning/phases/05-trade-simulation-engine/05-VERIFICATION.md
@src/vwap_revert/simulation.py
@src/vwap_revert/cli.py
@tests/indicators/test_trade_simulation_validation.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing analytics-unit tests for trade-log enrichment and metric formulas</name>
  <files>tests/indicators/test_core_analytics.py</files>
  <read_first>
    - src/vwap_revert/simulation.py
    - tests/indicators/test_trade_simulation_validation.py
    - .planning/phases/06-core-analytics-output/06-CONTEXT.md
    - .planning/phases/06-core-analytics-output/06-RESEARCH.md
    - .planning/phases/06-core-analytics-output/06-VALIDATION.md
  </read_first>
  <action>Create `tests/indicators/test_core_analytics.py` with a fixture DataFrame that mirrors the exact Phase 5 trade-log columns `trading_date`, `direction`, `signal_ts`, `entry_ts`, `entry_price`, `exit_ts`, `exit_price`, `exit_reason`, `target_price`, `gross_points`, `net_dollars`, `mae_points`, `duration_seconds`, `setup_sigma_signed`, `nearest_structural_level`, `nearest_structural_distance`, `regime_label`, and `regime_reason`. Import the yet-to-be-created `prepare_trade_log` and `compute_performance_metrics` symbols from `vwap_revert.analytics`. Add the exact tests `test_prepare_trade_log_adds_ticks_and_preserves_phase5_context` and `test_compute_performance_metrics_handles_wins_losses_drawdown_and_sharpe_edges`. In the trade-log test, assert exact columns `pnl_points`, `pnl_ticks`, `sigma_at_entry`, `entry_price`, `exit_price`, `net_dollars`, `duration_seconds`, `nearest_structural_level`, and `regime_label`, and assert `pnl_ticks == gross_points / 0.25` for one winning trade and one losing trade. In the metrics test, use `net_dollars` values `15.0`, `-25.0`, and `35.0`, then assert exact values `trade_count == 3`, `wins == 2`, `losses == 1`, `total_net_dollars == 25.0`, `average_win_dollars == 25.0`, `average_loss_dollars == -25.0`, `average_win_loss_ratio == 1.0`, `profit_factor == 2.0`, and `max_drawdown_dollars == 25.0`. Add edge-case assertions where a one-trade or zero-variance input returns `sharpe_ratio is None` and a no-loss sample returns `profit_factor is None`.</action>
  <acceptance_criteria>
    - `tests/indicators/test_core_analytics.py` contains `from vwap_revert.analytics import prepare_trade_log, compute_performance_metrics`
    - `tests/indicators/test_core_analytics.py` contains the exact test names `test_prepare_trade_log_adds_ticks_and_preserves_phase5_context` and `test_compute_performance_metrics_handles_wins_losses_drawdown_and_sharpe_edges`
    - `tests/indicators/test_core_analytics.py` contains the exact strings `pnl_ticks`, `sigma_at_entry`, `profit_factor`, `max_drawdown_dollars`, and `sharpe_ratio`
    - `python -m pytest tests/indicators/test_core_analytics.py -q` fails before implementation because `vwap_revert.analytics` or its exported symbols do not yet exist
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_core_analytics.py -q</automated>
  </verify>
  <done>Failing tests define the exact Phase 6 analytics API, required trade-log columns, and deterministic metric formulas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the analytics core for trade-log preparation and deterministic metrics</name>
  <files>src/vwap_revert/analytics.py, tests/indicators/test_core_analytics.py</files>
  <read_first>
    - src/vwap_revert/simulation.py
    - tests/indicators/test_core_analytics.py
    - .planning/phases/06-core-analytics-output/06-CONTEXT.md
    - .planning/phases/06-core-analytics-output/06-RESEARCH.md
  </read_first>
  <action>Create `src/vwap_revert/analytics.py` with module-level constants `PHASE5_TRADE_LOG_ARTIFACT_NAME = "phase5_trade_log.parquet"`, `PHASE6_TRADE_LOG_CSV_NAME = "phase6_trade_log.csv"`, `PHASE6_METRICS_JSON_NAME = "phase6_metrics.json"`, and `DEFAULT_TICK_SIZE = 0.25`. Add `prepare_trade_log(trade_log: pl.DataFrame, tick_size: float = 0.25) -> pl.DataFrame` that sorts by `entry_ts`, preserves one row per trade, and adds exact columns `pnl_points` from `gross_points`, `pnl_ticks` from `gross_points / tick_size`, and `sigma_at_entry` from `setup_sigma_signed` without dropping `entry_price`, `exit_price`, `net_dollars`, `duration_seconds`, `nearest_structural_level`, `nearest_structural_distance`, `regime_label`, or `regime_reason`. Add `compute_performance_metrics(trade_log: pl.DataFrame) -> dict[str, object]` that computes exact keys `trade_count`, `wins`, `losses`, `win_rate`, `average_win_dollars`, `average_loss_dollars`, `average_win_loss_ratio`, `profit_factor`, `max_drawdown_dollars`, `sharpe_ratio`, and `total_net_dollars`. Use `net_dollars` as the aggregate metric basis. Define `average_win_loss_ratio` as `average_win_dollars / abs(average_loss_dollars)` when both samples exist, else `None`. Define `profit_factor` as gross positive `net_dollars` divided by absolute gross negative `net_dollars` when losses exist, else `None`. Define cumulative equity from `net_dollars` sorted by `entry_ts` and compute `max_drawdown_dollars` as the largest peak-to-trough decline. Define `sharpe_ratio` as `mean(net_dollars) / sample_std(net_dollars) * sqrt(trade_count)` using sample standard deviation and return `None` when `trade_count < 2` or `sample_std == 0`. Ensure empty inputs return `trade_count == 0`, `wins == 0`, `losses == 0`, `total_net_dollars == 0.0`, `max_drawdown_dollars == 0.0`, and ratio-style metrics `None`.</action>
  <acceptance_criteria>
    - `src/vwap_revert/analytics.py` contains `def prepare_trade_log`
    - `src/vwap_revert/analytics.py` contains `def compute_performance_metrics`
    - `src/vwap_revert/analytics.py` contains the exact strings `pnl_ticks`, `sigma_at_entry`, `average_win_loss_ratio`, `profit_factor`, `max_drawdown_dollars`, and `sharpe_ratio`
    - `src/vwap_revert/analytics.py` contains `phase5_trade_log.parquet`, `phase6_trade_log.csv`, and `phase6_metrics.json`
    - `python -m pytest tests/indicators/test_core_analytics.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_core_analytics.py -q</automated>
  </verify>
  <done>The repo exposes a tested analytics core that prepares a Phase 6-ready trade log and computes the roadmap's baseline metrics deterministically from Phase 5 outputs.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/indicators/test_core_analytics.py -q` after `analytics.py` exists. Confirm the analytics-ready frame preserves Phase 5 context columns and the metrics dict uses explicit `None` behavior for undefined ratios.
</verification>

<success_criteria>
Phase 6 has a reusable analytics core that turns the Phase 5 trade parquet into an analyst-ready trade log and deterministic baseline performance metrics.
</success_criteria>

<output>
After completion, create `.planning/phases/06-core-analytics-output/06-01-SUMMARY.md`
</output>
