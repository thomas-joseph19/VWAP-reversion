---
phase: 08-regime-&-multi-timeframe-vwap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vwap_revert/indicators/regime.py
  - tests/indicators/test_regime.py
autonomous: true
requirements:
  - REGM-01
  - REGM-02
must_haves:
  truths:
    - "Each trading_date receives one causal session-level regime summary row built from completed-session data only."
    - "Sessions without enough prior history stay explicitly unlabeled instead of silently defaulting to a regime."
    - "Once history is sufficient, each active session is labeled `short_gamma` or `long_gamma` with auditable threshold metadata."
  artifacts:
    - path: "src/vwap_revert/indicators/regime.py"
      provides: "Phase 8 session-regime contract and causal realized-volatility classification helpers"
      exports:
        - "SessionRegimeConfig"
        - "build_session_regimes"
    - path: "tests/indicators/test_regime.py"
      provides: "Unit coverage for causal realized-volatility summaries and binary regime classification"
      contains:
        - "test_build_session_regimes_marks_insufficient_history_before_threshold_window"
        - "test_build_session_regimes_labels_short_and_long_gamma_from_prior_completed_sessions"
  key_links:
    - from: "tests/indicators/test_regime.py"
      to: "src/vwap_revert/indicators/regime.py"
      via: "Fixtures define the exact realized-volatility columns, lookback semantics, and insufficient-history behavior"
      pattern: "build_session_regimes|SessionRegimeConfig|insufficient_history|short_gamma|long_gamma"
    - from: ".planning/phases/08-regime-&-multi-timeframe-vwap/08-CONTEXT.md"
      to: "src/vwap_revert/indicators/regime.py"
      via: "Locked decisions D-01 through D-04 require causal prior-session classification with explicit audit metadata"
      pattern: "D-01|D-02|D-03|D-04"
---

<objective>
Define the reusable Phase 8 session-regime layer that converts completed-session volatility observations into causal `short_gamma` / `long_gamma` labels with explicit audit columns.

Purpose: satisfy `REGM-01` and `REGM-02` before any row-level weekly/monthly VWAP enrichment depends on those labels.
Output: a tested `src/vwap_revert/indicators/regime.py` module that builds one session summary row per `trading_date`.
</objective>

<execution_context>
@C:/Users/pranav/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pranav/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-regime-&-multi-timeframe-vwap/08-CONTEXT.md
@.planning/phases/08-regime-&-multi-timeframe-vwap/08-RESEARCH.md
@.planning/phases/02-daily-vwap-engine/02-02-SUMMARY.md
@.planning/phases/07-volume-profile-engine/07-04-SUMMARY.md
@src/vwap_revert/cli.py
@src/vwap_revert/indicators/vwap.py
@src/vwap_revert/indicators/setup_detection.py
@src/vwap_revert/simulation.py
@src/vwap_revert/analytics.py

<interfaces>
From `src/vwap_revert/indicators/setup_detection.py`:
```python
setup_log = extract_setup_events(enriched).with_columns(
    pl.lit(None, dtype=pl.Utf8).alias("regime_label"),
    pl.lit(None, dtype=pl.Utf8).alias("regime_reason"),
)
```

From `src/vwap_revert/simulation.py`:
```python
"regime_label": None if setup.get("regime_label") is None else str(setup.get("regime_label")),
"regime_reason": None if setup.get("regime_reason") is None else str(setup.get("regime_reason")),
```

New contract to create in `src/vwap_revert/indicators/regime.py`:
```python
@dataclass(frozen=True)
class SessionRegimeConfig:
    lookback_sessions: int = 20
    min_history_sessions: int = 20
    threshold_quantile: float = 0.5

def build_session_regimes(
    df: pl.DataFrame,
    config: SessionRegimeConfig = SessionRegimeConfig(),
) -> pl.DataFrame: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the Phase 8 session-regime contract and RED tests per D-01 through D-04</name>
  <files>src/vwap_revert/indicators/regime.py, tests/indicators/test_regime.py</files>
  <read_first>
    - .planning/phases/08-regime-&-multi-timeframe-vwap/08-CONTEXT.md
    - .planning/phases/08-regime-&-multi-timeframe-vwap/08-RESEARCH.md
    - src/vwap_revert/indicators/vwap.py
    - src/vwap_revert/indicators/setup_detection.py
    - src/vwap_revert/simulation.py
    - tests/indicators/test_vwap.py
  </read_first>
  <action>Create `src/vwap_revert/indicators/regime.py` with a minimal exported contract: `SessionRegimeConfig(lookback_sessions=20, min_history_sessions=20, threshold_quantile=0.5)` plus stub signatures for `build_session_regimes(...)` and any private helpers needed for session-close extraction. In `tests/indicators/test_regime.py`, add the exact test names `test_build_session_regimes_marks_insufficient_history_before_threshold_window` and `test_build_session_regimes_labels_short_and_long_gamma_from_prior_completed_sessions`. Build a deterministic fixture with at least 24 ordered `trading_date` rows, RTH trade rows only, and explicit `front_symbol`, `ts_recv`, `is_rth`, `side`, `price`, and `size` columns. The RED assertions must require output columns `trading_date`, `session_close_price`, `session_log_return`, `realized_volatility`, `regime_threshold_value`, `regime_label`, `regime_reason`, `history_session_count`, and `regime_quality_status`; they must also require `regime_quality_status == "insufficient_history"` and null `regime_label` for the first 20 active sessions, then require later rows to emit both `short_gamma` and `long_gamma` labels from prior completed-session statistics. Use D-02 explicitly: the fixture should prove the active session label never depends on same-day closing data.</action>
  <acceptance_criteria>
    - `src/vwap_revert/indicators/regime.py` contains the exact strings `class SessionRegimeConfig`, `lookback_sessions: int = 20`, `min_history_sessions: int = 20`, and `def build_session_regimes`
    - `tests/indicators/test_regime.py` contains the exact test names `test_build_session_regimes_marks_insufficient_history_before_threshold_window` and `test_build_session_regimes_labels_short_and_long_gamma_from_prior_completed_sessions`
    - `tests/indicators/test_regime.py` contains the exact strings `regime_threshold_value`, `history_session_count`, `regime_quality_status`, `short_gamma`, and `long_gamma`
    - `python -m pytest tests/indicators/test_regime.py -q` fails before implementation because `build_session_regimes(...)` is only a stub and the required columns are not yet produced
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_regime.py -q</automated>
  </verify>
  <done>The Phase 8 session-regime contract and its expected causal behavior are pinned down in executable tests before real implementation starts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement causal completed-session realized-volatility summaries and binary regime labels</name>
  <files>src/vwap_revert/indicators/regime.py, tests/indicators/test_regime.py</files>
  <read_first>
    - src/vwap_revert/indicators/regime.py
    - tests/indicators/test_regime.py
    - .planning/phases/08-regime-&-multi-timeframe-vwap/08-CONTEXT.md
    - .planning/phases/08-regime-&-multi-timeframe-vwap/08-RESEARCH.md
    - src/vwap_revert/data_pipeline/sessions.py
    - src/vwap_revert/indicators/vwap.py
  </read_first>
  <action>Implement `build_session_regimes(...)` in `src/vwap_revert/indicators/regime.py` as the Phase 8 source of truth for regime metadata. Reuse D-05 session semantics by filtering to RTH trade rows where `is_rth == True` and `side in {"A", "B"}`, then build one summary row per `trading_date` sorted by the existing canonical order. For each completed session, derive `session_close_price` from the last eligible RTH trade, compute `session_log_return = ln(session_close_price / prior_session_close_price)`, and compute `realized_volatility` for the active session as the rolling sample standard deviation of prior completed-session `session_log_return` values over `config.lookback_sessions`, shifted so session `T` uses only sessions `< T` per D-01 and D-02. Compute `regime_threshold_value` as the rolling median of those same prior realized-volatility observations over the same 20-session window, emit `history_session_count`, and set `regime_quality_status` to exactly `insufficient_history`, `threshold_unavailable`, or `ok`. When both a realized-vol value and threshold exist, label `short_gamma` if `realized_volatility >= regime_threshold_value`, otherwise `long_gamma`, and set `regime_reason` to exact strings `rv_above_threshold` or `rv_below_threshold` per D-03. Do not fabricate labels when the history window is incomplete; leave `regime_label` and `regime_reason` null and keep the explicit quality state per D-04.</action>
  <acceptance_criteria>
    - `src/vwap_revert/indicators/regime.py` contains the exact strings `session_close_price`, `session_log_return`, `realized_volatility`, `regime_threshold_value`, `history_session_count`, `regime_quality_status`, `rv_above_threshold`, and `rv_below_threshold`
    - `src/vwap_revert/indicators/regime.py` contains both exact strings `short_gamma` and `long_gamma`
    - `src/vwap_revert/indicators/regime.py` contains a logarithmic-return expression equivalent to `pl.col("session_close_price") / pl.col("prior_session_close_price")`
    - `python -m pytest tests/indicators/test_regime.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_regime.py -q</automated>
  </verify>
  <done>`src/vwap_revert/indicators/regime.py` now produces a deterministic, causal session-level regime table that later Phase 8 plans can join onto row-level artifacts without schema churn.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/indicators/test_regime.py -q`. Confirm the first sufficiently historical session is the first row allowed to emit a regime label, and confirm both `short_gamma` and `long_gamma` appear in the deterministic fixture output.
</verification>

<success_criteria>
Phase 08 has a reusable session-regime source of truth: one row per trading date, causal realized-volatility metadata, explicit insufficient-history states, and stable downstream `regime_label` / `regime_reason` values.
</success_criteria>

<output>
After completion, create `.planning/phases/08-regime-&-multi-timeframe-vwap/08-01-SUMMARY.md`
</output>
