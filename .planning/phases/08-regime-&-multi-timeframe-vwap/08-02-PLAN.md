---
phase: 08-regime-&-multi-timeframe-vwap
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/vwap_revert/indicators/regime.py
  - src/vwap_revert/indicators/__init__.py
  - tests/indicators/test_regime.py
autonomous: true
requirements:
  - VWAP-04
  - VWAP-05
  - REGM-03
must_haves:
  truths:
    - "Weekly VWAP resets at the first eligible RTH trade of each ET trading week and remains causal on every row."
    - "Monthly VWAP resets at the first eligible RTH trade of each ET calendar month and remains causal on every row."
    - "Weekly and monthly anchors stay comparable across quarterly contract rolls by bridging prior-session prices onto the active contract axis before cumulative VWAP math."
    - "Row-level Phase 8 enrichment appends `weekly_vwap`, `monthly_vwap`, `regime_label`, and `regime_reason` without breaking the existing downstream schema."
    - "Phase 8 applies explicit regime-adjusted sigma-expectation columns on the enriched output so `REGM-03` is implemented as concrete behavior, not metadata only."
  artifacts:
    - path: "src/vwap_revert/indicators/regime.py"
      provides: "Phase 8 row-level weekly/monthly VWAP enrichment and roll-bridge helpers"
      exports:
        - "attach_regime_and_multi_timeframe_vwap"
        - "build_roll_bridge_table"
    - path: "tests/indicators/test_regime.py"
      provides: "Unit coverage for weekly/monthly anchor resets, causal null-before-anchor behavior, roll bridging, downstream-compatible row-level fields, and regime-adjusted sigma expectations"
      contains:
        - "test_attach_regime_and_multi_timeframe_vwap_resets_weekly_and_monthly_anchors"
        - "test_attach_regime_and_multi_timeframe_vwap_bridges_roll_windows_onto_active_contract_axis"
  key_links:
    - from: "src/vwap_revert/indicators/vwap.py"
      to: "src/vwap_revert/indicators/regime.py"
      via: "Phase 8 extends the exact Phase 2 cumulative trade-only, null-before-anchor, forward-fill VWAP pattern to weekly and monthly anchors"
      pattern: "cum_sum|daily_vwap|daily_sigma|forward_fill"
    - from: "src/vwap_revert/indicators/volume_profile.py"
      to: "src/vwap_revert/indicators/regime.py"
      via: "Phase 8 reuses the Phase 7 deterministic roll-bridge precedent for higher-timeframe continuity across symbol changes"
      pattern: "roll_adjustment_applied|roll_anchor_symbol|front_symbol"
    - from: ".planning/phases/08-regime-&-multi-timeframe-vwap/08-CONTEXT.md"
      to: "src/vwap_revert/indicators/regime.py"
      via: "Locked decisions D-05 through D-11 define the anchor reset semantics, roll handling, and downstream schema contract"
      pattern: "D-05|D-06|D-07|D-08|D-09|D-10|D-11"
---

<objective>
Build the reusable Phase 8 row-level enrichment core that combines session regimes with causal weekly and monthly VWAP anchors on the canonical timeseries.

Purpose: satisfy `VWAP-04`, `VWAP-05`, and the row-level application portion of `REGM-03` before artifact/CLI wiring.
Output: a tested enrichment helper that appends weekly/monthly VWAP columns, stable regime fields, and explicit regime-adjusted sigma expectation columns onto the canonical row stream.
</objective>

<execution_context>
@C:/Users/pranav/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pranav/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-regime-&-multi-timeframe-vwap/08-CONTEXT.md
@.planning/phases/08-regime-&-multi-timeframe-vwap/08-RESEARCH.md
@.planning/phases/08-regime-&-multi-timeframe-vwap/08-01-PLAN.md
@src/vwap_revert/indicators/regime.py
@src/vwap_revert/indicators/vwap.py
@src/vwap_revert/indicators/volume_profile.py
@src/vwap_revert/indicators/setup_detection.py
@src/vwap_revert/indicators/__init__.py
@tests/indicators/test_regime.py

<interfaces>
From `src/vwap_revert/indicators/regime.py` after 08-01:
```python
@dataclass(frozen=True)
class SessionRegimeConfig:
    lookback_sessions: int = 20
    min_history_sessions: int = 20
    threshold_quantile: float = 0.5

def build_session_regimes(
    df: pl.DataFrame,
    config: SessionRegimeConfig = SessionRegimeConfig(),
) -> pl.DataFrame: ...
```

New contract to create:
```python
def build_roll_bridge_table(df: pl.DataFrame) -> pl.DataFrame: ...

def attach_regime_and_multi_timeframe_vwap(
    df: pl.DataFrame,
    config: SessionRegimeConfig = SessionRegimeConfig(),
) -> pl.DataFrame: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED tests for weekly/monthly anchor resets, null-before-anchor behavior, and roll-bridged higher-timeframe VWAP</name>
  <files>tests/indicators/test_regime.py</files>
  <read_first>
    - tests/indicators/test_regime.py
    - src/vwap_revert/indicators/regime.py
    - src/vwap_revert/indicators/vwap.py
    - src/vwap_revert/indicators/volume_profile.py
    - src/vwap_revert/indicators/setup_detection.py
    - .planning/phases/08-regime-&-multi-timeframe-vwap/08-CONTEXT.md
    - .planning/phases/08-regime-&-multi-timeframe-vwap/08-RESEARCH.md
  </read_first>
  <action>Extend `tests/indicators/test_regime.py` with the exact tests `test_attach_regime_and_multi_timeframe_vwap_resets_weekly_and_monthly_anchors` and `test_attach_regime_and_multi_timeframe_vwap_bridges_roll_windows_onto_active_contract_axis`. Build a canonical-row fixture that spans at least two ET trading weeks, two calendar months, and one quarterly contract-roll boundary, using exact columns `trading_date`, `ts_recv`, `ts_recv_et`, `is_rth`, `side`, `price`, `size`, `front_symbol`, `bid_px_00`, `ask_px_00`, `daily_vwap`, and `daily_sigma`. In the reset test, assert the enriched output contains exact columns `weekly_anchor_id`, `monthly_anchor_id`, `weekly_vwap`, `monthly_vwap`, `weekly_cum_notional`, `weekly_cum_volume`, `monthly_cum_notional`, `monthly_cum_volume`, `regime_label`, `regime_reason`, `regime_min_sigma`, `regime_max_sigma`, and `regime_extension_passes`, and assert the first pre-trade row inside a new week or month keeps `weekly_vwap` and `monthly_vwap` null until the first eligible RTH trade in that anchor. In the same test, require exact regime-adjusted expectation behavior: rows labeled `short_gamma` must receive `regime_min_sigma == 1.7` and `regime_max_sigma == 3.2`, rows labeled `long_gamma` must receive `regime_min_sigma == 1.5` and `regime_max_sigma == 2.4`, and `regime_extension_passes` must evaluate exact boolean outcomes from `(price - daily_vwap) / daily_sigma` using those label-specific bounds. In the roll-bridge test, require exact audit columns `weekly_roll_adjustment_applied`, `monthly_roll_adjustment_applied`, `weekly_roll_anchor_symbol`, and `monthly_roll_anchor_symbol`, and assert a mixed-roll week retains non-null `weekly_vwap` and `monthly_vwap` on the target contract axis instead of resetting to raw-price-discontinuous values. Also assert the enriched output preserves exact downstream placeholder fields `regime_label` and `regime_reason` so later setup/simulation work can consume the Phase 8 surface without schema changes.</action>
  <acceptance_criteria>
    - `tests/indicators/test_regime.py` contains the exact test names `test_attach_regime_and_multi_timeframe_vwap_resets_weekly_and_monthly_anchors` and `test_attach_regime_and_multi_timeframe_vwap_bridges_roll_windows_onto_active_contract_axis`
    - `tests/indicators/test_regime.py` contains the exact strings `weekly_anchor_id`, `monthly_anchor_id`, `weekly_roll_adjustment_applied`, `monthly_roll_adjustment_applied`, `weekly_roll_anchor_symbol`, and `monthly_roll_anchor_symbol`
    - `tests/indicators/test_regime.py` contains the exact strings `weekly_vwap`, `monthly_vwap`, `weekly_cum_notional`, `monthly_cum_notional`, `regime_label`, `regime_reason`, `regime_min_sigma`, `regime_max_sigma`, and `regime_extension_passes`
    - `python -m pytest tests/indicators/test_regime.py -q` fails before implementation because the row-level enrichment and roll-bridge helpers do not yet exist
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_regime.py -q</automated>
  </verify>
  <done>Phase 8 now has executable RED tests that lock the weekly/monthly anchor semantics, null-before-anchor rules, and cross-roll continuity expectations before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement roll-bridged weekly/monthly VWAP enrichment and export the Phase 8 indicator module</name>
  <files>src/vwap_revert/indicators/regime.py, src/vwap_revert/indicators/__init__.py, tests/indicators/test_regime.py</files>
  <read_first>
    - src/vwap_revert/indicators/regime.py
    - src/vwap_revert/indicators/__init__.py
    - tests/indicators/test_regime.py
    - src/vwap_revert/indicators/vwap.py
    - src/vwap_revert/indicators/volume_profile.py
    - src/vwap_revert/indicators/setup_detection.py
    - .planning/phases/08-regime-&-multi-timeframe-vwap/08-CONTEXT.md
    - .planning/phases/08-regime-&-multi-timeframe-vwap/08-RESEARCH.md
  </read_first>
  <action>Extend `src/vwap_revert/indicators/regime.py` with `build_roll_bridge_table(df: pl.DataFrame) -> pl.DataFrame` and `attach_regime_and_multi_timeframe_vwap(df: pl.DataFrame, config: SessionRegimeConfig = SessionRegimeConfig()) -> pl.DataFrame`. Reuse the Phase 2 trade-only input policy exactly: filter to rows where `is_rth == True` and `side in {"A", "B"}`, compute `adjusted_price` from `price + cumulative_roll_shift`, and build cumulative notional/volume over two exact anchor groups: `weekly_anchor_id` derived from ET `trading_date` ISO year plus ISO week, and `monthly_anchor_id` derived from the ET month start of `trading_date`. Add exact columns `weekly_anchor_id`, `monthly_anchor_id`, `weekly_cum_notional`, `weekly_cum_volume`, `monthly_cum_notional`, `monthly_cum_volume`, `weekly_vwap`, `monthly_vwap`, `weekly_roll_adjustment_applied`, `monthly_roll_adjustment_applied`, `weekly_roll_anchor_symbol`, `monthly_roll_anchor_symbol`, `regime_label`, `regime_reason`, `regime_quality_status`, `regime_threshold_value`, `history_session_count`, `regime_min_sigma`, `regime_max_sigma`, and `regime_extension_passes`. Preserve strict causality by computing the anchor VWAPs only on eligible trade rows and then forward-filling them over their exact anchor IDs rather than globally. Implement cross-roll continuity by summing deterministic completed-session roll deltas between the source session symbol and the active anchor symbol before cumulative VWAP math, mirroring the Phase 7 bridge pattern instead of hard-resetting at rolls. Join the `build_session_regimes(...)` output back onto the full canonical frame by exact `trading_date`, keep null `regime_label` / `regime_reason` when `regime_quality_status != "ok"`, and populate exact regime-adjusted expectation defaults so `REGM-03` is actually applied on the Phase 8 enriched surface: rows with `regime_label == "short_gamma"` receive `regime_min_sigma = 1.7` and `regime_max_sigma = 3.2`; rows with `regime_label == "long_gamma"` receive `regime_min_sigma = 1.5` and `regime_max_sigma = 2.4`; rows without an `ok` regime state keep both columns null. Compute `regime_extension_passes` as a boolean only when exact columns `daily_vwap`, `daily_sigma`, `regime_min_sigma`, and `regime_max_sigma` are all non-null and `daily_sigma != 0`, using the absolute signed sigma distance `(price - daily_vwap) / daily_sigma` against the label-specific min/max bounds. Preserve downstream compatibility by keeping the existing `regime_label` / `regime_reason` column names used by `src/vwap_revert/indicators/setup_detection.py` and update `src/vwap_revert/indicators/__init__.py` so the new `regime` module is imported and exported in `__all__`.</action>
  <acceptance_criteria>
    - `src/vwap_revert/indicators/regime.py` contains `def build_roll_bridge_table` and `def attach_regime_and_multi_timeframe_vwap`
    - `src/vwap_revert/indicators/regime.py` contains the exact strings `weekly_anchor_id`, `monthly_anchor_id`, `weekly_vwap`, `monthly_vwap`, `weekly_roll_adjustment_applied`, `monthly_roll_adjustment_applied`, `weekly_roll_anchor_symbol`, `monthly_roll_anchor_symbol`, `regime_min_sigma`, `regime_max_sigma`, and `regime_extension_passes`
    - `src/vwap_revert/indicators/regime.py` contains exact strings or expressions equivalent to `dt.iso_year`, `dt.week`, and `dt.month_start`
    - `src/vwap_revert/indicators/__init__.py` contains `regime`
    - `python -m pytest tests/indicators/test_regime.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_regime.py -q</automated>
  </verify>
  <done>The repo now has a tested Phase 8 enrichment core that appends causal weekly/monthly VWAP anchors and stable regime metadata onto canonical rows without breaking downstream contracts.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/indicators/test_regime.py -q`. Confirm the first eligible row of each new week and month establishes the anchor, pre-trade rows remain null inside the new anchor, and mixed-roll windows retain non-null `weekly_vwap` and `monthly_vwap` with explicit roll audit columns.
</verification>

<success_criteria>
Phase 08 exposes a reusable row-level enrichment core for weekly/monthly VWAP and causal regime metadata, with deterministic behavior across week/month resets and quarterly contract rolls.
</success_criteria>

<output>
After completion, create `.planning/phases/08-regime-&-multi-timeframe-vwap/08-02-SUMMARY.md`
</output>
