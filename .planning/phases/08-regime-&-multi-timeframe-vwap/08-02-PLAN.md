---
phase: 08-regime-&-multi-timeframe-vwap
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/vwap_revert/indicators/regime.py
  - tests/indicators/test_regime.py
autonomous: true
requirements:
  - VWAP-04
  - VWAP-05
must_haves:
  truths:
    - "Row-level Phase 8 enrichment exposes weekly and monthly VWAP values that remain causal within each anchor period."
    - "Weekly and monthly anchors reset on ET trading-week and trading-month boundaries while staying comparable across contract rolls."
    - "The row-level enriched output backfills the existing downstream `regime_label` and `regime_reason` placeholders without changing column names."
  artifacts:
    - path: "src/vwap_revert/indicators/regime.py"
      provides: "Roll-bridged weekly/monthly VWAP enrichment joined with session-level regime metadata"
      exports:
        - "attach_regime_and_multi_timeframe_vwap"
    - path: "tests/indicators/test_regime.py"
      provides: "Unit coverage for weekly/monthly anchor resets, null-before-anchor behavior, and mixed-roll continuity"
      contains:
        - "test_attach_regime_and_multi_timeframe_vwap_resets_weekly_and_monthly_anchors"
        - "test_attach_regime_and_multi_timeframe_vwap_bridges_rolls_without_backfilling_pre_anchor_rows"
  key_links:
    - from: "src/vwap_revert/indicators/regime.py"
      to: "src/vwap_revert/indicators/vwap.py"
      via: "Phase 8 must extend the Phase 2 cumulative VWAP pattern instead of creating a second anchor methodology"
      pattern: "cum_sum|forward_fill|daily_vwap"
    - from: "src/vwap_revert/indicators/regime.py"
      to: ".planning/phases/07-volume-profile-engine/07-04-SUMMARY.md"
      via: "D-08 requires Phase 7 style roll bridging before cumulative higher-timeframe aggregation"
      pattern: "roll_adjustment_applied|roll_anchor_symbol|mixed-roll"
---

<objective>
Extend the new Phase 8 regime module into a row-level enrichment surface that adds weekly and monthly VWAP anchors, joins the session regime table, and preserves strict causality across anchor resets and contract rolls.

Purpose: satisfy `VWAP-04` and `VWAP-05` while also backfilling the stable regime columns that later phases already expect.
Output: a tested row-level enrichment function inside `src/vwap_revert/indicators/regime.py`.
</objective>

<execution_context>
@C:/Users/pranav/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pranav/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-regime-&-multi-timeframe-vwap/08-CONTEXT.md
@.planning/phases/08-regime-&-multi-timeframe-vwap/08-RESEARCH.md
@.planning/phases/08-regime-&-multi-timeframe-vwap/08-01-PLAN.md
@.planning/phases/02-daily-vwap-engine/02-02-SUMMARY.md
@.planning/phases/07-volume-profile-engine/07-04-SUMMARY.md
@src/vwap_revert/indicators/regime.py
@src/vwap_revert/indicators/vwap.py
@src/vwap_revert/data_pipeline/sessions.py
@src/vwap_revert/indicators/setup_detection.py

<interfaces>
From `src/vwap_revert/indicators/vwap.py`:
```python
def attach_daily_vwap_bands(df: pl.DataFrame) -> pl.DataFrame: ...
```

From `src/vwap_revert/indicators/regime.py` after Plan 01:
```python
def build_session_regimes(
    df: pl.DataFrame,
    config: SessionRegimeConfig = SessionRegimeConfig(),
) -> pl.DataFrame: ...
```

New interface to add in this plan:
```python
def attach_regime_and_multi_timeframe_vwap(
    df: pl.DataFrame,
    config: SessionRegimeConfig = SessionRegimeConfig(),
) -> pl.DataFrame: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED coverage for weekly/monthly anchor resets, roll bridging, and stable regime join columns</name>
  <files>tests/indicators/test_regime.py</files>
  <read_first>
    - tests/indicators/test_regime.py
    - src/vwap_revert/indicators/regime.py
    - src/vwap_revert/indicators/vwap.py
    - .planning/phases/08-regime-&-multi-timeframe-vwap/08-CONTEXT.md
    - .planning/phases/07-volume-profile-engine/07-04-SUMMARY.md
  </read_first>
  <action>Extend `tests/indicators/test_regime.py` with the exact test names `test_attach_regime_and_multi_timeframe_vwap_resets_weekly_and_monthly_anchors` and `test_attach_regime_and_multi_timeframe_vwap_bridges_rolls_without_backfilling_pre_anchor_rows`. Build one deterministic fixture that spans at least one Monday reset, one first-trading-day-of-month reset, and one quarterly `front_symbol` change within a weekly or monthly anchor. Require output columns `weekly_vwap`, `monthly_vwap`, `weekly_anchor_id`, `monthly_anchor_id`, `weekly_roll_adjustment_applied`, `monthly_roll_adjustment_applied`, `regime_label`, `regime_reason`, `regime_threshold_value`, and `regime_quality_status`. Assert that pre-anchor rows remain null for `weekly_vwap` and `monthly_vwap` until the first eligible RTH trade per D-07, assert the Monday and month-start rows reset cumulative notional and volume, and assert a mixed-roll anchor still yields non-null `weekly_vwap` and `monthly_vwap` after applying bridge shifts rather than resetting at the symbol boundary per D-08.</action>
  <acceptance_criteria>
    - `tests/indicators/test_regime.py` contains the exact test names `test_attach_regime_and_multi_timeframe_vwap_resets_weekly_and_monthly_anchors` and `test_attach_regime_and_multi_timeframe_vwap_bridges_rolls_without_backfilling_pre_anchor_rows`
    - `tests/indicators/test_regime.py` contains the exact strings `weekly_vwap`, `monthly_vwap`, `weekly_anchor_id`, `monthly_anchor_id`, `weekly_roll_adjustment_applied`, and `monthly_roll_adjustment_applied`
    - `tests/indicators/test_regime.py` contains an assertion proving `weekly_vwap` is null before the first eligible anchor trade in a new week and `monthly_vwap` is null before the first eligible anchor trade in a new month
    - `python -m pytest tests/indicators/test_regime.py -q` fails before implementation because the row-level enrichment function and anchor columns do not yet exist
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_regime.py -q</automated>
  </verify>
  <done>The expected weekly/monthly VWAP semantics and roll-bridge behavior are now pinned down in executable tests before the enrichment code changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement row-level Phase 8 enrichment with weekly/monthly VWAP and joined regime metadata</name>
  <files>src/vwap_revert/indicators/regime.py, tests/indicators/test_regime.py</files>
  <read_first>
    - src/vwap_revert/indicators/regime.py
    - tests/indicators/test_regime.py
    - src/vwap_revert/indicators/vwap.py
    - .planning/phases/08-regime-&-multi-timeframe-vwap/08-CONTEXT.md
    - .planning/phases/08-regime-&-multi-timeframe-vwap/08-RESEARCH.md
    - .planning/phases/07-volume-profile-engine/07-04-SUMMARY.md
  </read_first>
  <action>Add `attach_regime_and_multi_timeframe_vwap(df: pl.DataFrame, config: SessionRegimeConfig = SessionRegimeConfig()) -> pl.DataFrame` to `src/vwap_revert/indicators/regime.py`. Reuse D-05 through D-08 exactly: filter to RTH trade rows with `side in {"A", "B"}`, derive `weekly_anchor_id` from `trading_date.dt.iso_year()` plus `trading_date.dt.week()`, derive `monthly_anchor_id` from `trading_date.dt.month_start()`, and compute `weekly_vwap` / `monthly_vwap` with cumulative `price * size` and `size` sums over those anchors using the same null-before-anchor and forward-fill-within-anchor pattern as Phase 2. Before cumulative sums, bridge mixed-roll source trades onto the active contract axis by adding cumulative completed-session roll deltas at each `front_symbol` boundary, where each delta is the exact difference between the first eligible RTH trade price of the new-symbol session and the last eligible RTH trade price of the prior completed session. Emit audit columns `weekly_roll_adjustment_applied`, `monthly_roll_adjustment_applied`, `weekly_roll_anchor_symbol`, and `monthly_roll_anchor_symbol`. Join the Plan 01 session-regime table back by `trading_date`, keep the exact downstream column names `regime_label` and `regime_reason`, and propagate `regime_threshold_value` plus `regime_quality_status` without mutating any existing Phase 2 or Phase 7 artifacts in place.</action>
  <acceptance_criteria>
    - `src/vwap_revert/indicators/regime.py` contains the exact strings `def attach_regime_and_multi_timeframe_vwap`, `weekly_vwap`, `monthly_vwap`, `weekly_anchor_id`, `monthly_anchor_id`, `weekly_roll_adjustment_applied`, and `monthly_roll_adjustment_applied`
    - `src/vwap_revert/indicators/regime.py` contains the exact strings `dt.iso_year`, `dt.week`, and `dt.month_start`
    - `src/vwap_revert/indicators/regime.py` contains exact strings `weekly_roll_anchor_symbol` and `monthly_roll_anchor_symbol`
    - `python -m pytest tests/indicators/test_regime.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_regime.py -q</automated>
  </verify>
  <done>Phase 08 now has a reusable row-level enrichment function that appends causal weekly/monthly VWAP anchors plus stable regime metadata to the canonical stream.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/indicators/test_regime.py -q`. Confirm that Monday and month-start pre-anchor rows remain null until the first eligible RTH trade, and confirm mixed-roll rows still carry non-null `weekly_vwap` and `monthly_vwap` with roll-audit flags set.
</verification>

<success_criteria>
Phase 08 has a row-level enrichment surface ready for artifacts and CLI wiring: causal weekly/monthly VWAP, roll-bridge audit columns, and joined `regime_label` / `regime_reason` fields.
</success_criteria>

<output>
After completion, create `.planning/phases/08-regime-&-multi-timeframe-vwap/08-02-SUMMARY.md`
</output>
