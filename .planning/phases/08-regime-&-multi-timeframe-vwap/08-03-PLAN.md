---
phase: 08-regime-&-multi-timeframe-vwap
plan: 03
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - src/vwap_revert/indicators/regime.py
  - src/vwap_revert/cli.py
  - tests/indicators/test_regime_validation.py
autonomous: true
requirements:
  - REGM-03
must_haves:
  truths:
    - "Phase 8 writes both a compact session-regime artifact and a row-level enriched artifact without mutating earlier phase outputs."
    - "A single CLI command rebuilds Phase 8 outputs from the canonical cache and emits deterministic validation exports for selected sessions."
    - "Validation outputs expose weekly/monthly VWAP values and regime decision metadata so manual spot-checking is reproducible."
  artifacts:
    - path: "src/vwap_revert/indicators/regime.py"
      provides: "Phase 8 artifact writers and validation export helpers"
      exports:
        - "write_phase8_artifacts"
        - "write_phase8_validation_export"
    - path: "src/vwap_revert/cli.py"
      provides: "Phase 8 CLI build command for canonical-cache rebuilds"
      exports:
        - "build_phase8_regime_vwap"
        - "main"
    - path: "tests/indicators/test_regime_validation.py"
      provides: "Integration coverage for artifact names, validation schema, and CLI emission"
      contains:
        - "test_write_phase8_validation_export_contains_regime_and_multi_timeframe_columns"
        - "test_cli_writes_phase8_regime_and_multi_timeframe_artifacts"
  key_links:
    - from: "src/vwap_revert/cli.py"
      to: "src/vwap_revert/indicators/regime.py"
      via: "CLI orchestrates canonical-cache load, Phase 8 enrichment, artifact writing, and validation export emission"
      pattern: "build-phase8-regime-vwap|write_phase8_artifacts|write_phase8_validation_export"
    - from: "tests/indicators/test_regime_validation.py"
      to: "src/vwap_revert/indicators/regime.py"
      via: "Integration tests lock the artifact contract that later phases will consume"
      pattern: "phase8_session_regimes.parquet|phase8_regime_vwap_enriched.parquet|phase8_regime_vwap_validation_export.csv"
---

<objective>
Turn the new Phase 8 enrichment layer into a reproducible build surface with deterministic artifact names, validation exports, and CLI wiring from the canonical cache.

Purpose: satisfy `REGM-03` for downstream compatibility by materializing stable regime metadata and higher-timeframe VWAP context in reusable Phase 8 outputs.
Output: artifact writers, validation helpers, and a `build-phase8-regime-vwap` CLI command with integration tests.
</objective>

<execution_context>
@C:/Users/pranav/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pranav/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-regime-&-multi-timeframe-vwap/08-CONTEXT.md
@.planning/phases/08-regime-&-multi-timeframe-vwap/08-RESEARCH.md
@.planning/phases/08-regime-&-multi-timeframe-vwap/08-01-PLAN.md
@.planning/phases/08-regime-&-multi-timeframe-vwap/08-02-PLAN.md
@src/vwap_revert/cli.py
@src/vwap_revert/indicators/regime.py
@src/vwap_revert/data_pipeline/cache.py
@src/vwap_revert/indicators/vwap.py
@tests/indicators/test_vwap_validation.py
@tests/indicators/test_volume_profile_validation.py

<interfaces>
From `src/vwap_revert/cli.py`:
```python
def build_phase7_volume_profile(
    cache_root: Path,
    output_root: Path,
    validation_dates: list[str],
    proximity_threshold_points: float = 10.0,
    bucket_size: float = 0.25,
    htf_lookback_sessions: int = 180,
) -> Path: ...
```

New artifacts to create in this plan:
```python
PHASE8_SESSION_ARTIFACT_NAME = "phase8_session_regimes.parquet"
PHASE8_ENRICHED_ARTIFACT_NAME = "phase8_regime_vwap_enriched.parquet"
PHASE8_VALIDATION_EXPORT_NAME = "phase8_regime_vwap_validation_export.csv"
PHASE8_VALIDATION_MANIFEST_NAME = "validation_sessions.json"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add integration tests for Phase 8 artifacts, validation schema, and CLI wiring</name>
  <files>tests/indicators/test_regime_validation.py</files>
  <read_first>
    - src/vwap_revert/indicators/regime.py
    - src/vwap_revert/cli.py
    - tests/indicators/test_vwap_validation.py
    - tests/indicators/test_volume_profile_validation.py
    - .planning/phases/08-regime-&-multi-timeframe-vwap/08-CONTEXT.md
  </read_first>
  <action>Create `tests/indicators/test_regime_validation.py` with the exact test names `test_write_phase8_validation_export_contains_regime_and_multi_timeframe_columns` and `test_cli_writes_phase8_regime_and_multi_timeframe_artifacts`. The first test must require artifact names `phase8_session_regimes.parquet`, `phase8_regime_vwap_enriched.parquet`, `validation/phase8_regime_vwap_validation_export.csv`, and `validation/validation_sessions.json`, and must assert the validation CSV contains exact columns `trading_date`, `ts_recv_et`, `weekly_vwap`, `monthly_vwap`, `weekly_anchor_id`, `monthly_anchor_id`, `regime_label`, `regime_reason`, `regime_threshold_value`, `regime_quality_status`, `weekly_roll_adjustment_applied`, and `monthly_roll_adjustment_applied`. The second test must exercise a CLI subcommand named exactly `build-phase8-regime-vwap` with flags `--cache-root`, `--output-root`, one or more `--validation-date YYYY-MM-DD`, `--lookback-sessions`, and `--min-history-sessions`, and it must assert both parquet artifacts plus the validation CSV/manifest are written from a fixture canonical cache. Keep the output surface additive per D-09 through D-12; do not mutate Phase 2 or Phase 7 artifact names.</action>
  <acceptance_criteria>
    - `tests/indicators/test_regime_validation.py` exists and contains the exact test names `test_write_phase8_validation_export_contains_regime_and_multi_timeframe_columns` and `test_cli_writes_phase8_regime_and_multi_timeframe_artifacts`
    - `tests/indicators/test_regime_validation.py` contains the exact strings `phase8_session_regimes.parquet`, `phase8_regime_vwap_enriched.parquet`, `phase8_regime_vwap_validation_export.csv`, `--lookback-sessions`, and `--min-history-sessions`
    - `tests/indicators/test_regime_validation.py` contains the exact strings `weekly_vwap`, `monthly_vwap`, `regime_label`, `regime_reason`, `regime_threshold_value`, and `regime_quality_status`
    - `python -m pytest tests/indicators/test_regime_validation.py -q` fails before implementation because the writers and CLI command do not yet exist
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_regime_validation.py -q</automated>
  </verify>
  <done>Phase 8 artifact names, validation schema, and CLI contract are locked in executable integration tests before the writers are implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Phase 8 artifact writers and the `build-phase8-regime-vwap` CLI command</name>
  <files>src/vwap_revert/indicators/regime.py, src/vwap_revert/cli.py, tests/indicators/test_regime_validation.py</files>
  <read_first>
    - src/vwap_revert/indicators/regime.py
    - src/vwap_revert/cli.py
    - tests/indicators/test_regime_validation.py
    - src/vwap_revert/data_pipeline/cache.py
    - .planning/phases/08-regime-&-multi-timeframe-vwap/08-CONTEXT.md
    - .planning/phases/08-regime-&-multi-timeframe-vwap/08-RESEARCH.md
  </read_first>
  <action>Implement deterministic Phase 8 build surfaces. In `src/vwap_revert/indicators/regime.py`, add constants `PHASE8_SESSION_ARTIFACT_NAME = "phase8_session_regimes.parquet"`, `PHASE8_ENRICHED_ARTIFACT_NAME = "phase8_regime_vwap_enriched.parquet"`, `PHASE8_VALIDATION_EXPORT_NAME = "phase8_regime_vwap_validation_export.csv"`, and `PHASE8_VALIDATION_MANIFEST_NAME = "validation_sessions.json"`. Add `write_phase8_artifacts(df: pl.DataFrame, output_dir: Path, config: SessionRegimeConfig = SessionRegimeConfig()) -> tuple[Path, Path]` that writes the compact session summary parquet plus the row-level enriched parquet, and add `write_phase8_validation_export(enriched_df: pl.DataFrame, session_regimes: pl.DataFrame, output_dir: Path, validation_dates: list[str], config: SessionRegimeConfig = SessionRegimeConfig()) -> None` that writes the deterministic Phase 8 validation CSV and manifest under `validation/`. The validation export must include the exact columns from Task 1 and join session-level threshold metadata onto the selected row subset for manual review. In `src/vwap_revert/cli.py`, add `build_phase8_regime_vwap(cache_root: Path, output_root: Path, validation_dates: list[str], lookback_sessions: int = 20, min_history_sessions: int = 20) -> Path`, wire a new argparse subcommand named `build-phase8-regime-vwap`, validate the `--validation-date` inputs with `date.fromisoformat`, load the canonical cache via `scan_canonical_cache(cache_root).collect()`, and call the new writer helpers. Return the enriched artifact path `output_root / "phase8_regime_vwap_enriched.parquet"`.</action>
  <acceptance_criteria>
    - `src/vwap_revert/indicators/regime.py` contains the exact strings `PHASE8_SESSION_ARTIFACT_NAME`, `PHASE8_ENRICHED_ARTIFACT_NAME`, `PHASE8_VALIDATION_EXPORT_NAME`, `def write_phase8_artifacts`, and `def write_phase8_validation_export`
    - `src/vwap_revert/cli.py` contains the exact strings `build_phase8_regime_vwap`, `build-phase8-regime-vwap`, `--lookback-sessions`, and `--min-history-sessions`
    - `python -m pytest tests/indicators/test_regime.py tests/indicators/test_regime_validation.py -q` passes
    - `python -m pytest tests/indicators/test_vwap.py tests/indicators/test_vwap_validation.py tests/indicators/test_volume_profile.py tests/indicators/test_volume_profile_validation.py tests/indicators/test_setup_detection.py tests/indicators/test_setup_detection_validation.py tests/indicators/test_trade_simulation.py tests/indicators/test_trade_simulation_validation.py tests/indicators/test_core_analytics.py tests/indicators/test_core_analytics_validation.py tests/indicators/test_regime.py tests/indicators/test_regime_validation.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_regime.py tests/indicators/test_regime_validation.py -q</automated>
  </verify>
  <done>Phase 08 can now be rebuilt from the canonical cache through one narrow CLI command, producing stable session, enriched, and validation artifacts for later manual and downstream consumption.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/indicators/test_regime.py tests/indicators/test_regime_validation.py -q`, then run the broader indicators regression command. Smoke the new CLI with at least one `--validation-date` and verify the two parquet artifacts plus validation CSV and manifest exist under the requested output root.
</verification>

<success_criteria>
Phase 08 exposes deterministic artifact and CLI surfaces: a compact session-regime parquet, a row-level enriched parquet with weekly/monthly VWAP and stable regime metadata, and reproducible validation exports for manual spot checks.
</success_criteria>

<output>
After completion, create `.planning/phases/08-regime-&-multi-timeframe-vwap/08-03-SUMMARY.md`
</output>
