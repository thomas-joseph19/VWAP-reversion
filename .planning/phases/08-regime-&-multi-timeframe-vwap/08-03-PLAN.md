---
phase: 08-regime-&-multi-timeframe-vwap
plan: 03
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - src/vwap_revert/indicators/regime.py
  - src/vwap_revert/cli.py
  - tests/indicators/test_regime_validation.py
autonomous: true
requirements:
  - REGM-01
  - REGM-02
  - REGM-03
  - VWAP-04
  - VWAP-05
must_haves:
  truths:
    - "One Phase 8 CLI command rebuilds the session-regime summary, the row-level enriched dataset, and deterministic validation artifacts directly from the canonical cache."
    - "The validation export exposes timestamped weekly/monthly VWAP values plus session-level regime decision metadata for selected dates."
    - "The Phase 8 output directory is self-describing through a manifest that records artifact names, validation dates, regime configuration, and anchor settings."
  artifacts:
    - path: "src/vwap_revert/indicators/regime.py"
      provides: "Phase 8 artifact writers for session-regime outputs, enriched row-level outputs, and validation surfaces"
      exports:
        - "write_regime_and_multi_timeframe_vwap_artifacts"
        - "write_regime_and_multi_timeframe_validation_export"
    - path: "src/vwap_revert/cli.py"
      provides: "Phase 8 CLI rebuild command"
      contains:
        - "build-phase8-regime-vwap"
    - path: "tests/indicators/test_regime_validation.py"
      provides: "CLI and artifact integration coverage for Phase 8 outputs"
      contains:
        - "test_cli_writes_phase8_regime_and_multi_timeframe_vwap_artifacts"
  key_links:
    - from: "src/vwap_revert/cli.py"
      to: "src/vwap_revert/indicators/regime.py"
      via: "The Phase 8 command scans the canonical cache and delegates all artifact writing to the indicator module"
      pattern: "build_phase8_regime_vwap|write_regime_and_multi_timeframe_vwap_artifacts|write_regime_and_multi_timeframe_validation_export"
    - from: ".planning/phases/08-regime-&-multi-timeframe-vwap/08-VALIDATION.md"
      to: "tests/indicators/test_regime_validation.py"
      via: "Validation strategy names the exact CLI, artifact outputs, and selected verification commands for Phase 8"
      pattern: "08-03-01|phase8_session_regimes.parquet|phase8_regime_vwap_enriched.parquet|regime_vwap_validation_export.csv"
    - from: "src/vwap_revert/indicators/setup_detection.py"
      to: "src/vwap_revert/indicators/regime.py"
      via: "Phase 8 must preserve the downstream placeholder column contract by populating `regime_label` and `regime_reason` without renaming them"
      pattern: "regime_label|regime_reason"
---

<objective>
Complete Phase 8 by wiring deterministic artifact emission and a dedicated CLI command that rebuilds the entire regime-and-multi-timeframe-VWAP surface from the canonical cache.

Purpose: finish the executable Phase 8 contract so manual validation and later Phase 9 work can consume a stable output directory.
Output: artifact writers, CLI wiring, and integration tests for the full Phase 8 build path.
</objective>

<execution_context>
@C:/Users/pranav/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pranav/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-regime-&-multi-timeframe-vwap/08-CONTEXT.md
@.planning/phases/08-regime-&-multi-timeframe-vwap/08-RESEARCH.md
@.planning/phases/08-regime-&-multi-timeframe-vwap/08-VALIDATION.md
@.planning/phases/08-regime-&-multi-timeframe-vwap/08-02-PLAN.md
@src/vwap_revert/data_pipeline/cache.py
@src/vwap_revert/indicators/regime.py
@src/vwap_revert/cli.py
@tests/indicators/test_vwap_validation.py
@tests/indicators/test_volume_profile_validation.py

<interfaces>
From `src/vwap_revert/indicators/regime.py` after 08-02:
```python
def build_session_regimes(
    df: pl.DataFrame,
    config: SessionRegimeConfig = SessionRegimeConfig(),
) -> pl.DataFrame: ...

def attach_regime_and_multi_timeframe_vwap(
    df: pl.DataFrame,
    config: SessionRegimeConfig = SessionRegimeConfig(),
) -> pl.DataFrame: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add integration tests for the Phase 8 CLI, parquet artifacts, validation CSV, and manifest metadata</name>
  <files>tests/indicators/test_regime_validation.py</files>
  <read_first>
    - tests/indicators/test_vwap_validation.py
    - tests/indicators/test_volume_profile_validation.py
    - src/vwap_revert/indicators/regime.py
    - src/vwap_revert/cli.py
    - .planning/phases/08-regime-&-multi-timeframe-vwap/08-CONTEXT.md
    - .planning/phases/08-regime-&-multi-timeframe-vwap/08-VALIDATION.md
  </read_first>
  <action>Create `tests/indicators/test_regime_validation.py` with a temporary canonical-cache fixture written via the same parquet partition pattern used in earlier phase validation suites. Add the exact integration test `test_cli_writes_phase8_regime_and_multi_timeframe_vwap_artifacts`. That test must call `main([...])` with the exact subcommand `build-phase8-regime-vwap`, `--cache-root`, `--output-root`, one or more `--validation-date` values, and explicit config flags `--lookback-sessions 20`, `--min-history-sessions 20`, and `--threshold-quantile 0.5`. Assert that exact files `phase8_session_regimes.parquet`, `phase8_regime_vwap_enriched.parquet`, `validation/regime_vwap_validation_export.csv`, and `validation/validation_sessions.json` exist. Load the validation CSV and assert it contains exact columns `trading_date`, `ts_recv_et`, `weekly_vwap`, `monthly_vwap`, `regime_label`, `regime_reason`, `regime_quality_status`, `regime_threshold_value`, `history_session_count`, `weekly_roll_adjustment_applied`, and `monthly_roll_adjustment_applied`. Assert the session-regime parquet contains exact columns `session_close_price`, `session_log_return`, `realized_volatility`, `regime_threshold_value`, `regime_label`, `regime_reason`, `history_session_count`, and `regime_quality_status`. Assert the validation manifest contains exact keys `validation_dates`, `lookback_sessions`, `min_history_sessions`, `threshold_quantile`, `session_regimes_artifact`, `enriched_artifact`, `validation_export`, and `row_count`.</action>
  <acceptance_criteria>
    - `tests/indicators/test_regime_validation.py` contains the exact test name `test_cli_writes_phase8_regime_and_multi_timeframe_vwap_artifacts`
    - `tests/indicators/test_regime_validation.py` contains `build-phase8-regime-vwap`
    - `tests/indicators/test_regime_validation.py` contains the exact strings `phase8_session_regimes.parquet`, `phase8_regime_vwap_enriched.parquet`, `regime_vwap_validation_export.csv`, `weekly_vwap`, `monthly_vwap`, and `regime_quality_status`
    - `python -m pytest tests/indicators/test_regime_validation.py -q` fails before the artifact and CLI layer is implemented
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_regime_validation.py -q</automated>
  </verify>
  <done>The integration suite freezes the Phase 8 artifact names, CLI arguments, validation-export schema, and manifest keys before production wiring is added.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Phase 8 artifact writers and the dedicated CLI rebuild command</name>
  <files>src/vwap_revert/indicators/regime.py, src/vwap_revert/cli.py, tests/indicators/test_regime_validation.py</files>
  <read_first>
    - src/vwap_revert/indicators/regime.py
    - src/vwap_revert/cli.py
    - tests/indicators/test_regime_validation.py
    - src/vwap_revert/data_pipeline/cache.py
    - .planning/phases/08-regime-&-multi-timeframe-vwap/08-CONTEXT.md
    - .planning/phases/08-regime-&-multi-timeframe-vwap/08-RESEARCH.md
    - .planning/phases/08-regime-&-multi-timeframe-vwap/08-VALIDATION.md
  </read_first>
  <action>Extend `src/vwap_revert/indicators/regime.py` with exact artifact-name constants `PHASE8_SESSION_REGIMES_ARTIFACT_NAME = "phase8_session_regimes.parquet"`, `PHASE8_ENRICHED_ARTIFACT_NAME = "phase8_regime_vwap_enriched.parquet"`, `PHASE8_VALIDATION_EXPORT_NAME = "regime_vwap_validation_export.csv"`, and `PHASE8_VALIDATION_MANIFEST_NAME = "validation_sessions.json"`. Add `write_regime_and_multi_timeframe_vwap_artifacts(df: pl.DataFrame, output_dir: Path, config: SessionRegimeConfig = SessionRegimeConfig()) -> tuple[Path, Path]` that writes the session-level regime table and the row-level enriched parquet using the exact filenames above. Add `write_regime_and_multi_timeframe_validation_export(enriched_df: pl.DataFrame, session_regimes: pl.DataFrame, output_dir: Path, validation_dates: list[str], config: SessionRegimeConfig = SessionRegimeConfig()) -> None` that creates `validation/`, filters selected `trading_date` rows, writes `validation/regime_vwap_validation_export.csv` with the exact columns `trading_date`, `ts_recv_et`, `weekly_vwap`, `monthly_vwap`, `regime_label`, `regime_reason`, `regime_quality_status`, `regime_threshold_value`, `history_session_count`, `weekly_roll_adjustment_applied`, and `monthly_roll_adjustment_applied`, and writes `validation/validation_sessions.json` with exact keys `validation_dates`, `lookback_sessions`, `min_history_sessions`, `threshold_quantile`, `session_regimes_artifact`, `enriched_artifact`, `validation_export`, and `row_count`. Modify `src/vwap_revert/cli.py` to import the Phase 8 artifact writers and `SessionRegimeConfig`, add `_parse_phase8_validation_dates(validation_dates: list[str]) -> list[str]`, add `build_phase8_regime_vwap(cache_root: Path, output_root: Path, validation_dates: list[str], lookback_sessions: int = 20, min_history_sessions: int = 20, threshold_quantile: float = 0.5) -> Path`, and wire a `build-phase8-regime-vwap` subcommand with required `--cache-root`, `--output-root`, and one or more `--validation-date` arguments plus optional `--lookback-sessions`, `--min-history-sessions`, and `--threshold-quantile`. The command must read the canonical cache via `scan_canonical_cache(cache_root).collect()`, instantiate `SessionRegimeConfig` from the CLI values, call the Phase 8 artifact writers, call the validation-export writer, and return the path `output_root / "phase8_regime_vwap_enriched.parquet"`.</action>
  <acceptance_criteria>
    - `src/vwap_revert/indicators/regime.py` contains `def write_regime_and_multi_timeframe_vwap_artifacts` and `def write_regime_and_multi_timeframe_validation_export`
    - `src/vwap_revert/indicators/regime.py` contains the exact strings `phase8_session_regimes.parquet`, `phase8_regime_vwap_enriched.parquet`, `regime_vwap_validation_export.csv`, and `validation_sessions.json`
    - `src/vwap_revert/indicators/regime.py` contains the exact strings `lookback_sessions`, `min_history_sessions`, `threshold_quantile`, `session_regimes_artifact`, and `enriched_artifact`
    - `src/vwap_revert/cli.py` contains `build-phase8-regime-vwap`
    - `src/vwap_revert/cli.py` contains `--cache-root`, `--output-root`, `--validation-date`, `--lookback-sessions`, `--min-history-sessions`, and `--threshold-quantile`
    - `python -m pytest tests/indicators/test_regime.py tests/indicators/test_regime_validation.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/indicators/test_regime.py tests/indicators/test_regime_validation.py -q</automated>
  </verify>
  <done>The repo can rebuild every Phase 8 artifact from the canonical cache through one CLI command, with deterministic validation exports and manifest metadata for manual regime/VWAP checks.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/indicators/test_regime.py tests/indicators/test_regime_validation.py -q`. Confirm the validation CSV is a filtered subset of `phase8_regime_vwap_enriched.parquet`, the session parquet contains one row per `trading_date`, and the manifest records `lookback_sessions` `20`, `min_history_sessions` `20`, and `threshold_quantile` `0.5`.
</verification>

<success_criteria>
Phase 08 is executable end to end: one CLI command rebuilds the session-regime summary, row-level weekly/monthly VWAP enrichment, and validation outputs from the canonical cache with deterministic filenames and metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/08-regime-&-multi-timeframe-vwap/08-03-SUMMARY.md`
</output>
